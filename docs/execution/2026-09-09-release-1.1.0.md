# Release 1.1.0

## Publication Contract

- User requested packaging and pushing. This authorizes production dist, commits and normal pushes to the established source and minimal-install repositories.
- Source baseline: 4b42fb8, origin nameisnt/ziyong, main. Install baseline: 2c9a92b, clone tmp/release-20260907, origin nameisnt/ziyong-dist, main.
- Include the three completed 2026-09-09 batches: status scheme scope, release/update settings and status web runtime, with their owned source/tests/docs and the release-note follow-up.
- All existing tracked modifications belong to these batches. Explicitly stage the owned new files; exclude other untracked docs, prototypes, image scripts, PDF/video materials and all tmp data.
- Install repository remains dist, manifest.json and i18n only. No source code, private fixtures or test backups there.
- Verification inherited from the unchanged implementation: final 662/662 unit tests, static/type/contracts/style/UI reuse checks, 33 status-web regression cases, 30 release/settings cases and prior status scope coverage. Release-note follow-up passed 4/4 focused tests. Rebuild production output and recheck release settings UI after the added note.
- Fetch and require fast-forward publication. No force push or history rewriting. Compare both published dist trees and versions after pushing.

## Preflight Results

- Both fetched main branches match local HEAD (ahead/behind 0/0); install worktree was clean before synchronization.
- `pnpm build`: passed in 20.68s. Existing chunk-size/dependency annotation warnings unchanged.
- Updated release note layout/interaction: 6/6 settings-release light/dark cases pass at 350x700, 390x844 and 430x900.
- Install dist synchronized only after verifying absolute source/target paths and the release clone's Git root; manifest and i18n copied from the source workspace.
- `git diff --check` passed in both repositories. Evidence: tmp/release-1.1.0-build.log and tmp/release-1.1.0-ui.log.

Status: release candidate verified; normal publication authorized. Remote commit IDs are reported after push.
