# 自动执行缺陷台账

更新时间：2026-08-16

只登记已经复现或由仓库状态直接证明的问题。代码审计意见、未来增强和归档方案旧待办不进入本表。状态只使用：`已发现`、`处理中`、`已验证`、`阻塞`、`不适用`。

## 当前缺陷

| 编号 | 优先级 | 可复现问题 | 共同根因 | 证据 | 目标批次 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| D-EXEC-001 | P0 | 自动执行协议要求工作树归属、缺陷台账和批次状态，但仓库此前没有实际记录载体 | 协议只有模板，没有当前执行状态 | E00 检查时 `docs/execution/` 不存在 | E00 | 已验证 |
| D-PUBLISH-001 | P0 | 安全推送会忽略已经暂存但尚未存在于 `origin/main` 的第 07 号总则、`docs/archive/` 和其他 `src` 外新文件 | 临时索引只执行 `git add --update .` 和 `git add --all src` | dry-run 已显式纳入第 07 号总则、`docs/archive/`、`docs/execution/` 和允许的验证脚本 | E01 | 已验证 |
| D-PUBLISH-002 | P0 | 从 `origin/main` 重新克隆后，`pnpm verify` 会缺少契约、样式和单元测试脚本 | `package.json` 已引用文件，但脚本未被 Git 跟踪且安全推送忽略 `scripts/` 新文件 | 契约/样式脚本和 `scripts/unit/` 已进入明确发布树；本地 `pnpm verify` 全部通过；远端落地等待后续提交推送 | E01 | 已验证 |
| D-PUBLISH-003 | P0 | 普通提交候选与当前工作区验证候选不同，且都不能单独表示当前成果 | E01/E02 文档和基线已暂存，但生成该基线的源码与场景修改仍在工作区 | R01 后 index 与工作区差异为 0；50 个变更文件与 safe-push dry-run 候选一致；index 场景不含 `side-swipe-back`且基线为 204×3；保护资料为 0 | R01 | 已验证 |
| D-DOC-001 | P1 | `docs/07-当前代码与UI复用审计及设计要求.md` 要求同时遵守 `.agents/AGENT-v2.md`，但该文件未跟踪且远端不存在 | 当前总则依赖本地文件 | 公开总则已取消 `.agents/AGENT-v2.md` 依赖，改为根目录 `AGENTS.md` 与第 07 号文档双入口；`.agents/` 保留为本地忽略文件 | E01 | 已验证 |
| D-PUBLISH-004 | P1 | 普通 `git add -A` 可能把本地备份、视觉截图和用户预设加入仓库 | 本地资料未被 `.gitignore` 完整覆盖 | `.gitignore` 已覆盖 `.agents/`、`backups/`、`ui-review/`、`日记 (1).json`；`git check-ignore` 和 dry-run 排除断言均通过 | E01 | 已验证 |
| D-PUBLISH-005 | P0 | 安全推送脚本把“本地 main 已提交且仅领先 origin/main”误判为分叉，无法推送已验证检查点 | 同步分支只识别 HEAD 等于远端或 HEAD 是远端祖先，末尾更新引用也假定本地仍停在远端旧提交 | P01 增加远端祖先、干净工作树、候选树等于 HEAD tree 和远端 SHA 四重校验；2 项契约测试、完整静态验证、local-ahead dry-run 和正式脚本推送均通过 | P01 | 已验证 |
| D-EXEC-002 | P0 | README 已指向 U01C，但工作树、缺陷基线和总状态仍把旧 SHA、U01A 或“进行中”写成当前状态 | 历史快照与当前控制状态没有明确覆盖关系，自动执行可能选择错误基线或批次 | R02 以 `edcb741` 建立唯一当前覆盖基线；44 个 U01 组件族合计 159 条并有确定性选择规则；状态矛盾搜索归零 | R02 | 已验证 |
| D-ENV-001 | P1 | 受限环境运行 `pnpm verify` 时，所有 Node 测试会因 `spawn EPERM` 失败；沙箱外相同代码通过 | 测试运行器需要创建子进程，环境禁止 spawn | 判定为执行环境能力限制，不修改业务代码或增加静默回退；允许子进程的本地环境中 `verify`/`verify:full` 均通过 | E02 | 不适用 |
| D-VERIFY-001 | P1 | `pnpm verify` 通过不代表完成方案要求的构建、视觉场景和严格 UI 复用检查 | `verify` 命令未包含 build、ui:check 和 `-Strict` | `pnpm verify` 已固定执行增量静态守卫、`-Strict`、构建和 3 个关键交互×3 尺寸；`verify:full` 执行 204×3 全量比较 | E02 | 已验证 |
| D-UI-TEST-001 | P1 | 视觉检查默认窄屏仍为 `360x700`，与当前协议要求 `350x700` 不一致 | 脚本默认尺寸未更新 | 默认尺寸及默认参数回退已统一为 `350x700`、`390x844`、`430x900`；全量报告实测 612 次 | E02 | 已验证 |
| D-UI-TEST-002 | P1 | 视觉检查固定使用 5179；`--list` 仍先构建并启动预览，受限环境还会因 esbuild `spawn EPERM` 失败 | 列表发现、构建和预览生命周期耦合 | `--list` 已改为构建后从 `file://` 发现场景，不启动 preview；目标场景使用系统分配端口，连续两次成功且无进程/5179 端口残留 | E01 | 已验证 |
| D-BASELINE-001 | P1 | 3 条 ESLint 警告和样式 `!important 20/20` 没有机器可读的遗留基线 | 现有检查只有总结果，没有增量比较 | `scripts/baselines/eslint-warnings.json` 按文件＋规则＋消息锁定 3 条；正常基线通过，故意删除一条的临时基线退出 1；样式 20/20 守卫继续通过 | E02 | 已验证 |
| D-BASELINE-002 | P1 | 当前没有与执行状态绑定的三尺寸全量视觉基线 | 旧报告位于忽略的 `tmp`，且 390/430 最近只运行了 6 个相关场景 | `scripts/baselines/ui-visual.json` 由 204×3 实际运行生成；自检清理 21 条检测器误报后，最终 612 次 0 fail/0 warning，第二轮全量比较通过 | E02 | 已验证 |
| D-TRACE-001 | P0 | Q1—Q8 没有从需求追溯到缺陷、批次、权威入口和验证证据 | 执行目录只有归属、缺陷和批次三份独立记录 | D00 前 `rg` 只在第 07 号总则命中 Q1/Q8；已新建 `03-需求追踪与能力矩阵.md` | D00 | 已验证 |
| D-BATCH-001 | P0 | 现有 U02、GEN01、READ01、CRUD01 各自同时覆盖多个根因 | 初始批次按大业务域打包，没有严格应用“一批一根因” | D00 已在批次状态中拆为 U01—U05、GEN01—GEN03、READ01—READ03、CRUD01—CRUD02/SETTINGS01 | D00 | 已验证 |
| D-CAP-001 | P1 | 方案要求跨 App 能力逐项通过或标记不适用，但执行记录没有 App 正式身份和能力入口矩阵 | 显示名、源码目录和 registry 可选能力未转换为验收记录 | D00 确认 App registry 权威入口和 43 个静态正式 App，并建立共享能力入口表；页内 CRUD 保持待逐 App 验收 | D00/CRUD01 | 已验证 |
| D-GUARD-001 | P1 | 当前严格 UI 复用检查通过，但不会阻止图标按钮缺名称、明显无事件按钮、硬编码非业务颜色或重复标签页基础样式 | `check-ui-reuse.ps1` 只有 4 组逐行 CSS 正则，没有 Vue template 语义、精确遗留签名和规则失败测试 | `ui-contract-check.mjs` 已检查图标按钮 title/aria、明显惰性 `type=button`、受保护控件局部尺寸/硬编码主题色；8 项规则测试和 `verify:static` 通过；279 个旧命中按文件＋规则＋消息＋元素/选择器签名锁定，新命中 0 | E03A | 已验证 |
| D-BUILD-001 | P1 | 普通批次执行 `pnpm verify` 会直接改写正式 `dist`，即使该批契约未授权修改发布产物 | `verify`/`verify:full` 与正式发布构建共用 `pnpm build`和 `dist` 输出 | 新增 `build:check -> tmp/build-check`，`verify`/`verify:full` 只调用临时构建；2 项契约测试和 `pnpm verify` 通过；构建前后 dist SHA-256 一致且工作区 dist diff 为 0 | E03B | 已验证 |
| D-INTERACT-001 | P1 | 204 个视觉场景中，通用检查器只对 3 个场景执行了专用操作后状态断言 | 已有返回、删除、目录和持久化动作散落在视觉 harness，默认 `verify:ui` 只运行 3 个手写场景；七类交互没有单一清单，重新解析也没有浏览器真实点击断言 | `ui-interaction-contracts.mjs` 现固定七类唯一代表场景；默认 `verify:ui` 直接消费清单；3 项契约单测、7 场景×3 尺寸和完整 `pnpm verify` 通过，21 次 0 finding | E04 | 已验证 |
| D-VISUAL-001 | P1 | 当前官方基线为 0 warning 时，仍不能证明字号、颜色、间距和日夜主题符合方案 | 基线只保存 `scenario/size/findings`，不保存截图内容或关键 computed-style；精确 RGBA 哈希又受同环境少量渲染像素波动影响 | E05A 对五场景三尺寸记录精确计算样式和 64 位感知截图哈希；固定最大汉明距离 4；3 项契约单测、15/15 独立比较和完整 `pnpm verify` 通过 | E05A | 已验证 |
| D-VISUAL-002 | P1 | `reader-catalog` 单独运行会报 `Reader detail shell is missing`，但在全量场景顺序中曾通过 | 视觉场景依赖此前场景遗留的阅读数据，缺少场景级数据隔离或自包含 fixture | E05B 改为等待 `.pc-reader-detail-shell` 就绪；单独三尺寸、与 `home` 混排六次及完整 36 次浏览器验证均通过 | E05B | 已验证 |
| D-STRUCT-VISUAL-001 | P1 | 四个 reader 视觉场景的 fixture、动作和断言散落在 3700+ 行协调器两处，时序修复必须直接修改巨型文件 | reader 场景没有独立域模块和单一 dispatch，协调器同时承担域逻辑与全局编排 | E05C 先以结构测试复现模块缺失，再迁移四个场景至 `readerScenarios.ts`；结构单测、12 次定向浏览器运行及完整 `pnpm verify` 通过，场景仍为 204 个 | E05C | 已验证 |
| D-STRUCT-PHONE-001 | P1 | `PhoneOverlay.vue` 仍以约 1065 行脚本同时拥有首页拖拽/文件夹、窗口位置、toast 桥接、标题适配和路由滚动，修改任一生命周期都要进入同一协调器 | 壳层行为没有按所有权拆为内部 composable；根组件既装载 App 又实现全局桥接与页面生命周期 | S01A—S01D 唯一迁出四类壳生命周期；S01E 将首页布局、拖拽、文件夹、上下文刷新及专属样式迁入 `PhoneHome`；根组件降至 662 行 | S01A—S01E | 已验证 |
| D-STRUCT-READER-001 | P1 | `ReaderApp.vue` 已把文本编辑弹窗拆出，但选区读取、重复命中、会话开关、编辑期间并发校验和原楼层安全写回仍共同留在约 834 行根脚本中 | 弹窗只是视图组件，文本编辑会话没有唯一内部所有者；根组件同时协调聊天加载、阅读导航和编辑事务 | S02A 唯一迁入 `useReaderTextEditSession`；结构与既有工具 4/4、三尺寸 6/6、248/248 单元及完整门禁通过，根组件不再声明旧会话状态或事务函数 | S02A | 已验证 |
| D-STRUCT-READER-002 | P1 | Reader 的当前/历史聊天读取、历史角色与文件定位、请求竞态、加载/错误/缓存状态及详情失效回退仍共同位于根组件 | 聊天来源加载没有独立会话所有者；根组件同时拥有阅读规则 UI、导航、编辑和约 150 行异步来源协调 | S02B 唯一迁入 `useReaderChatSession`，以只读 ref 返回消息/加载/错误状态；结构与相邻会话 5/5、三尺寸 18/18、249/249 单元及完整门禁通过 | S02B | 已验证 |
| D-STRUCT-RECOVERY-001 | P1 | `RecoveryApp.vue` 同时渲染备份只读详情、导入确认和导入结果，并在根组件持有目录、目标角色、导入及跳转状态 | 三个连续路由页没有页面级所有者；根组件还同时承担设置快照、查重、清理和书架事务 | S03A 唯一迁入 `RecoveryReadImportFlow`；结构/安全 2/2、三尺寸 9/9、250/250 单元及完整门禁通过，RecoveryApp 降至 1393 行/473 行脚本 | S03A | 已验证 |
| D-STRUCT-RECOVERY-002 | P1 | Recovery 的完全查重与小备份清理两页仍在根组件共同持有阈值、三组选择、扫描进度、分组映射和删除确认事务 | 聊天备份维护没有页面级会话所有者；根组件同时渲染书架、设置快照和两类维护流程 | S03B 唯一迁入常驻 `RecoveryMaintenanceFlow`，返回候选阅读不丢选择；结构/安全 3/3、三尺寸 6/6、251/251 单元及完整门禁通过 | S03B | 已验证 |
| D-TEST-RECOVERY-SETTINGS-001 | P1 | Recovery 设置快照列表有 `recovery-settings` 场景，但设置查重路由没有独立渲染/三尺寸证据 | 视觉 fixture 只注入快照列表，未提供 settings duplicate result，结构拆分会失去页面级回归护栏 | S03C 新增隔离 `recovery-settings-duplicates` 场景；契约 1/1、正式 272×3＝816/816、新场景比较 3/3、252/252 单元及完整门禁通过 | S03C | 已验证 |
| D-STRUCT-RECOVERY-003 | P1 | Recovery 设置快照列表与设置查重仍在根组件共同持有创建、恢复、删除、扫描、选择与批量删除事务 | 设置快照管理没有页面级会话所有者；根组件仍同时协调书架/角色分组和设置文件管理 | S03D 唯一迁入 `RecoverySettingsFlow`；结构/安全 2/2、两场景三尺寸 6/6、253/253 单元及完整门禁通过，RecoveryApp 降至 483 行/135 行脚本 | S03D | 已验证 |
| D-TEST-ARCHIVE-FLOOR-001 | P1 | `archive-floor-backup` 场景只有在“阅读备份”按钮碰巧可用时才进入阅读页，未显式建立楼层备份夹具 | 场景把核心断言放在 `if (!disabled)` 可选分支，空 IndexedDB 也可通过，无法保护阅读页、楼层正文/思维链与底部操作 | S04A 先等待启动自动备份，再用现有 v1 API 写入固定 2 层/思维链夹具并强制角色/聊天刷新；静态 1/1、三尺寸 3/3、254/254 单元及完整门禁通过 | S04A | 已验证 |
| D-STRUCT-ARCHIVE-001 | P1 | ChatArchive 的楼层备份阅读页模板、消息/思维链展示和专属样式仍与角色索引、详情、改名/迁移及备份事务共同位于 1452 行根组件 | 已有连续 `floor-backup` 路由和确定性隔离场景，但没有页面级视图所有者；修改阅读布局仍需进入真实事务协调器 | S04B 唯一迁入 `ChatArchiveFloorBackupPage`，导出/恢复事务仍在根；结构/思维链 4/4、三尺寸 3/3、255/255 单元及完整门禁通过 | S04B | 已验证 |
| D-STRUCT-ARCHIVE-002 | P1 | ChatArchive 根脚本仍以约 865 行同时拥有角色卡读取、楼层备份归组、聊天列表读取、请求竞态、scope 映射、选择状态与真实写事务 | 角色/聊天目录没有内部会话所有者；根组件的写事务直接依赖一大组目录状态和帮助函数 | 角色/聊天读取、归组、请求竞态、scope 映射、选择与路由已唯一迁入 `useChatArchiveCatalogSession`；根仅消费状态/刷新入口并继续唯一持有写事务 | S04C | 已验证 |
| D-UI-BUTTON-001 | P1 | `PromptsApp.vue` 的 26 个图标按钮全部缺少 `aria-label`，其中 12 个同时没有 `title` | 提示词页的图标操作没有按全局图标按钮语义同时提供可见提示和可访问名称 | U01A 定向测试由 38 条失败转为 0；精确基线只删除该文件 38 条签名，实际遗留由 279 降至 241；33 次提示词域浏览器回归和完整验证通过 | U01A | 已验证 |
| D-UI-BUTTON-002 | P1 | MVU 修改器主页面和树节点的 12 个图标按钮已有准确 `title`，但全部缺少 `aria-label` | MVU 组件族的图标操作只实现鼠标悬停提示，没有同步可访问名称 | U01B 定向测试由 12 条失败转为 0；精确基线只删除主页面 8 条和树节点 4 条；MVU 三尺寸场景及完整验证通过 | U01B | 已验证 |
| D-UI-BUTTON-003 | P1 | `RecoveryApp.vue` 的 6 个图标按钮已有准确 `title`，但全部缺少 `aria-label` | 恢复管理组件族只提供鼠标悬停标题，没有同步可访问名称 | 定向测试由精确 6 条失败转为 1/1 通过；基线只删除该文件 6 条；recovery 27/27 与完整验证通过 | U01C | 已验证 |
| D-UI-BUTTON-004 | P1 | 三个共享生成组件的 8 个图标按钮已有准确 `title`，但全部缺少 `aria-label` | 生成表单、渠道字段和任务中心共享组件只提供鼠标悬停标题，没有同步可访问名称 | 定向测试由精确 8 条失败转为 1/1 通过；基线只删除三文件 8 条；生成/任务 12/12 与完整验证通过 | U01D | 已验证 |
| D-UI-BUTTON-005 | P1 | App 构建器目录和自制 App 宿主的 7 个图标按钮已有准确 `title`，但全部缺少 `aria-label` | App 构建器组件族只提供鼠标悬停标题，没有同步可访问名称 | 定向测试由精确 7 条失败转为 1/1 通过；基线只删除两文件 7 条；custom-app 6/6 与完整验证通过 | U01E | 已验证 |
| D-UI-BUTTON-006 | P1 | 条目库目录、绑定和查重页的 7 个图标按钮已有准确 `title`，但全部缺少 `aria-label` | 条目库组件族只提供鼠标悬停标题，没有同步可访问名称 | 定向测试由精确 7 条失败转为 1/1 通过；基线只删除三页面 7 条；条目库 9/9 与完整验证通过 | U01F | 已验证 |
| D-UI-BUTTON-007 | P1 | 预设管理目录、详情、条目编辑和条目行的 7 个图标按钮已有准确 `title`，但全部缺少 `aria-label` | 预设管理组件族只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 Catalog 3、Detail 1、Editor 1、PromptRow 2 条；修复后定向测试 1/1、三尺寸 21/21、完整 `pnpm verify` 89/89 单测与 36/36 UI、15/15 外观证据通过 | U01G | 已验证 |
| D-UI-BUTTON-008 | P1 | 八股规则管理中的新增、恢复默认和删除共 6 个图标按钮已有准确 `title`，但缺少 `aria-label` | BaguApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 6 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 90/90 单测与 36/36 UI、15/15 外观证据通过 | U01H | 已验证 |
| D-UI-BUTTON-009 | P1 | 番外书籍概览的生成、编辑、删除、排序和总结操作共 6 个图标按钮已有准确 `title`，但缺少 `aria-label` | ExtrasBookOverviewPage 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 6 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 91/91 单测与 36/36 UI、15/15 外观证据通过 | U01I | 已验证 |
| D-UI-BUTTON-010 | P1 | 论坛目录与板块页的生成、发帖、新建、编辑和删除共 6 个图标按钮已有准确 `title`，但缺少 `aria-label` | ForumBoardPage/ForumCatalogPage 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 Board 2、Catalog 4 条；修复后定向测试 1/1、三尺寸 6/6、完整 `pnpm verify` 92/92 单测与 36/36 UI、15/15 外观证据通过 | U01J | 已验证 |
| D-UI-BUTTON-011 | P1 | 资料目录、资料表编辑和管理页共 6 个图标按钮已有准确 `title`，但缺少 `aria-label`；管理页没有独立视觉场景 | profiles 三页纯图标操作未同步可访问名称，验证目录未覆盖 tables 路由 | T0 精确复现 3+2+1 条；新增管理页场景并经官方 615 次基线验证；修复后定向 9/9、完整 `pnpm verify` 93/93 单测与 36/36 UI、15/15 外观证据通过 | U01K | 已验证 |
| D-UI-BUTTON-012 | P1 | 正则向导的字段排序/删除与结果复制共 6 个图标按钮已有准确 `title`，但缺少 `aria-label` | RegexWizardApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 6 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 94/94 单测与 36/36 UI、15/15 外观证据通过 | U01L | 已验证 |
| D-UI-BUTTON-013 | P1 | 关系网的 AI 识别、人物与单向关系增删共 6 个图标按钮已有准确 `title`，但缺少 `aria-label` | RelationshipApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 6 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 95/95 单测与 36/36 UI、15/15 外观证据通过 | U01M | 已验证 |
| D-UI-BUTTON-014 | P1 | 工作台的新建流程、刷新预设、新增步骤及步骤排序/删除共 6 个图标按钮已有准确 `title`，但缺少 `aria-label` | WorkbenchApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 6 条；修复后定向测试 1/1、三场景三尺寸 9/9、完整 `pnpm verify` 96/96 单测与 36/36 UI、15/15 外观证据通过 | U01N | 已验证 |
| D-UI-BUTTON-015 | P1 | 写卡工坊的成品库入口、世界书刷新与成品复制/导入/删除共 5 个图标按钮已有准确 `title`，但缺少 `aria-label` | CardWriterApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 5 条；修复后定向测试 1/1、两场景三尺寸 6/6、完整 `pnpm verify` 97/97 单测与 36/36 UI、15/15 外观证据通过 | U01O | 已验证 |
| D-UI-BUTTON-016 | P1 | 云媒体的 AI 生成入口、服务设置、配置增删与密钥显隐共 5 个图标按钮已有准确 `title`，但缺少 `aria-label` | CloudMediaApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 5 条；修复后定向测试 1/1、三场景三尺寸 9/9、完整 `pnpm verify` 98/98 单测与 36/36 UI、15/15 外观证据通过 | U01P | 已验证 |
| D-UI-BUTTON-017 | P1 | 日记书架的生成、批量生成、排序、重命名和删除共 5 个图标按钮已有准确 `title`，但缺少 `aria-label` | DiaryBookPage 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 5 条；修复后定向测试 1/1、两场景三尺寸 6/6、完整 `pnpm verify` 99/99 单测与 36/36 UI、15/15 外观证据通过 | U01Q | 已验证 |
| D-UI-BUTTON-018 | P1 | 阅读首页刷新/规则开关、阅读工具入口和两个共享阅读弹窗关闭按钮共 5 个图标按钮已有准确 `title`，但缺少 `aria-label`；文本编辑与思维链弹窗没有隔离视觉场景 | reader-shared 四组件的纯图标操作只提供鼠标悬停标题，视觉目录未覆盖两个共享弹窗 | T0 精确复现 2+1+1+1 条；新增两项只打开不保存的内存场景并经官方 621 次基线验证；修复后定向 12/12、完整 `pnpm verify` 100/100 单测与 36/36 UI、15/15 外观证据通过 | U01R | 已验证 |
| D-UI-BUTTON-019 | P1 | 时间确认页的写入输入框、保存/删除全局历法、新增/删除人物共 5 个图标按钮已有准确 `title`，但缺少 `aria-label` | TimekeeperApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 5 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 101/101 单测与 36/36 UI、15/15 外观证据通过 | U01S | 已验证 |
| D-UI-BUTTON-020 | P1 | 世界书目录刷新、应用/捕获/停止联动和删除条目共 5 个图标按钮已有准确 `title`，但缺少 `aria-label` | worldbook-link 三页面的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1+3+1 条；修复后定向测试 1/1、三场景三尺寸 9/9、完整 `pnpm verify` 102/102 单测与 36/36 UI、15/15 外观证据通过 | U01T | 已验证 |
| D-UI-BUTTON-021 | P1 | 聊天档案的刷新角色、刷新聊天、迁移到当前聊天和跳转酒馆聊天共 4 个图标按钮已有准确 `title`，但缺少 `aria-label` | ChatArchiveApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 4 条；修复后定向测试 1/1、三场景三尺寸 9/9、完整 `pnpm verify` 103/103 单测与 36/36 UI、15/15 外观证据通过 | U01U | 已验证 |
| D-UI-BUTTON-022 | P1 | 相册的新增、编辑、下载和删除共 4 个图标按钮已有准确 `title`，但缺少 `aria-label` | GalleryApp 的部分纯图标操作未同步可访问名称 | T0 精确复现 4 条且上一张/下一张已合格；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 104/104 单测与 36/36 UI、15/15 外观证据通过 | U01V | 已验证 |
| D-UI-BUTTON-023 | P1 | 主题页两个返回、清除 App 图标覆盖和导入字体共 4 个图标按钮已有准确 `title`，但缺少 `aria-label` | ThemeApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 4 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 105/105 单测与 36/36 UI、15/15 外观证据通过 | U01W | 已验证 |
| D-UI-BUTTON-024 | P1 | 视频页的新增、编辑、下载和删除共 4 个图标按钮已有准确 `title`，但缺少 `aria-label` | VideoApp 的部分纯图标操作未同步可访问名称 | T0 精确复现 4 条且上一个/下一个已合格；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 106/106 单测与 36/36 UI、15/15 外观证据通过 | U01X | 已验证 |
| D-UI-BUTTON-025 | P1 | 手机主页文件夹管理的前移、移出到主界面和后移共 3 个图标按钮已有准确 `title`，但缺少 `aria-label` | PhoneOverlay 文件夹控制的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 3 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 107/107 单测与 36/36 UI、15/15 外观证据通过 | U01Y | 已验证 |
| D-UI-BUTTON-026 | P1 | 正则显示页的新增规则、提高优先级和降低优先级共 3 个图标按钮已有准确 `title`，但缺少 `aria-label` | RegexDisplayApp 的部分纯图标操作未同步可访问名称 | T0 精确复现 3 条且复制/删除已合格；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 108/108 单测与 36/36 UI、15/15 外观证据通过 | U01Z | 已验证 |
| D-UI-BUTTON-027 | P1 | 总结书架的生成总结/排序和导入页的刷新楼层共 3 个图标按钮已有准确 `title`，但缺少 `aria-label` | summary 两页面的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 2+1 条；修复后定向测试 1/1、两场景三尺寸 6/6、完整 `pnpm verify` 109/109 单测与 36/36 UI、15/15 外观证据通过 | U01AA | 已验证 |
| D-UI-BUTTON-028 | P1 | ComfyUI 页的读取模型和新建工作流共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | ComfyApp 的部分纯图标操作未同步可访问名称 | T0 精确复现 2 条且两个折叠按钮已合格；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 110/110 单测与 36/36 UI、15/15 外观证据通过 | U01AB | 已验证 |
| D-UI-BUTTON-029 | P1 | 摘录页的手动新增和 AI 摘抄共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | DigestApp 的部分纯图标操作未同步可访问名称 | T0 精确复现 2 条且动态排序名称保持合格；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 111/111 单测与 36/36 UI、15/15 外观证据通过 | U01AC | 已验证 |
| D-UI-BUTTON-030 | P1 | 书信书架的生成回信和动态排序共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | LettersBookPage 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 2 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 112/112 单测与 36/36 UI、15/15 外观证据通过 | U01AD | 已验证 |
| D-UI-BUTTON-031 | P1 | 连接设置页的显示/隐藏 API Key 和获取模型列表共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | SettingsConnectionPanel 的部分纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 2 条且其余 5 个图标按钮已合格；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 113/113 单测与 36/36 UI、15/15 外观证据通过 | U01AE | 已验证 |
| D-UI-BUTTON-032 | P1 | 统计页的动态刷新和内容统计折叠共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | StatsApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 2 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 114/114 单测与 36/36 UI、15/15 外观证据通过 | U01AF | 已验证 |
| D-UI-BUTTON-033 | P1 | 剧情线编辑页的增加关联资料和移除关联共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | StorylineEditorPage 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 2 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 115/115 单测与 36/36 UI、15/15 外观证据通过 | U01AG | 已验证 |
| D-UI-BUTTON-034 | P1 | 小剧场历史页的动态排序和标签筛选共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | TheaterHistoryPage 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 2 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 116/116 单测与 36/36 UI、15/15 外观证据通过 | U01AH | 已验证 |
| D-UI-BUTTON-035 | P1 | 教程代码块复制和 App 目录打开共 2 个图标按钮已有准确 `title`，但缺少 `aria-label` | tutorial 组件族两个页面的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1+1 条；修复后定向测试 1/1、两场景三尺寸 6/6、完整 `pnpm verify` 117/117 单测与 36/36 UI、15/15 外观证据通过 | U01AI | 已验证 |
| D-UI-BUTTON-036 | P1 | 楼层插入页的插入模板快捷图标按钮已有准确动态 `title`，但缺少 `aria-label` | ChatInsertApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 118/118 单测与 36/36 UI、15/15 外观证据通过 | U01AJ | 已验证 |
| D-UI-BUTTON-037 | P1 | 共享失败草稿列表的删除图标按钮已有准确动态 `title`，但缺少 `aria-label` | FailedDraftList 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 119/119 单测与 36/36 UI、15/15 外观证据通过 | U01AK | 已验证 |
| D-UI-BUTTON-038 | P1 | 插件文件仓库的刷新仓库清单图标按钮已有准确 `title`，但缺少 `aria-label` | FileRepositoryApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 120/120 单测与 36/36 UI、15/15 外观证据通过 | U01AL | 已验证 |
| D-UI-BUTTON-039 | P1 | 媒体生成页的 AI 填写参数图标按钮已有准确 `title`，但缺少 `aria-label` | MediaGenerateApp 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 121/121 单测与 36/36 UI、15/15 外观证据通过 | U01AM | 已验证 |
| D-UI-BUTTON-040 | P1 | 音乐页的新增音乐图标按钮已有准确 `title`，但缺少 `aria-label` | MusicApp 的新增纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 122/122 单测与 36/36 UI、15/15 外观证据通过 | U01AN | 已验证 |
| D-UI-BUTTON-041 | P1 | 预设绑定页的刷新预设状态图标按钮已有准确 `title`，但缺少 `aria-label` | PresetLinkApp 的刷新纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 123/123 单测与 36/36 UI、15/15 外观证据通过 | U01AO | 已验证 |
| D-UI-BUTTON-042 | P1 | 共享资料条目选择器的查看资料图标按钮已有准确动态 `title`，但缺少 `aria-label` | ProfileEntryPicker 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 124/124 单测与 36/36 UI、15/15 外观证据通过 | U01AP | 已验证 |
| D-UI-BUTTON-043 | P1 | 提示词类型编辑页的新建分组图标按钮已有准确 `title`，但缺少 `aria-label` | PromptTypeEditorPage 的纯图标操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 125/125 单测与 36/36 UI、15/15 外观证据通过 | U01AQ | 已验证 |
| D-UI-BUTTON-044 | P1 | 场景编排页的新方案图标按钮已有准确 `title`，但缺少 `aria-label` | ScenePlannerApp 的纯图标新建操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 126/126 单测与 36/36 UI、15/15 外观证据通过 | U01AR | 已验证 |
| D-UI-BUTTON-045 | P1 | 共享可搜索下拉的展开按钮已有准确动态 `title`，但缺少 `aria-label` | SearchableCombobox 的纯图标切换操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 127/127 单测与 36/36 UI、15/15 外观证据通过 | U01AS | 已验证 |
| D-UI-BUTTON-046 | P1 | 世界书槽位页的新增槽位图标按钮已有准确 `title`，但缺少 `aria-label` | WorldSlotsApp 的纯图标新建操作只提供鼠标悬停标题，没有同步可访问名称 | T0 精确复现 1 条；修复后定向测试 1/1、三尺寸 3/3、完整 `pnpm verify` 128/128 单测与 36/36 UI、15/15 外观证据通过 | U01AT | 已验证 |
| D-UI-TABS-001 | P1 | 全局 `.pc-segment-btn` 与普通文字按钮共用 `min-height: 36px`，不符合标签页 32px/13px 的权威密度 | 全局基础规则把两类不同控件合并在同一尺寸声明中 | 定向测试由精确失败转为 1/1；普通按钮保持 36px、标签页为 32px/13px；六场景修改后 18/18、完整 `pnpm verify` 129/129 单测与 36/36 UI、15/15 外观证据通过 | U02A | 已验证 |
| D-UI-TABS-002 | P1 | DISC-U02 初次把 7 个页面的 8 条局部 `.pc-segment-btn` 几何签名合并登记为“局部特殊覆盖” | 初次发现只按选择器外观聚合，尚未区分标准分段控件、紧凑模式控件、筛选项、选项卡片和仅宽度布局 | DISC-U02B 已逐项核对模板语义、局部声明和七场景三尺寸 21/21 证据，确认至少 6 类不同根因；本条仅保留为发现总账，不得直接建立实施批次 | DISC-U02B | 已验证（已拆分） |
| D-UI-TABS-003 | P1 | 正则向导三项“结果类型”标准分段控件局部把 `.pc-segment-btn` 字号覆盖为 12px、水平内边距覆盖为 4px | 标准三项分段控件重复改写全局标签页基础密度；只有 `min-width: 0` 属于第 10.6 节允许的三列宽度收缩 | 两段 T0 分别精确捕获字号与内边距；修复后定向 2/2、机器遗留精确减 1、三尺寸 3/3、完整 `pnpm verify` 的 130/130 单测与 36/36 UI、15/15 外观证据通过 | U02B | 已验证 |
| D-UI-TABS-004 | P1 | 设置页五项顶部导航在常规宽度使用 12px、窄于 380px 使用 11px，与全局 13px 规范冲突 | 五列窄屏适配通过 viewport 媒体规则缩小字号；这直接违反第 6.8 节“字号不随 viewport 宽度缩放”和第 10.6 节标签页 13px，且第 11.8 节禁止用编号例外绕过普通标签页 | 用户确认 07 文档覆盖旧语义；T0 精确列出 5 个声明，修复后定向/守卫 10/10、基线 1 个签名/2 个命中清零、三尺寸 3/3、完整 `pnpm verify` 的 132/132 单测与 36/36 UI、15/15 外观证据通过 | U02D | 已验证 |
| D-UI-TABS-005 | P2 | Comfy 参数来源模式选择器局部使用 30px/11px，并重新声明边框、背景和激活态 | 该控件是参数行内的紧凑三态选择器，不是页面导航；30px 符合第 10.6 节紧凑按钮高度，但 11px 低于 12—13px 可读字号，且局部重画了共享分段控件外观；既有窄屏规则还错误依赖 browser viewport | 两段 T0 分别精确列出共享语义缺失和错误 `@media`；修复后定向/回归 3/3、机器遗留精确减 1、隔离场景修改前后各三尺寸，正式视觉清单 208/624，完整 `pnpm verify` 133/133 单测与 36/36 UI、15/15 外观证据通过 | U02E | 已验证 |
| D-UI-TABS-006 | P1 | 教程页 8 个分类筛选项使用 `.pc-segment-btn`，同时局部改为 34px、独立背景和四列多行排列 | 实际语义是四列两行的同页单选筛选按钮组，并非 2—4 项的单行分段导航；当前共享类和 `<nav>` 语义均不一致 | T0 精确列出 7 项语义/复用缺口；修复后定向/回归 4/4、机器遗留精确减 1、修改前后三尺寸各 3/3、完整 `pnpm verify` 134/134 单测与 36/36 UI、15/15 外观证据通过 | U02F | 已验证 |
| D-UI-TABS-007 | P1 | 提示词变体选择使用 `.pc-segment-btn`，但局部要求 38px、高度自适应和文字换行，并以两列选项卡片排列 | 实际语义是注册式 App 提供的动态提示词选项，名称与数量不固定，必须允许长标题换行；不是单行分段标签，也不能靠缩短内建文案破坏扩展能力 | T0 精确列出 6 项语义/复用缺口；修复后定向/前批回归 5/5、机器遗留精确减 1、修改前后三尺寸各 3/3、完整 `pnpm verify` 135/135 单测与 36/36 UI、15/15 外观证据通过 | U02G | 已验证 |
| D-UI-TABS-008 | P2 | 角色档案固定 40px 双图标切换和世界书五项等宽栏目分别使用 0px、6px 水平内边距，却仍被机器基线计入“局部几何遗留” | 两处内边距分别与固定图标宽度、350px 五列含数量布局直接绑定，且没有覆盖高度或字号；守卫按选择器记账，缺少带业务原因的编号例外与防扩张契约 | T0 精确列出两个缺失编号原因；实现后定向/守卫 10/10、基线精确减 2、两场景三尺寸 6/6、完整 `pnpm verify` 的 131/131 单测与 36/36 UI、15/15 外观证据通过 | U02C | 已验证 |
| D-UI-FIELD-001 | P1 | 共享 `.pc-field/.pc-select/.pc-area` 只写 `font: inherit` 和 10px 垂直 padding，没有显式 14px；隔离场景实际计算为 16px，输入 43px、下拉 45px且 `min-height: 0` | 插件根节点未隔离正文字号，字段继续继承视觉宿主/浏览器字号；全局字段入口也没有显式保证第 10.6 节要求的输入/下拉至少 40px | T0 精确失败 5 项；修复后四场景逐项为 14px、输入 41px/下拉 43px且最低 40px，九场景 27/27、外观 6/6、136/136 单测与完整验证通过 | U03A | 已验证 |
| D-UI-FIELD-002 | P2 | App 工坊、条目库收集、教程首页和教程 App 目录共 4 个搜索框用 `.pc-field` 加局部 `padding-left: 40px` 手工复刻搜索控件 | 已有权威 `.pc-search-field`，四处仍各自绝对定位图标并覆盖字段内边距 | T0 精确列出 20 项重复结构；修复后定向/相邻 3/3、机器遗留减 4、修改前后三尺寸各 12/12、137/137 单测与完整验证通过 | U03B | 已验证 |
| D-UI-FIELD-003 | P1 | 22 个 `.pc-area` 机器签名分别把紧凑文本域改为 82/90/100/120/180px，或把普通文本域改为 0/120/150/160/220/240/260/300px | `compact` 和普通 area 同时承担短备注、生成要求、预览、长正文和全高编辑器，现有共享语义不足，导致各 App 用 scoped CSS 重定义基础几何 | U03D—U03F 清零短输入、预览/测试和 6 个 multiline 签名；U03I 把快速短语/格式模板统一为 120px；U03K 按真实 96px 起点迁移解析样本到 160px；U03L 把 8 类普通长正文统一为 260px；U03M 清理无消费者父级规则。最终机器遗留降至 39/39，完整门禁通过 | U03D—U03F、U03I、U03K—U03M | 已验证 |
| D-UI-FIELD-006 | P2 | U03G 初始把 Prompts 260/180px 与 Extras 220px 父级 `.pc-area` 规则一并判断为组件拆分后的死样式 | T0 模板截断漏读 Prompts 真实控件；完整回滚后用户确认重新拆分。Extras 在唯一直属 area 改为 multiline 后确实无普通 area 消费者 | U03H T0 精确 2 项；删除无消费者规则、人工探针及过期历史断言后，定向 2/2、机器 44/44→43/43、真实摘要三尺寸 3/3 同哈希；首次完整验证暴露 1 条过期 U03F 守卫，契约内修正后 142/142 单测与完整验证通过 | U03G 初始契约、U03H | 已验证；Prompts 进入 U03I 起独立语义批次 |
| D-UI-FIELD-004 | P2 | 猜数字输入把通用字段改为 22px 粗体，版本导航把通用字段改为 42×42px 并重写 padding/行高 | 两者都是特殊数字控件，却复用普通文本字段后各自重画基础几何/排版；产品语义不同，不能与普通字段批次合并 | U03N/U03O 均先精确失败 5 项，再各迁移到专用共享语义并补三尺寸场景；最终机器 39→37、正式 222/666、148/148 单测与完整验证通过，输入/版本业务未变 | U03N（游戏输入）、U03O（版本步进器） | 已验证 |
| D-UI-FIELD-005 | P2 | Workbench 把 `.pc-field-group span` 混入局部辅助文字组合规则再次声明 12px | 全局 `.pc-field-group > span` 已为 12px/800，局部字号声明冗余并扩大选择器影响面 | T0 精确证明局部规则重复命中；修复后定向 2/2、基线减 1、修改前后三尺寸各 3/3且截图哈希完全一致、138/138 单测与完整验证通过 | U03C | 已验证 |
| D-UI-GAME-001 | P2 | 猜数字页面在 350px 下顶部第三个统计卡把“状态/进行中”同时裁成省略文本 | 三个等宽统计卡在窄屏中容纳的内容长度不同，第三项需要比“次数/最佳”更宽的轨道 | G01 给猜数字统计区增加独立布局钩子，以 `0.8fr/0.8fr/1.4fr` 按内容分配宽度；定向契约 2/2，真实玩法场景 350/390/430 三尺寸逐项断言六段文字均无溢出，350px 截图人工核对通过 | G01 | 已验证 |
| D-UI-COMBO-001 | P1 | SearchableCombobox 菜单项的文字被固定为 `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` | 动态资源名称可以任意长，但共享组件主动裁掉唯一可辨识的完整名称，违反 07 的“长选项不被容器裁切”通过条件 | T0 精确失败 4 项且明暗 350px 截图均在“超长…”处截断；改为 normal/anywhere 后两行完整显示，223/669 全量视觉与完整验证通过 | U04A | 已验证 |
| D-TEST-COMBO-001 | P2 | 共享可搜索下拉只有点击、过滤、选择和重新打开的场景断言，没有键盘选择、Escape、外部点击关闭、长选项或夜间菜单证据 | 组件代码实现了相应行为，但自动回归无法阻止后续破坏；现有夜间连接场景也只显示关闭状态 | U04A 增加长名 fixture、方向键/Enter、Escape、点外关闭和 dark-long 场景；6/6 定向、149/149 单测与完整验证通过 | U04A | 已验证 |
| D-UI-SELECT-001 | P2 | ChatInsert 两个固定枚举与 ExtrasBookEditor 生成模式共 3 个原生 select 只使用 `pc-field`，未使用固定枚举权威类 `pc-select` | 控件语义写成普通字段；Extras 还把 `pc-field` 偷作 14px 间距钩子，不能把视觉接近当作继续误用的依据 | T0 精确失败 6 项；三个控件迁移到 `pc-select`，Extras 用专属布局类显式保留旧间距；两场景三尺寸前后逐图同哈希，150/150 单测及完整验证通过 | U04B | 已验证 |
| D-UI-SELECT-002 | P1 | 番外摘要正则、提示词类型分组、剧情节点所属剧情线两处共 4 个用户可增长资源仍使用原生 select | 这些选项来自用户创建的正则、分组和剧情线，数量/名称不固定，属于 07 明确要求的动态资源，不是固定短枚举 | 四项迁移后，内存长正则/分组/剧情线均在 350/390/430px 完整显示；正式 224/672、151/151 单测及完整验证通过 | U04C | 已验证 |
| D-UI-SELECT-003 | P1 | 设置→阅读的字体 select 只有 5 个内置项，无法选择 `settings.customFont.fonts` 中已导入字体 | 该页面用固定原生 select 且没有生成自定义字体选项；ThemeApp 留有未渲染的 reader 映射草稿，实际设置页没有接线 | 实际设置页迁移为可搜索选择器并接通 `custom:<id>`；内存导入字体可搜索、选择并映射 family，三尺寸完整长名，152/152 单测及完整验证通过 | U04D | 已验证 |
| D-UI-MODAL-001 | P1 | 9 个文件中的 10 个 dialog 定义都有遮罩点击关闭，但仅 3 个处理 Escape、1 个拦截手机返回、2 个主动聚焦；背景滚动没有统一锁定 | 项目没有共享弹窗生命周期入口，各弹窗分别补事件，导致同一关闭语义随页面漂移 | U05A/C/D 已把全部 10 个定义接到唯一生命周期；顶层栈、焦点、Escape、返回和引用计数滚动锁统一，Bagu/Reader/Creation 重复监听清除 | U05A/U05C/U05D | 已验证 |
| D-UI-MODAL-002 | P2 | 目录、创建方式、八股详情、文本编辑、思维链、内容迁移、解析提示、主页文件夹和提示词详情分别重复 mask/card 几何与主题；Catalog 关闭按钮还自画 38×38px 且无 aria-label | 缺少共享弹窗视觉语义，业务内容布局与遮罩/卡片基础样式混在 scoped CSS | `.pc-modal-backdrop` + `.pc-modal-dialog` 覆盖 10/10 定义；局部仅保留层级/偏移/模糊和业务尺寸布局；七场景三尺寸 21/21、完整 UI 51/51 | U06A/U06B | 已验证 |
| D-TEST-MODAL-001 | P1 | 正式“close”契约只点击 Prompts 关闭按钮；没有覆盖遮罩、Escape、手机返回、初始焦点或滚动锁定 | 现有截图证明弹窗能显示，却不能阻止共享关闭行为退化 | 正式交互扩为 Catalog、Prompts、Creation、Reasoning、Bagu、ReaderEdit、ContentTransfer；51/51 覆盖焦点、按钮、Escape、返回、遮罩和滚动恢复 | U05A/U05C/U05D | 已验证 |
| D-TEST-MODAL-002 | P2 | 现有 modal 场景能检查几何和关闭行为，但没有守卫所有 dialog 必须使用唯一 backdrop/dialog 视觉类，也没有证明基于 `.pc-section-card` 的弹窗在深色主题下为不透明表面 | 局部 scoped mask/card 即使继续复制也不会被 UI 复用脚本识别；截图存在不等于主题背景不透明 | 两条定向契约守卫全局类、10 个消费者及局部重复；ContentTransfer 正式交互场景强制深色并断言计算背景 alpha≥0.99；158/158 单测与 51/51 UI 通过 | U06A/U06B | 已验证 |
| D-UI-MENU-001 | P1 | ActionMenu 有点外与执行后关闭，但不处理 Escape/手机返回，panel 固定 `width:max-content` 且选项 `white-space:nowrap`，没有视口上限 | 共享菜单只覆盖鼠标主路径，任意长插槽文案可把 350px 窄屏挤出容器 | 共享组件已补 Escape/返回、220px 宽度与 60vh/320px 高度上限、纵向滚动和任意长词换行；5 个实例自动继承 | U05B | 已验证 |
| D-TEST-MENU-001 | P2 | ActionMenu 的主要视觉场景直接 `setAttribute('open','')`，没有验证 summary 点击、点外、执行后关闭、Escape 或长选项 | 自动回归绕开了组件的实际打开链和关闭链 | entry-library/comfy 及内部到达场景改为真实 summary；正式交互覆盖 Escape、返回、点外、执行后关闭和 350px 长标签 | U05B | 已验证 |
| D-GEN-LIFECYCLE-001 | P1 | 普通单次生成离开所属 App 后会被卸载钩子停止或失去页面局部 running/raw/error/结果状态，无法从任务中心恢复；只有批量日记、批量总结和工作台能持久跟踪 | 单次生成的生命周期由 15 个业务协调所有者分别持有；14 个在 scope dispose/unmount 主动停止，自制 App 虽不主动停止但卸载后仍没有稳定状态所有者；`GenerationTaskKindSchema` 只允许 `diary-batch`、`summary-batch`、`workbench` | GEN02A 建立持久基础；GEN02B—GEN02F 完成全部固定、动态及多阶段消费者迁移，27/27 离页 mock、176/176 单测与完整验证通过；批量 kind/恢复语义未改 | GEN02A—GEN02F | 已验证 |
| D-SUMMARY-001 | P1 | 总结失败草稿可编辑且“重新解析”按钮启用，但填入合法 `<result>` 后点击没有进入预览，也没有成功/失败反馈 | `SummaryApp.vue` 两处调用 `parseSimpleXmlResult` 却缺少运行时导入；环境声明掩盖静态错误，Vue 事件层接住浏览器 `ReferenceError`，形成静默失败 | GEN03A 独立场景先复现按钮启用、合法输入、无反馈，并捕获 `ReferenceError`；补显式导入后失败草稿进入预览且保留标题/正文，三尺寸 3/3、178/178 单测及完整验证通过 | GEN03A | 已验证 |
| D-READ-SHELL-001 | P1 | 剧情梳理详情仍自建 `pc-section-card + DetailFooter`，没有进入第 07 号文档指定的 `ReaderDetailShell` 唯一阅读流程 | StorylineDetailPage 在共享阅读壳建立后仍保留旧详情外壳；标题滚动、中心点击底栏、悬浮工具及阅读器字体/主题入口因此与其他阅读域分叉 | READ01 以 custom-content 迁移入共享壳，保留全部结构化字段、关联跳转、编辑与删除语义；定向契约 2/2，详情/编辑三尺寸 6/6，180/180 单测及全量 UI/外观门禁通过 | READ01 | 已验证 |
| D-READ-REASON-001 | P1 | 阅读聊天详情无法显示酒馆楼层已经保存的思维链 | `normalizeArchivedMessage` 只提取正文、作者、时间等字段，Pending/ReaderMessage 没有 reasoning，ReaderApp 也未向 ReaderDetailShell 传递；同仓库 ChatArchive 已能从 extra/data 嵌套字段提取思维链 | READ03A 新增唯一循环安全提取器并同时复用于 Reader/ChatArchive；raw→Pending→ReaderMessage→ReaderDetailShell 数据流已接通，定向 3/3、折叠场景三尺寸 3/3、183/183 单测及完整 `pnpm verify` 通过 | READ03A | 已验证 |
| D-READ-SOURCE-001 | P1 | 日记、番外、论坛、书信和小剧场详情虽然保存了生成来源，却没有在阅读详情显示来源楼层/范围 | 五个详情页向 ReaderDetailShell 传 reasoning，但 meta/kicker 未读取 `generationRecord.replay.source.label`（番外另有 sourceLabel）；Summary/Digest 已有来源展示参考 | READ03B 在共享壳建立唯一可选 sourceLabel 展示位，五域传入已持久 replay source，番外保留 legacy sourceLabel 回退；契约 2/2、五场景三尺寸 15/15、185/185 单测及完整验证通过 | READ03B | 已验证 |
| D-CRUD-CONFIRM-001 | P1 | 云媒体删除服务配置、聊天档案迁移手机内容时弹出浏览器原生确认框，无法使用手机内统一标题、危险态、返回键和弹窗生命周期 | 两个较早实现直接调用 `window.confirm`，绕过 `phone.confirmNotice` 共享入口 | 两处已迁入共享 warning 确认；精确契约由失败转 1/1，186/186 单测、37/37 机器、51/51 UI/交互、15/15 外观及临时构建通过 | CRUD01A | 已验证 |
| D-CRUD-CONFIRM-002 | P1 | 八股规则、条目库查重项、关系连线、工作台步骤/待插入草稿及 MVU 修改记录可被一次点击永久移除，且没有撤销入口 | 页面把危险按钮直接绑定 store 删除，或本地 handler 未经过共享确认；临时表单行与可逆主页移位不属于此问题 | 五页均迁入共享 warning 确认；定向契约 2/2、取消/确认三尺寸 3/3、189/189 单测、完整门禁及正式全量 711/711 通过 | CRUD01B | 已验证 |
| D-VISUAL-003 | P1 | `reader-footer-persistence` 单独/短序列可通过，但三尺寸全目录在 430px 阶段报 `Reader detail shell is missing` | 场景路由到详情后只等待一次 paint 就点击共享壳；同模块 `reader-catalog` 已有显式壳就绪等待，但 footer 场景遗漏 | 契约由失败转 1/1；reader 混排 9/9、正式全量 711/711 和关键基线读取 12/12 通过 | E05D | 已验证 |
| D-CRUD-TRANSFER-001 | P0 | 单个 App 内容迁移在写入、rehydrate 或持久化失败后尝试回滚，但回滚自身失败会覆盖最初错误，且无法保证原快照重新持久化 | `importContentTransfer` 自建 try/catch 事务；恢复、rehydrate、二次保存不在独立错误边界内，没有复用完整备份已经验证的 `executeBackupImportTransaction` | 已复用唯一事务执行器；定向接线 1/1、事务行为 4/4、190/190 单测及完整门禁通过 | CRUD02A | 已验证 |
| D-CRUD-BACKUP-001 | P1 | MVU 收藏和修改记录不进入“导出全部”或插件文件仓库快照，迁移浏览器或清理站点数据后会丢失 | MVU 修改器把两类持久数据直接写到浏览器 `localStorage`，既没有注册 `PhoneBackupDomain`，也不在 `buildPhoneBackup()` 数据源中 | 新增全局配置备份域，以新持久状态接管页面读写；首次仅复制旧 localStorage 且保留旧键，空备份恢复由迁移标记阻止旧值复活；定向 3/3 及完整验证通过 | CRUD02B | 已验证 |
| D-CRUD-BACKUP-002 | P1 | 设置中的“导出全部”不包含插件私有预设文件；用户仅保存该 JSON 时无法完整迁移插件预设 | 私有预设正文位于酒馆 `user/files`，索引在 extension settings；文件仓库快照额外调用 `pluginPresets.exportRecords()`，但手工 `PhoneBackup` 格式没有该层 | 手工完整备份升级 v2，嵌入预设记录及 App 默认绑定；v1 恢复不碰预设，文件仓库保持独立 v1 包装；文件/设置双事务及加载失败拒绝导出已验证 | CRUD02C | 已验证 |
| D-SETTINGS-RESOURCE-001 | P0 | 删除自定义字体或壁纸时，服务端删除失败后页面虽报错，资源却已经从设置列表消失，重试所需路径也随之丢失 | settings store 先移除内存引用并触发持久化 watch，最后才请求 `/api/files/delete`；缺少请求头时删除函数还静默返回 | SETTINGS01A 改为服务端文件删除成功后才移除设置引用；不支持删除接口时明确抛错；定向事务 2/2、198/198 单测与完整门禁通过 | SETTINGS01A | 已验证 |
| D-SETTINGS-CLEAR-001 | P0 | “清空当前聊天”并行调用全部 App 的 reset handler；任一 handler 中途失败时，其他域、世界书联动条目和世界书槽位同步结果可能已经清除，页面没有统一恢复边界 | reset registry 原只暴露 `() => void | Promise<void>`，没有快照/rollback；世界书联动直接改条目，世界书槽位还 fire-and-forget 自动同步 | 共享上下文允许外部域登记补偿；中央顺序 reset、逆序补偿、恢复 extension settings 并 rehydrate/persist；主错误和所有补偿错误进入同一 AggregateError；4/4 定向、202/202 单测及完整门禁通过 | SETTINGS01B | 已验证 |
| D-TEST-HOME-001 | P1 | 主页拖拽、App 成组、文件夹打开/改名/移出只有纯 `appLayout` 函数单测，没有真实长按指针链；组件事件断线或防误触误伤不会被默认门禁发现 | 默认交互清单没有 drag 类别，四个 home 场景只静态检查入口、五列密度和任务卡 | 新增唯一 `home-layout-drag` 交互契约，三尺寸真实长按拖动成组并验证打开、改名、移出/自动解散；203/203 单测、54/54 默认交互、714/714 正式目录通过 | A01 | 已验证 |
| D-TEST-SETTINGS-PERSIST-001 | P1 | 设置和主题页面已有静态/选择场景，却没有证明 UI 改值写入唯一持久源后，rehydrate 与重进页面仍恢复同一值 | E04 的 persistence 代表场景属于内容目录排序；设置场景只选择阅读字体，主题根场景只检查名称不截断 | 新增 `settings-theme-persistence` 默认交互：界面列数与内置主题包经 extension settings 快照/rehydrate 后 UI 和 store 一致；204/204 单测、57/57 默认交互、正式基线 239×3＝717 | A02 | 已验证 |
| D-TEST-BUSINESS-REPARSE-001 | P1 | B 批七条失败草稿解析链中，只有总结和小剧场会在浏览器内编辑原文并点击重新解析；日记、论坛、书信只渲染失败页，摘抄和剧情梳理没有失败草稿重解析场景 | 七个业务域使用 `parseDiaryGeneratedResult`、论坛专用解析器、简单 XML 解析器和配置化剧情线解析器等不同实现；一个代表场景不能证明其余事件和路由链没有断线 | 七域现均在默认交互中修复原文、点击重新解析、进入预览、保留标题/正文并离开失败页；21/21 定向、206/206 单元、75/75 默认 UI、正式 244×3＝732 全量通过 | B01 | 已验证 |
| D-TEST-WORLD-STRATEGY-001 | P1 | 世界书联动与世界书槽位已按 `strategy.type` 显示蓝/绿灯，但视觉 fixture 没有同时提供 constant/selective 条目，也没有断言两处映射 | 现有旧世界书 fixture 缺少激活策略字段，槽位根场景只创建默认 constant；颜色类断线或回退为按关键词判断不会被门禁发现 | 双消费者场景故意让关键词与 strategy 相反，仍验证 constant=蓝、selective=绿；2/2 定向契约、三尺寸 3/3、正式 245×3＝735 及完整门禁通过 | C02 | 已验证 |
| D-TEST-TIMEKEEPER-PROFILE-001 | P1 | 时间确认宣称会自动读取资料表人物，并在新增、改名、填写出生日期和删除时双向维护人物资料，但视觉目录只有 App 根页静态渲染 | `useTimekeeperStore` 通过 profiles watch 和四个写操作维持跨 App 一致性；没有真实 UI 场景时，事件断线、重复创建或只删一侧均不会被门禁发现 | `timekeeper-profile-sync` 真实点击新增、修改姓名/生日和确认删除；2/2 定向契约、三尺寸 3/3、正式 246×3＝738 及完整门禁通过 | C03 | 已验证 |
| D-TEST-FILE-REPOSITORY-001 | P1 | 插件文件仓库只有根页静态截图；立即快照、详情读取、保护切换、确认删除和持久失败提示均未经过真实 UI 事件 | 仓库主体依赖文件上传/读取/删除接口，现有视觉 fixture 没有隔离文件服务，因而按钮断线、详情载入失败或错误只在控制台出现都不会被门禁发现 | 页内内存文件服务完整执行快照、详情、保护/取消保护、确认删除和失败卡片；2/2 契约、三尺寸 3/3、正式 247×3＝741 及完整门禁通过 | C04 | 已验证 |
| D-TEST-PLUGIN-PRESET-LIFECYCLE-001 | P1 | 插件预设详情可设置默认 App、改名和删除，但现有浏览器场景只打开内置日记预设，没有操作可删除的私有预设 | 私有预设文件持久化、默认选择索引和详情路由共同参与生命周期；缺少隔离文件服务时无法验证改名后仍在原详情、删除后清理默认选择 | 隔离文件服务下真实切换默认 App、两段确认改名和警告确认删除；2/2 契约、三尺寸 3/3、正式 248×3＝744 及完整门禁通过 | C05 | 已验证 |
| D-TEST-RELATIONSHIP-CRUD-001 | P1 | 关系网只有生成任务离页完成场景，没有真实操作人物和单向关系 | 人物、关系和删除确认都在同一根页中写入聊天作用域；新增/改名/编辑事件或人物删除的关系级联断线时，现有生成场景不会发现 | 真实新增/改名人物、建立/编辑/确认删除关系及人物级联删除；2/2 契约、三尺寸 3/3、正式 249×3＝747 及完整门禁通过 | C06 | 已验证 |
| D-TEST-REGEX-WIZARD-SAVE-001 | P1 | 正则向导已有保存目标和规则库入口，但浏览器场景只展示字段模式与测试区，没有执行保存或验证阅读绑定 | `saveRule()` 同时新增规则并写入阅读提取 usage；任一按钮、可搜索选择器或双写链断线时，现有静态场景不会发现 | 隔离设置中真实选择“全局阅读正文”、命名保存并验证新增规则、reader contentRuleId 和规则库回显保持同一 ID；2/2 契约、三尺寸 3/3、正式 250×3＝750 及完整门禁通过 | C07 | 已验证 |
| D-TEST-REGEX-DISPLAY-CRUD-001 | P1 | 正则替换只有预览区静态场景，规则库自身的新增、编辑、使用开关、复制和确认删除没有真实 UI 事件覆盖 | `RegexDisplayApp` 同页协调规则对象与按 App usage；向导写入链不能发现规则库控件断线或删除未清理 | 隔离设置中真实新增/编辑显示规则、为阅读器启用、复制并确认删除副本，断言规则和 usage 一致；2/2 契约、三尺寸 3/3、正式 251×3＝753 及完整门禁通过 | C08 | 已验证 |
| D-TEST-PROMPT-LIBRARY-CRUD-001 | P1 | 快速短语与格式模板的编辑器场景只确认字段存在，没有点击保存或验证返回列表、编辑和确认删除 | 两类对象共用编辑组件但走不同 store 分支；静态渲染不能发现 kind 分支、路由返回或删除动作断线 | 隔离 prompt store 中真实创建短语组/短语、编辑并确认删除；另创建模板组/模板并确认删除组，断言两类数据互不串联；2/2 契约、三尺寸 3/3、正式 252×3＝756 及完整门禁通过 | C09 | 已验证 |
| D-TEST-PROMPT-CONFIG-SAVE-001 | P1 | 任务模板、类型提示词和输出解析编辑器已有字段场景，但都没有点击保存并验证返回后的权威状态 | 三类编辑页分别写入 taskTemplates、typePrompts、outputRules；仅测试占位符插入或字段展开无法发现保存/返回链断线 | 隔离 prompt store 中真实修改并保存三类配置，逐项断言权威状态写回且路由返回，最后恢复测试默认；2/2 契约、三尺寸 3/3、正式 253×3＝759 及完整门禁通过 | C10 | 已验证 |
| D-TEST-MEDIA-LIBRARY-CRUD-001 | P1 | 相册、音乐、视频已有编辑器或查看器静态场景，但没有通过真实按钮创建、编辑和确认删除媒体对象，也没有验证三类对象共用 store 时按 `kind` 隔离 | 三个 App 共用 `useMediaStore`，页面仅在 computed 中过滤 `kind`；静态渲染无法发现保存返回、编辑路由、删除确认或跨类型串项 | 隔离聊天作用域从三个根页完成新增、编辑、类型隔离和共享 warning 确认删除；2/2 契约、三尺寸 3/3、正式 254×3＝762 及完整门禁通过 | D01 | 已验证 |
| D-TEST-MINIGAME-PLAY-001 | P1 | 10 个小游戏只有猜数字输入框的静态外观场景，没有任何移动、填格、落子、抽牌或撤回等真实玩法事件证据 | 2048 使用独立 Pinia store，其余 9 个组件各自读写独立持久字段和状态机；一个代表场景不能证明其他玩法按钮、棋盘事件或保存链未断 | 10 个独立场景均完成最小合法动作并断言 DOM/持久状态同步；2/2 契约、三尺寸 30/30、正式 264×3＝792 及完整门禁通过 | D02 | 已验证 |
| D-TEST-WORKBENCH-CRUD-001 | P1 | 工作台场景都先由 fixture 直接调用 store 创建流程和步骤，只展示日志或论坛步骤字段，没有通过页面新建、改名、添加、排序和确认删除 | 工作流及步骤在同页由 SearchableCombobox、图标操作与共享确认协调；静态 fixture 无法发现选择器、事件或删除链断线 | 隔离 workbench store 中真实完成流程新建/改名、两次选步、上移及步骤/流程确认删除；2/2 契约、三尺寸 3/3、正式 265×3＝795 和完整门禁通过；运行工作流与聊天插入仍排除 | D03 | 已验证 |
| D-TEST-CHAT-INSERT-001 | P1 | 楼层插入只有通用 App 根页截图，没有任何模式点击“确认写入”，无法证明模式切换、目标楼层、共享确认和运行时调用链可达 | 四种模式共用一页动态字段与两组运行时函数；静态根页不能发现条件字段、参数或确认链断线 | 视觉沙箱内逐模式提交并断言 create/set/save 参数与次数，结束恢复聊天、设置和函数；2/2 契约、三尺寸 3/3、正式 266×3＝798 及完整门禁通过 | D04 | 已验证 |
| D-TEST-CLOUD-MEDIA-PROFILE-001 | P1 | 云媒体的现有场景只直达生成页和配置页，没有通过页面新增、切换、修改服务商/类型或确认删除本地配置 | 配置页用动态组合框协调多 profile，并由 provider/kind 决定字段与默认值；静态截图无法发现选择、重置默认值或删除链断线 | 隔离设置中完成新增/改名、MiniMax 音频默认值、组合框往返和取消/确认删除；2/2 契约、三尺寸 3/3、正式 267×3＝801 及完整门禁通过 | D05 | 已验证 |
| D-TEST-COMFY-WORKFLOW-001 | P1 | ComfyUI 现有场景只展开管理菜单、展示 JSON 和参数模式，没有通过页面新建、改名、选择、复制或确认删除本地工作流 | 同一工作流库由组合框、管理菜单和 legacy 同步字段共同协调；静态展示无法发现活动项或复制/删除链断线 | 隔离 settings 中完成新建/改名、菜单复制、组合框往返和取消/确认删除；2/2 契约、三尺寸 3/3、正式 268×3＝804 及完整门禁通过 | D06 | 已验证 |
| D-TEST-APP-BUILDER-DEFINITION-001 | P1 | App 工坊已有新建保存和内容转换场景，但根列表的复制与共享确认删除没有真实 UI 事件证据 | 定义列表、动态 App 注册目录和全局/聊天内容清理由同一生命周期协调；保存新 App 不能发现复制命名、取消删除或确认删除链断线 | 隔离 extension settings 中真实复制定义、验证动态目录，并分别取消和确认删除副本；2/2 契约、三尺寸 3/3、正式 269×3＝807 及完整门禁通过 | D07 | 已验证 |
| D-TEST-MVU-CRUD-001 | P1 | MVU 修改器现有场景只覆盖值编辑、撤销和重做，没有新增子项、收藏、取消/确认删除或清空修改记录的真实 UI 证据 | 树节点事件、MVU runtime 写回和插件持久的收藏/历史由同页协调；单一编辑链无法发现其余操作或共享确认断线 | 视觉内存 MVU runtime 中完成新增、收藏、取消/确认删除和取消/确认清空记录；2/2 契约、三尺寸 3/3、正式 270×3＝810 及完整门禁通过 | D08 | 已验证 |
| D-TEST-SCENE-PLANNER-HISTORY-001 | P1 | 场景编排只有生成任务离页场景，没有通过历史列表选择方案、新方案清空或取消/确认删除 | 根页同时协调活动方案、表单回填和 chat-scope 方案集合；生成成功不能发现历史列表事件或活动项删除后的重置链断线 | 隔离 scene-planner store 中完成历史选择、表单回填、新方案重置和取消/确认删除活动方案；2/2 契约、三尺寸 3/3、正式 271×3＝813 及完整门禁通过 | D09 | 已验证 |

| D-RUNTIME-PRESET-ROUTE-001 | P0 | 从插件预设页进入酒馆预设详情时可能继承旧 `presetSource=plugin`，酒馆对象被错误送入插件预设生命周期 | `openPreset` 只写 `presetName`，共享路由合并参数时保留旧 `presetId/presetSource` | 酒馆入口显式清空 `presetId` 并写入 `presetSource=tavern`；定向契约锁定来源隔离，真实组件状态确认 `isPluginDetail=false` | F00-T3 | 已验证 |
| D-RUNTIME-PRESET-RENAME-001 | P0 | 酒馆原生预设改名会隐式切换当前预设，并可能弹出内置正则导入确认 | 当前宿主只提供加载式 `renamePreset` 或依赖当前选择的 manager 改名，没有不切换、不导入正则的原子接口 | 酒馆预设改名在打开输入弹窗前明确阻止；插件私有预设改名与非当前酒馆预设删除不受影响；3/3 定向契约和隔离浏览器无聊天差异证据通过 | F00-T3 | 已验证 |
| D-UI-HOME-001 | P1 | 首页随着 App 增加持续把全部入口平铺并追加到末页，缺少默认分组、常用区、全部应用搜索、隐藏入口和显式整理模式 | 首页布局只保存顺序、Dock 与手工文件夹；App 注册没有首页分类/推荐位置语义，`defaultVisible` 也没有消费者；既有 S01E 仅无行为拆分组件 | 当前代码直接证明默认 `folders: []`、新增 App 追加到 `appOrder`；截图中第二页近 20 个不同业务域 App 同等平铺。当前仅登记，不得把 S01E 结构完成视为首页视觉整改完成 | HOME01 | 已发现 |
| D-GEN-REPARSE-002 | P0 | 生成预览打开“原始输出”后点击“重新解析”没有反应 | `RawOutputEditor` 调用了 `withDefaults(defineProps())` 却没有保存返回值，点击处理器在发出事件前读取不存在的 `props.modelValue` | 绑定 `const props`；论坛预览三尺寸真实修改 XML 并点击重新解析后回到更新后的预览；265/265 单测及完整门禁通过 | FIX01 | 已验证 |
| D-FORUM-PREVIEW-001 | P1 | 论坛生成预览直接把八股检测嵌在正文与回复之间，普通预览无法只查看生成结果 | 页面关闭共享预览的 scan 能力后又手工常驻挂载 `BaguScanPanel`，形成第二套显示入口 | 删除手工面板，主题帖通过共享底部“八股”按钮按需切换，回复生成不显示八股入口；三尺寸实际点击通过 | FORUM01 | 已验证 |
| D-UI-FORUM-SORT-001 | P2 | 论坛板块页长期占用一整行显示回复/发布/热度/收藏四种排序，但当前使用价值低 | 板块页为低频排序维持独立 segment、持久状态和四分支排序逻辑 | 删除排序控件与页面状态，保留搜索和固定“最新回复优先”顺序；论坛板块页三尺寸通过 | FORUM02 | 已验证 |
| D-PROMPT-TASK-001 | P1 | 小剧场会隐式发送类型名却无法在任务模板中找到；论坛生成帖子的可编辑任务模板没有板块说明变量 | 动态业务说明分别写死在适配器的类型预设兜底或聊天历史尾部，未注册为任务模板变量 | 小剧场新增 `typeName/typeInstruction`，论坛新增 `boardName/boardDescription` 及格式化变量；板块说明移入任务层，旧默认精确迁移且非空自定义模板不覆盖；265/265 单测及完整门禁通过 | PROMPT01 | 已验证 |
| D-ROUTE-ARCHIVE-001 | P0 | 进入聊天档案详情时，阅读聊天的延迟加载偶尔把当前路由改成阅读聊天首楼，导致档案详情空白；后退后恢复 | `useReaderChatSession` 的详情回退只判断 `page === 'detail'`，没有确认当前 route 仍属 Reader；`replacePage` 会按当前 appId 改写 ChatArchive detail | 仅 Reader route 回退增加 `appId === reader` 前置条件；定向回归 1/1 与 TypeScript 检查通过 | ROUTE01 | 已验证 |
| D-REASON-CAPTURE-001 | P1 | 日记/番外详情能渲染 reasoning 但外部兼容 API 的独立思维链字段没有保存，因此经常无内容可展开 | 生成管线仅把 XML 清洗移除的前缀作为 reasoning；普通与流式外部 API 返回的 reasoning 字段未进入归一化结果 | 新增纯响应归一化工具，识别 `reasoning_content/reasoningContent/reasoning/thinking`；外部普通/流式与 XML 前缀合并保存，定向 3/3、TypeScript 通过 | REASON01 | 已验证 |
| D-THEATER-SCROLL-001 | P1 | 小剧场在文字与多个网页渲染片段混排时，普通文字出现固定高度内层滚动，无法作为一篇内容连续下滚 | 文本片段误复用独立阅读正文的固定高度滚动语义，和详情外层滚动容器叠加 | 小剧场文本片段局部覆盖固定 body 几何，保留 iframe `embedded` 高度桥接；定向契约 1/1、三尺寸隔离场景 3/3、类型与严格 UI 检查通过 | THEATER01 | 已验证 |
| D-READER-SWIPE-001 | P1 | 阅读聊天只能看到当前 swipe，无法只读查看同一楼层的其他候选回复 | 读取未请求完整 swipe 数据，归一化模型只保留当前一条正文和数量 | Reader 读取 `include_swipes`、当前/历史归一化均保留候选，页面局部切换不写回；定向 2/2、三尺寸 6/6、类型/严格 UI 通过 | SWIPE01 | 已验证 |
| D-PREVIEW-NAV-001 | P2 | 原始输出/八股检测等内容视图被做成预览底栏的大操作按钮，和保存操作混在一起 | 共享 `GenerationPreviewPanel` 未区分“查看模式”与“内容提交动作” | 预览/原始输出/八股迁入紧凑 tablist，底栏只保存；论坛实际切换与重新解析通过，定向 1/1、三尺寸 3/3、类型/严格 UI 通过 | PREVIEW01 | 已验证 |
| D-EDITOR-SAVED-BODY-001 | P2 | 已保存内容的主要正文编辑高度缺少统一语义，用户难以连续编辑长文本 | 现有高度档位覆盖短输入、模板、测试和提示词；保存正文没有独立消费清单和统一规则 | 保存正文编辑页面及全局 textarea 语义 | EDITOR01 | 已发现 |
| D-PREVIEW-DRAFT-MULTI-001 | P1 | 同一 App/page 的新预览会覆盖之前未保存预览，用户无法回到较早草稿 | `previewDrafts` 用 `{appId,page}` 作唯一 key，模型没有草稿 id/活动草稿与列表生命周期 | preview-drafts scope、全量/当前聊天备份域、生成预览恢复 UI | DRAFT01—DRAFT03 | 已发现（C 级授权待确认） |

## 已验证基线

以下是证据，不代表上述缺陷已经修复：

- 当前覆盖基线：`main` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，工作树、index、untracked 均为空。下列测试数字是 E02 当时的历史验证证据，不替代后续批次完成记录。
- 沙箱外 `pnpm verify` 通过：63 项单元测试、4 类契约、类型检查、样式守卫和 UI 复用检查均通过；ESLint 保留 3 条警告。
- 生产临时构建成功；`tmp/audit-build/index.js`、`index.css` 与当前 `dist` 的 SHA-256 相同。
- 视觉场景入口能够列出 204 个场景；E01 后 `--list` 仍执行视觉构建，但不再启动 preview 或占用监听端口。

## 登记规则

- 新缺陷必须写明复现证据和共同根因；根因未知时状态只能为“已发现”，不得直接修改。
- 一个批次只处理同一根因。相似外观但根因不同的缺陷分开编号。
- 修复后必须把验证命令、场景和结果写回本表，才能标为“已验证”。
- 用户真实数据、真实 API 或不可逆操作不通过本表自动授权。
