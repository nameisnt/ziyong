# 自动执行工作树归属

## 当前覆盖基线（R02，2026-08-13）

本节是后续自动批次判断工作树归属的唯一当前基线；下方 E00—P01 各节保留为历史快照，其中出现的旧 `HEAD`、index 数量、未跟踪状态和“未推送”描述不得再解释为当前状态。

- 分支：`main`
- `HEAD` / `origin/main`：`edcb741f466ad11013c5f3b625eb373a3f5835e2`，无分叉。
- index、worktree、untracked：均为 0。
- 最近检查点：`fe995c5`（E00—U01B）、`76156a0`（P01 脚本修复）、`edcb741`（发布证据记录）；三者均已在 GitHub `main`。
- UI 契约遗留：226 个签名、229 个实际命中，其中 `aria-label=159`、几何 67、主题色 3、`title=0`。
- 正式产物：`dist/index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`；`dist/index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

R02 只允许修改 `docs/execution/`：校正当前状态、建立 U01 队列和登记下一批契约。不得修改业务源码、测试、基线、正式 `dist`、Git index、提交或远端。

快照时间：2026-08-13 16:59:04 +08:00

基线：

- 分支：`main`
- `HEAD`：`8e4fa9e28e5546f6bbe02f47e2126284d4f77c76`
- `origin/main`：`8e4fa9e28e5546f6bbe02f47e2126284d4f77c76`
- 分叉：无

本表只确认 E00 开始时的归属与保护边界，不表示所有既有修改都已完成发布。执行前已有的修改默认保留；任何后续批次要修改保护项，必须在该批次契约中逐文件授权并先阅读现有 diff。

## 一、此前任务成果：保留并保护

| 文件或范围 | E00 状态 | 归属判断 | 已知用途 | 后续允许条件 |
| --- | --- | --- | --- | --- |
| `AGENTS.md` | 已跟踪、未暂存修改 | 本轮规则建设 | UI 复用、防复发和自动执行强制入口 | E01 可处理发布一致性；其他批次不得删除协议入口 |
| `docs/00-项目总览.md` | 已跟踪、未暂存修改 | 本轮文档整理 | 增加历史归档入口 | E01 可处理发布一致性 |
| `docs/07-当前代码与UI复用审计及设计要求.md` | 新文件、已暂存 | 本轮规则建设 | 当前总则、完整方案和自动执行协议 | 只允许规则批次修改；不得与其他方案并行维护 |
| `docs/archive/` | 新目录、已暂存 | 本轮文档整理 | 已完成方案的历史归档 | 只修复归档标识和死链接，不重新执行正文旧待办 |
| `components.d.ts` | 已跟踪、未暂存修改 | 此前阅读器实现 | 注册新增阅读器组件类型 | 与 `ReasoningDisclosure.vue` 一并保护；构建工具确需更新时才修改 |
| `src/components/ReasoningDisclosure.vue` | 未跟踪 | 此前阅读器实现 | 阅读详情思维链折叠展示 | 必须保留；READ01 批次才允许改行为 |
| `src/components/ReaderContent.vue` | 已跟踪、未暂存修改 | 此前阅读器实现 | 提供思维链标题前插槽 | READ01 批次且先读 diff |
| `src/components/ReaderDetailShell.vue` | 已跟踪、未暂存修改 | 此前阅读器实现 | 统一思维链、阅读工具和正文编辑 | READ01 批次且先读 diff |
| `src/components/PhoneOverlay.vue` | 已跟踪、未暂存修改 | 此前交互收口 | 删除左右滑动返回及相关状态与样式 | A/S 批次且有现有导航回归场景后才允许修改 |
| `src/components/FrontendFrame.vue` | 已跟踪、未暂存修改 | 此前小剧场阅读修复 | iframe 点击坐标映射与阅读底栏交互 | READ01 或小剧场同根因批次且先读 diff |
| `src/util/theaterFrontend.ts` | 已跟踪、未暂存修改 | 此前小剧场阅读修复 | iframe 阅读点击消息与侧滑逻辑清理 | 同上 |
| `src/testing/visual-harness.ts` | 已跟踪、未暂存修改 | 此前视觉回归 | 阅读器、小剧场和侧滑相关场景更新 | E01/E02 可拆测试基础设施；不得删除已有行为覆盖 |
| `src/testing/visual/scenarioCatalog.ts` | 已跟踪、未暂存修改 | 此前视觉回归 | 场景目录清理 | E01/E02 且先读 diff |
| `scripts/ui-visual-check.mjs` | 已跟踪、未暂存修改 | 此前视觉回归 | 视觉检查交互断言增量 | E01/E02 的目标文件；不得覆盖现有改动 |
| `src/components/diary/DiaryBookPage.vue` | 已跟踪、未暂存修改 | 此前日记修复 | 恢复来源楼层显示 | 日记同根因批次且先读 diff |
| `dist/index.js`、`dist/index.css` | 已跟踪、未暂存修改 | 此前源代码构建产物 | 当前可部署构建 | 2026-08-13 临时构建 SHA-256 与当前文件一致；仅源代码验证通过后重建 |

关键保护文件快照：

| 文件 | SHA-256 |
| --- | --- |
| `AGENTS.md` | `6A8891A94091BF4747D852CA479D87742E89D9B104538ABF7270840A21E588E2` |
| `docs/07-当前代码与UI复用审计及设计要求.md` | `324D42E7A53B3B0B6FB34C2E9575B83637D89B9E3B7388DFDCEE3A0A53B7C2C7` |
| `scripts/ui-visual-check.mjs` | `841FC582DBAB0E913B565E93E6C60CC4A1D72CFAB6CA9E4F76AA6B736F057821` |
| `src/components/PhoneOverlay.vue` | `394418DA349AA338E60EA8DDE3AC748918ECBF669138D496101FA6C29094B900` |
| `src/components/ReaderDetailShell.vue` | `BF7888DB1C15B0359839D19965166C2502128B4C82A7CB6D5F36275995D92BE9` |
| `src/components/ReasoningDisclosure.vue` | `0E8AD9E2D3A8193B217E46818875AB2BC6F294FCBE6DECC100C9F6C24AE8F3ED` |
| `dist/index.js` | `6A05337060CC760289156F2881384B02AF0F0EE360792A6D040B9E6178711F56` |
| `dist/index.css` | `75DF60D228DB7A49A68C9188A7C8CE20319E4442260110FDD2848BDCDDEE42A6` |

哈希用于识别后续变化，不用于禁止经过批次授权的正常修改。

## 二、此前测试与契约基础设施：保留，等待 E01 纳入

| 文件或范围 | E00 状态 | 归属判断 | 保护与后续处理 |
| --- | --- | --- | --- |
| `scripts/backup-contract-check.mjs` | 未跟踪 | 此前测试基础设施 | E01 评估并纳入 Git；当前 `package.json` 已依赖 |
| `scripts/config-recovery-contract-check.mjs` | 未跟踪 | 此前测试基础设施 | 同上 |
| `scripts/recovery-contract-check.mjs` | 未跟踪 | 此前测试基础设施 | 同上 |
| `scripts/structure-contract-check.mjs` | 未跟踪 | 此前测试基础设施 | 同上 |
| `scripts/check-style-guard.ps1` | 未跟踪 | 此前 UI 基础设施 | 同上；当前样式验证已依赖 |
| `scripts/unit/` | 13 个未跟踪文件 | 此前单元测试 | E01 评估并纳入 Git；沙箱外 63 项测试通过 |
| `.agents/AGENT-v2.md` | 未跟踪 | 本地/项目代理规则 | `docs/07-当前代码与UI复用审计及设计要求.md` 当前引用它；E01 必须选择“纳入仓库”或“移除公开文档依赖”，未决定前不得删除 |

## 三、用户或本地资料：禁止自动修改和发布

| 文件或范围 | E00 状态 | 保守归属 | 自动执行边界 |
| --- | --- | --- | --- |
| `backups/` | 19 个未跟踪文件 | 用户/此前任务本地备份 | 禁止删除、移动、覆盖、暂存、提交和发布 |
| `ui-review/` | 182 个未跟踪文件 | 本地视觉审计资料 | 只读参考；禁止自动暂存、提交和发布 |
| `日记 (1).json` | 未跟踪 | 用户提供的参考预设 | 只读参考；禁止修改、删除、暂存、提交和发布 |
| `.codex/` | 已忽略 | 本地工具状态 | 禁止纳入仓库 |
| `tmp/` | 已忽略 | 生成测试产物 | 后续批次只能写入自己的命名目录；不能把旧报告冒充当前基线 |
| `可参考拓展/`、`预设/`、`QITA/`、`酒馆手机-脚本基础款/` | 已忽略 | 本地参考资料 | 只读；禁止自动发布 |

## 四、E00 新建记录

| 范围 | 归属 | 允许操作 |
| --- | --- | --- |
| `docs/execution/` | E00 自动执行记录 | 后续每个批次必须更新；E01 负责确保其能随仓库发布 |

## 五、全局边界

- 不使用 `git add -A`，因为当前存在用户资料和大量本地审计文件。
- 不清理未跟踪文件，不恢复已有 diff，不全仓格式化。
- 任一后续批次修改上表中的保护项，必须在 `02-批次状态.md` 写出文件名、根因和验证方法。
- 当前未登记的文件默认不可修改；需要时先更新本表。

## 六、E01 归属变更

| 文件或范围 | E01 处理 | 当前边界 |
| --- | --- | --- |
| `.gitignore` | 增加 `.agents/`、`backups/`、`ui-review/`、`日记 (1).json` | 只改忽略规则，没有操作这些本地资料 |
| `scripts/safe-push-dist.ps1` | 建立允许新文件清单；dry-run 改为纯本地、无询问、无提交对象 | 后续只能按显式清单扩展，不得改为全部未跟踪文件 |
| `scripts/ui-visual-check.mjs` | 列表模式使用 `file://`；目标场景使用系统分配端口并统一清理 | 保留此前阅读器/小剧场交互断言，E02 只可扩展验证基线 |
| `docs/07-*`、`docs/archive/`、`docs/execution/` | 纳入显式发布树 | 第 07 号文档与本目录是当前规则/状态；归档只读 |
| 契约、样式和 `scripts/unit/` | 验证通过并纳入显式发布树 | `package.json` 的当前验证依赖，后续修改必须有对应缺陷和批次 |
| `.agents/AGENT-v2.md` | 未修改，取消公开文档依赖并改为忽略 | 本地文件，不发布；公开执行以根目录 `AGENTS.md` 与第 07 号文档为准 |

E01 关键结果哈希：

| 文件 | SHA-256 |
| --- | --- |
| `.gitignore` | `4C7DC04F31F41AF843D82894ACEB52FC1AC747BD994F2EF59D6A0758D296739F` |
| `scripts/safe-push-dist.ps1` | `3A44D75189A7A3BDDA1DEB7F117F37A8BB650179EBE0DDD46BC63386AB77C70A` |
| `scripts/ui-visual-check.mjs` | `579A337DDAB93759E05C1970E5A19911B1C980AD1303425729AE8B1BB18A2607` |
| `docs/07-当前代码与UI复用审计及设计要求.md` | `28A99324141E624F523386D814BC39AC881C972D9CA936F0FF31D9CC79B6EFE2` |

## 七、E02 归属变更

| 文件或范围 | E02 处理 | 当前边界 |
| --- | --- | --- |
| `package.json` | 新建 `verify:static`、`verify:ui`、`verify:full` 分层，默认 `verify` 加入严格 UI、构建和三尺寸关键交互 | 后续不得绕开增量基线恢复为裸 `eslint` |
| `scripts/check-eslint-baseline.mjs`、`scripts/baselines/eslint-warnings.json` | 精确锁定 3 条已知警告 | 新增警告或旧警告失效均失败；基线变更必须对应编号缺陷 |
| `scripts/baselines/ui-visual.json` | 由 204 场景×3 尺寸生成，最终 0 fail/0 warning | 官方基线只能由全场景三尺寸覆盖；局部写入被硬拒绝 |
| `scripts/ui-visual-check.mjs` | 默认尺寸改为 350/390/430；增加视觉基线写入/比较；修正复合按钮和合法横向滚动的误报 | 保留 E01 的 file 列表、自动端口和进程清理 |
| `scripts/safe-push-dist.ps1` | 将 E02 基线脚本和 `scripts/baselines/` 加入显式发布树 | 仍不允许泛化发布其他未跟踪文件 |

E02 没有获得 `src`、`dist` 或 `components.d.ts` 的修改/暂存授权；构建仅用于验证现有 `dist` 与源代码一致。

## 八、R00 仓库归属校正

校正快照时间：2026-08-13 18:48:58 +08:00。本节取代前文“下一步可直接进入 U01”的推断；前文 E00/E01/E02 表格保留为历史快照。

### 8.1 Git 三层状态

| 层级 | 快照 | 归属与结论 |
| --- | --- | --- |
| `HEAD` / `origin/main` | 均为 `8e4fa9e28e5546f6bbe02f47e2126284d4f77c76`，无分叉 | 远端公共基线；不含 E00—E02 成果 |
| 暂存区 | 37 个文件；`src` 为 0 | E00—E02 规则、文档、脚本和基线的部分发布候选；不是完整验证单元 |
| 未暂存工作区 | 11 个已跟踪文件，其中 8 个 `src`；另有 1 个未跟踪 `src` 文件 | 此前阅读器、小剧场、导航和视觉场景任务成果；全部保留并作为当前运行/验证工作区 |
| 本地保护资料 | 已由 `.gitignore` 排除 | `backups/`、`ui-review/`、`日记 (1).json`、`.agents/` 等仍禁止发布 |

### 8.2 工作区源码的校正归属

| 文件或范围 | 当前状态 | 归属 | 不能丢失的行为 | 下次允许条件 |
| --- | --- | --- | --- | --- |
| `components.d.ts` | 已跟踪、未暂存 | 此前阅读器任务 | `ReasoningDisclosure` 全局组件类型 | 与对应组件同一验证/发布单元 |
| `src/components/ReasoningDisclosure.vue` | 未跟踪 | 此前阅读器任务 | 思维链展开/折叠 | READ 同根因批次或 R01 显式纳入 |
| `ReaderContent.vue`、`ReaderDetailShell.vue` | 已跟踪、未暂存 | 此前阅读器任务 | 思维链槽位与统一阅读详情 | READ 批次且先读完整 diff |
| `FrontendFrame.vue`、`theaterFrontend.ts` | 已跟踪、未暂存 | 此前小剧场阅读任务 | iframe 点击映射与阅读底栏 | READ/小剧场同根因批次 |
| `PhoneOverlay.vue` | 已跟踪、未暂存 | 此前导航收口 | 已删除左右侧滑返回实现 | 导航或结构批次，且必须保持“不恢复侧滑” |
| `DiaryBookPage.vue` | 已跟踪、未暂存 | 此前日记任务 | 来源最新楼层显示 | 日记同根因批次 |
| `visual-harness.ts`、`scenarioCatalog.ts` | 已跟踪、未暂存 | 上述行为的视觉回归 | 思维链 fixture；删除 `side-swipe-back` 场景 | E04/E05/S 批次，不得把已删除功能重新当成基线 |
| `dist/index.js`、`dist/index.css` | 已跟踪、未暂存 | 上述源码的生产构建 | 当前可部署产物 | 只能和对应源码同单元处理 |

### 8.3 已证明的暂存区与基线冲突

- 工作区 `scenarioCatalog.ts` 的 Git blob 为 `fb002a04e99ffee1ee4b1b83473812c7631819f8`，暂存区仍为 `866b9d010f9d3cb797dce2c095fd2ad5b613acf5`。
- 暂存区仍注册 `side-swipe-back`，工作区已按用户要求删除该功能及场景。
- `scripts/baselines/ui-visual.json` 记录 204 个场景和 612 次运行，该数字与工作区一致，与仅使用暂存区的场景目录不一致。
- 因此 D-PUBLISH-003 重新打开：当前只提交暂存内容会把“204 场景基线”和“仍含第 205 个场景的源码”组合在一起。

### 8.4 R00 后的强制边界

1. 本地工作区是当前唯一运行/验证基线；暂存区只是不完整的历史发布候选。
2. R01 前禁止 commit、push、发布或把暂存区结果称为“可发布”。
3. R01 必须在用户单独授权 Git 纳入后，将经验证的源码、`components.d.ts`、`dist`、测试、场景、基线和执行文档作为同一候选集合；不自动 commit 或 push。
4. R01 完成前不修改业务代码。D00 只允许更新 `docs/execution/` 和第 07 号总则中与执行记录、批次拆分直接冲突的文字。

### 8.5 D00 文档变更归属

| 文件 | D00 状态 | 归属 | 后续边界 |
| --- | --- | --- | --- |
| `docs/execution/00-工作树归属.md` | 已暂存新文件之上的未暂存修改 | R00/D00 | 保留历史快照，以第八节为当前事实 |
| `docs/execution/01-缺陷台账.md` | 已暂存新文件之上的未暂存修改 | D00 | 新缺陷必须保留复现证据和状态 |
| `docs/execution/02-批次状态.md` | 已暂存新文件之上的未暂存修改 | D00 | 顶部总状态是当前顺序权威入口 |
| `docs/execution/README.md` | 已暂存新文件之上的未暂存修改 | D00 | 已纳入第 03 号追踪文档 |
| `docs/execution/03-需求追踪与能力矩阵.md` | 未跟踪新文件 | D00 | R01 明确纳入候选；未授权时不自动 Git 暂存 |
| `docs/07-当前代码与UI复用审计及设计要求.md` | 已暂存新文件之上的未暂存修改 | D00 规则同步 | 仅修正验收列语义、执行记录入口和批次拆分，未改业务要求 |

## 九、R01 一致候选集合

完成时间：2026-08-13。

- 用户已明确授权 R01 的 Git 暂存整理；未授权 commit、push 或发布。
- Git index 现包含 50 个变更文件，其中 `src` 9 个；未暂存和未跟踪数量均为 0。
- `src/components/ReasoningDisclosure.vue`、`components.d.ts`、阅读/小剧场/导航源码、视觉场景、基线、测试、文档和 `dist` 已进入同一候选集合。
- index 内 `scenarioCatalog.ts` 已不包含 `side-swipe-back`；index 内基线为 204 场景、612 次、0 fail、0 warning。
- 保护资料进入 index 的数量为 0。`safe-push-dist.ps1 -DryRun -SkipBuild` 列出与正常 index 相同的 50 个变更文件，且未更改正常 index。
- 后续修改从此候选集合开始记录；没有新批次授权时，不再改动 index。

## 十、E03A 工作树归属

| 文件或范围 | E03A 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `scripts/check-ui-reuse.ps1`、`scripts/safe-push-dist.ps1` | 已暂存、工作区干净 | 是，前者仅作为总入口或可靠 CSS 规则，后者仅显式纳入新守卫脚本 | 保留 `-Strict` 语义和 `ui-reuse-allow` 有编号例外；不扩大安全发布边界 |
| `package.json` | 已暂存、工作区干净 | 是，仅接入 E03A 检查/测试 | 不改现有构建、基线或其他验证语义 |
| `scripts/*ui-contract*`、`scripts/unit/*ui-contract*` | 不存在 | 是，允许新建 | 不新增依赖；fixture 不进入业务 `src` |
| `scripts/baselines/` | 已暂存、工作区干净 | 仅当可靠规则反查出无法同批修正的旧问题时允许新增精确基线 | 新问题不得加入基线 |
| `docs/execution/` | 已暂存、工作区干净 | 是 | 只记归属、缺陷、契约和真实证据 |
| `src/`、`dist/`、`components.d.ts` | 已暂存、工作区干净 | 否 | E03A 只建守卫，不修页面；命中的旧问题只登记 |

E03A 不获得 Git 暂存、commit、push 或发布授权。R01 的 50 个文件候选保持不变；E03A 修改保持未暂存，便于与前批分离。

## 十一、E03B 工作树归属

| 文件或范围 | E03B 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `package.json` | R01 已暂存，E03A 有未暂存的 `ui:contracts` 入口 | 是，仅新建 `build:check` 并让 `verify`/`verify:full` 调用它 | 保留正式 `build` 为 `vite build`；不改 E03A 入口 |
| `scripts/unit/*verification-build*` | 不存在 | 是，允许新建 | 必须同时锁定临时输出和正式 build 语义 |
| `docs/execution/` | E03A 有未暂存记录 | 是 | 仅追加 E03B 契约和证据，不覆盖 E03A 结果 |
| `dist/index.js`、`dist/index.css` | R01 已暂存，E03B 开始时工作区无新 diff | 否 | 它们是 E03B 的不变证据；验证前后哈希必须一致 |
| `vite.config.ts`、`src/`、`components.d.ts` | 工作区无 E03B 修改 | 否 | CLI `--outDir` 足够时不加第二套 Vite 配置 |

E03B 继续保持 R01 的 Git index 不变，不执行暂存、commit、push 或发布。

## 十二、E04 工作树归属

| 文件或范围 | E04 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `scripts/ui-visual-check.mjs` | R01 已暂存，工作区无 E03A/E03B 修改 | 是，仅接入声明式交互契约并补足真实浏览器断言 | 保留现有 DOM/视觉基线语义，不拆分协调器，不修改截图规则 |
| `scripts/ui-interaction-contracts.mjs`、`scripts/unit/ui-interaction-contracts.test.mjs` | 不存在 | 是，允许新建 | 七类交互必须只有一份场景映射；不得复制到 `package.json` |
| `package.json` | R01 已暂存，E03A/E03B 有未暂存入口修改 | 是，仅让 `verify:ui` 消费 E04 交互契约 | 必须保留 `build:check`、`ui:contracts` 和正式 `build` 语义 |
| `scripts/safe-push-dist.ps1` | R01 已暂存，E03A 有未暂存的新守卫显式入列 | 仅在新增顶层脚本需要显式发布时允许追加文件名 | 不扩大目录或 Git 权限边界 |
| `docs/execution/` | E03A/E03B 有未暂存记录 | 是 | 只追加 E04 归属、契约、失败证据和验证结果 |
| `src/`、`dist/`、`components.d.ts`、视觉场景目录 | R01 已暂存，工作区无 E04 修改 | 否 | 现有场景已经具备代表性动作；E04 不修业务交互、不新增页面 fixture、不重写正式产物 |

E04 继续保持 R01 的 Git index 不变，不执行暂存、commit、push 或发布；浏览器场景只操作视觉测试内存数据，不连接酒馆真实聊天或真实 API。

E04 完成后反查：正常 index 仍为 R01 的 50 个文件；E03A/E03B/E04 合计为 9 个未暂存文件和 6 个未跟踪验证文件；其中没有 `src/`、`dist/`、`components.d.ts` 或 `vite.config.ts`。safe-push dry-run 已显式纳入全部 6 个新文件，且未更新正常 index。

## 十三、E05A 工作树归属

| 文件或范围 | E05A 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `scripts/ui-visual-check.mjs` | R01 已暂存，E04 有未暂存交互断言 | 是，仅追加声明式外观证据采集/比较 | 保留 E04 七类动作、现有 finding 与官方视觉基线语义 |
| `package.json` | R01 已暂存，E03A/E03B/E04 有未暂存验证入口 | 是，仅让默认 UI 验证合并外观清单 | 不复制场景名，不改变正式 build |
| `scripts/*ui-appearance*`、`scripts/unit/*ui-appearance*`、对应精确基线 | 不存在 | 是，允许新建 | 不引入依赖；只含五个关键场景和三尺寸证据 |
| `scripts/safe-push-dist.ps1`、`docs/execution/` | 有 E03/E04 未暂存修改 | 是，仅追加新文件显式入列和 E05A 记录 | 不覆盖前批结果或扩大发布边界 |
| `src/`、`dist/`、`components.d.ts`、`scripts/baselines/ui-visual.json` | 工作区无 E05A 修改 | 否 | E05A 不修页面、不动 fixture、不重写正式产物/既有 finding 基线 |

E05A 不获得暂存、commit、push 或发布授权；R01 的 50 个 index 文件保持不变。

## 十四、E05B 工作树归属

| 文件或范围 | E05B 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/testing/visual-harness.ts` | R01 已暂存，工作区无 E03—E05A 新修改 | 是，仅修改 `reader-catalog` 分支的异步就绪等待 | 保留所有现有 fixture、场景名称和行为断言 |
| `docs/execution/` | E03—E05A 有未暂存记录 | 是 | 只追加 E05B 契约和证据 |
| Reader 业务源码、其他 `src/testing/`、`scripts/`、基线、`dist` | 无 E05B 修改 | 否 | 不用缓存兜底，不混入协调器拆分或业务修复 |

E05B 不获得暂存、commit、push 或发布授权；R01 index 必须继续保持 50 个文件。

## 十五、E05C 工作树归属

| 文件或范围 | E05C 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/testing/visual-harness.ts` | R01 已暂存，E05B 有一处未暂存等待修复 | 是，仅迁移四个 reader 分支并接入单一 dispatch | E05B 的条件等待必须原样进入新模块 |
| `src/testing/visual/readerScenarios.ts` | 不存在 | 是，允许新建 | 只容纳四个 reader 场景，不复制全局状态或业务逻辑 |
| `scripts/unit/*visual-harness-reader-split*` | 不存在 | 是，允许新建 | 结构测试必须阻止旧分支回流和双入口 |
| `docs/execution/` | 有前批未暂存记录 | 是 | 只追加 E05C 契约/证据 |
| 其他 `src/`、场景目录、脚本/基线、`dist` | 无 E05C 修改 | 否 | 不修改业务表现、场景集合或验证规则 |

E05C 不获得暂存、commit、push 或发布授权；R01 index 继续保持 50 个文件。

E05C 完成反查：官方基线仍登记 204 个场景和 612 个运行项；`visual-harness.ts` 为 3688 行，新增 `readerScenarios.ts` 为 123 行。源码范围仅为协调器与该新模块，另新增一项结构单测；R01 index 仍为 50 个文件，正式 `dist/index.js` 和 `dist/index.css` 哈希未变化。

## 十六、U01A 工作树归属

| 文件或范围 | U01A 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/PromptsApp.vue` | R01 已暂存，工作区无 E03—E05 修改 | 是，仅给 26 个 `.pc-icon-btn` 增加准确的 `title`/`aria-label` | 不改已有事件、布局、样式或 5 条几何遗留 |
| `scripts/baselines/ui-contracts.json` | E03A 未暂存精确遗留基线，实际命中 279 条 | 是，只删除该文件 26 条 aria-label 和 12 条 title 签名 | 其他文件和其他规则的签名、计数必须完全不变 |
| `scripts/unit/*prompts-icon-button*` | 不存在 | 是，允许新建一项定向语义测试 | 只扫描该文件并断言两类命中归零 |
| `docs/execution/` | 有前批未暂存记录 | 是 | 只追加 U01A 契约与证据 |
| 其他源码、视觉场景、其他基线、`dist` | 无 U01A 修改 | 否 | 不把其余 171 条按钮缺陷或不同根因混入本批 |

U01A 不获得暂存、commit、push 或发布授权；R01 index 必须继续保持 50 个文件。

U01A 完成反查：`PromptsApp.vue` 两类名称缺陷由 38 条降为 0；UI 契约基线只删除对应签名，保留该文件 5 条几何遗留。11 个提示词场景三尺寸共 33 次和完整验证通过；正式 dist 哈希不变，R01 index 仍为 50。

## 十七、U01B 工作树归属

| 文件或范围 | U01B 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/mvu-modifier/MvuModifierApp.vue`、`MvuTreeNode.vue` | 已跟踪、index/worktree 均无修改 | 是，仅增加 12 个与 title 同值的 aria-label | 不改 title、事件、样式或 MVU 数据逻辑 |
| `scripts/baselines/ui-contracts.json` | U01A 未跟踪基线，238 个签名/241 个实际命中 | 是，只删除 MVU 两文件 12 条 aria-label 签名 | 其他 229 条实际命中必须完全不变 |
| `scripts/unit/*mvu-icon-button*` | 不存在 | 是，允许新建一项定向语义测试 | 只扫描这两个文件并断言 aria-label 命中归零 |
| `docs/execution/` | 有前批未暂存记录 | 是 | 只追加 U01B 契约与证据 |
| 其他源码、视觉场景、其他基线、`dist` | 无 U01B 修改 | 否 | 不混入其余按钮或不同根因 |

U01B 不获得暂存、commit、push 或发布授权；R01 index 必须继续保持 50 个文件。

U01B 完成反查：MVU 两组件的 12 条 aria-label 缺陷归零；UI 契约基线只删除对应签名，实际遗留为 229 条。MVU 三尺寸和完整验证通过；正式 dist 哈希不变，R01 index 仍为 50。

## 十八、C01 本地 Git 检查点归属

用户已授权先建立本地提交，不包含 push。正式 `pnpm build` 已把 U01A/U01B 的可访问名称修改写入产物；`dist/index.js` 和 `dist/index.css` 分别与 U01B 完整验证留下的 `tmp/build-check` 同名文件 SHA-256 一致。最终 staging 必须与 safe-push dry-run 的 67 文件候选相同，且不得包含忽略资料或 `可参考拓展`。

C01 已将上述 67 文件建立为单一检查点提交 `chore: establish verified extension checkpoint`。最终 commit ID 由 `git log -1` 提供；远端未写入，后续继续执行时以该本地 HEAD 作为新的工作树归属基点。

> 历史状态：本段记录的是 C01 刚完成且 P01 尚未推送时的事实；当前状态以文首 R02 覆盖基线为准。

## 十九、R02 自动启动校正归属

| 文件或范围 | R02 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `docs/execution/README.md`、`00`—`03` | 已跟踪、工作树与 index 均干净 | 是，仅校正当前基线、批次状态、追踪关系 | 历史证据保留并明确标注，不重写既有批次结果 |
| `docs/execution/04-U01组件族清单.md` | 不存在 | 是，允许新建 | 精确遗留仍以机器基线为准；清单只定义组件族和自动选择顺序 |
| `src/`、`scripts/`、`dist/`、`components.d.ts` | 已跟踪、工作树与 index 均干净 | 否 | R02 不修改业务、测试、守卫、基线或发布产物 |

R02 不获得暂存、commit、push 或发布授权，也不访问聊天、备份、设置、酒馆预设或真实 API。

R02 完成反查：当前覆盖 SHA、干净工作树起点、正式 dist 哈希和 229 条机器遗留均一致；U01 队列 44 个组件族合计 159 条，旧“当前只执行 U01A”和 E04“进行中”控制状态已清除。R02 只修改执行文档。

## 二十、U01C 工作树归属

| 文件或范围 | U01C 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/recovery/RecoveryApp.vue` | 已跟踪，U01C 前无 diff；机器基线登记 6 条 aria-label、2 条几何遗留 | 是，仅给 6 个 `.pc-icon-btn` 增加与现有准确 title 同义的 aria-label | 不改事件、禁用条件、删除/恢复行为、布局、样式或两条几何遗留 |
| `scripts/unit/recovery-icon-button-contract.test.mjs` | 不存在 | 是，允许新建一项定向语义测试 | 只扫描 RecoveryApp 并断言 6 个名称相等、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | 226 个签名、229 个实际命中 | 是，只删除 RecoveryApp 的 6 条 aria-label 精确签名 | 其余 223 个实际命中必须不变 |
| `docs/execution/` | R02 有未提交文档修改 | 是，仅追加 U01C 契约和证据 | 不覆盖 R02 结果 |
| 其他源码、视觉 fixture、正式 `dist`、Git index | U01C 前无 diff | 否 | 使用既有 recovery 隔离场景；不创建、读取或删除真实备份 |

U01C 不获得暂存、commit、push、发布或真实酒馆操作授权。

U01C 完成反查：源码只增加 6 个与现有 title 同值的 aria-label；定向测试由 6 条失败转为 1/1 通过，机器基线只删除该文件 6 条签名并由 229 降至 223。九个 recovery 场景三尺寸 27/27、完整 `pnpm verify` 85/85 单测与 36/36 UI、15/15 外观证据均通过；正式 dist 哈希不变。

## 二十一、U01D 工作树归属

| 文件或范围 | U01D 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/GenerationPanel.vue`、`GenerationProviderFields.vue`、`GenerationTaskCenter.vue` | 已跟踪，U01D 前无 diff；分别登记 2、1、5 条 aria-label，另有两条几何遗留 | 是，仅给 8 个 `.pc-icon-btn` 增加与现有准确 title 同义的 aria-label | 不改 title、事件、任务生命周期、生成设置、布局、样式或几何遗留 |
| `scripts/unit/generation-shared-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描上述三文件并断言 8 个名称同值、缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01C 后 220 个签名/223 个实际命中 | 是，只删除上述三文件 8 条 aria-label 精确签名 | 其他 215 个实际命中必须不变 |
| `docs/execution/` | R02/U01C 有本轮修改 | 是，仅追加 U01D 契约和证据 | 不覆盖已完成记录 |
| U01C 源码/测试、其他源码、视觉 fixture、正式 `dist`、Git index | 有已验证 U01C 修改或无 diff | 否 | 保留 U01C 结果；只使用既有生成与任务 fixture |

U01D 不获得暂存、commit、push、发布、真实生成请求或真实酒馆设置切换授权。

U01D 完成反查：三个共享生成组件只增加 8 个与现有 title 同值的 aria-label；定向测试由 8 条失败转为 1/1 通过，机器基线只删除对应 8 条并由 223 降至 215。四个生成/任务场景三尺寸 12/12、完整 `pnpm verify` 86/86 单测与 36/36 UI、15/15 外观证据均通过；正式 dist 哈希不变。

## 二十二、U01E 工作树归属

| 文件或范围 | U01E 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/app-builder/AppBuilderApp.vue`、`CustomAppHost.vue` | 已跟踪，U01E 前无 diff；分别登记 4、3 条 aria-label，另有两条几何遗留 | 是，仅给 7 个 `.pc-icon-btn` 增加与现有准确 title 同义的 aria-label | 不改导入/导出/复制/删除/排序/转换事件、布局、样式或几何遗留 |
| `scripts/unit/app-builder-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描上述两文件并断言 7 个名称同值、缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01D 后 212 个签名/215 个实际命中 | 是，只删除上述两文件 7 条 aria-label 精确签名 | 其他 208 个实际命中必须不变 |
| `docs/execution/` | R02/U01C/U01D 有本轮修改 | 是，仅追加 U01E 契约和证据 | 不覆盖已完成记录 |
| 既有本轮源码/测试、其他源码、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有 custom-app 内存 fixture；不操作用户自制 App |

U01E 不获得暂存、commit、push、发布或用户自制 App 导入、导出、复制、删除授权。

U01E 完成反查：两个 App 构建器组件只增加 7 个与现有 title 同值的 aria-label；定向测试由 7 条失败转为 1/1 通过，机器基线只删除对应 7 条并由 215 降至 208。两个 custom-app 场景三尺寸 6/6、完整 `pnpm verify` 87/87 单测与 36/36 UI、15/15 外观证据均通过；正式 dist 哈希不变。

## 二十三、U01F 工作树归属

| 文件或范围 | U01F 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/entry-library/pages/EntryLibraryBindingsPage.vue`、`EntryLibraryCatalogPage.vue`、`EntryLibraryDedupePage.vue` | 已跟踪，U01F 前无 diff；分别登记 2、4、1 条 aria-label，条目库另有三条几何遗留 | 是，仅给 7 个 `.pc-icon-btn` 增加与现有准确 title 同义的 aria-label | 不改同步、删除、分组、排序、查重事件、布局、样式或几何遗留 |
| `scripts/unit/entry-library-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描上述三文件并断言 7 个名称同值、缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01E 后 205 个签名/208 个实际命中 | 是，只删除上述三文件 7 条 aria-label 精确签名 | 其他 201 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01E 有本轮修改 | 是，仅追加 U01F 契约和证据 | 不覆盖已完成记录 |
| 既有本轮源码/测试、其他条目库源码、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有 entry-library 内存 fixture；不操作用户条目或绑定 |

U01F 不获得暂存、commit、push、发布或用户条目新建、同步、删除、查重处理授权。

U01F 完成反查：三个条目库页面只增加 7 个与现有 title 同值的 aria-label；定向测试由 7 条失败转为 1/1 通过，机器基线只删除对应 7 条并由 208 降至 201。三个条目库场景三尺寸 9/9、完整 `pnpm verify` 88/88 单测与 36/36 UI、15/15 外观证据均通过；正式 dist 哈希不变。

## 二十四、U01G 工作树归属

U01G 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 15 个已跟踪修改和 5 个未跟踪文件，全部属于 R02/U01C—U01F 的已验证未发布成果。本批在这些成果上增量执行，不得覆盖或重写前批文件。

| 文件或范围 | U01G 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/preset-manager/pages/PresetCatalogPage.vue`、`PresetDetailPage.vue`、`PresetPromptEditorPage.vue`、`src/apps/preset-manager/PresetPromptRow.vue` | 已跟踪，U01G 前无 diff；分别登记 3、1、1、2 条 aria-label；Editor/Copy 页另有五条几何遗留 | 是，仅给 7 个 `.pc-icon-btn` 增加与现有准确 title 同义的 aria-label | 不改 title、事件、预设/条目状态、拖拽、导入、切换、删除、布局、样式或几何遗留 |
| `scripts/unit/preset-manager-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描上述四文件并断言 7 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01F 后 198 个签名/201 个实际命中 | 是，只删除上述四文件 7 条 aria-label 精确签名 | 其他 194 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01F 有本轮修改 | 是，仅追加 U01G 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、其他 preset-manager 源码、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有 preset-manager 内存 fixture；不读取、切换、导入、删除或改写用户真实预设 |

U01G 不获得暂存、commit、push、发布、正式 build、真实酒馆预设操作或用户文件导入授权。开始时 `pnpm ui:contracts` 为 201 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01G 完成反查：四个预设管理组件只增加 7 个与现有准确 title 同值的 aria-label；定向测试先精确报告 3+1+1+2 条失败，实现后 1/1 通过。机器基线由 198 个签名/201 个实际命中精确降至 191/194，且 7 条差值全部属于本族。七个内存预设场景三尺寸 21/21、完整 `pnpm verify` 的 89/89 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未操作真实预设、用户文件或远端。

## 二十五、U01H 工作树归属

U01H 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 19 个已跟踪修改和 6 个未跟踪文件，全部属于 R02/U01C—U01G 的已验证未发布成果。本批只在无既有 diff 的 BaguApp 上增量执行。

| 文件或范围 | U01H 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/BaguApp.vue` | 已跟踪、无 diff；机器基线登记 5 个签名/6 个实际按钮 | 是，仅给新增词汇、恢复默认词汇、两类删除、新增句式、恢复默认句式按钮增加与既有准确 title 同义的 aria-label | 不改 title、事件、规则增删/重置/保存、布局或样式 |
| `scripts/unit/bagu-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 BaguApp 并断言 6 个实际按钮名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01G 后 191 个签名/194 个实际命中 | 是，只删除 BaguApp 的 5 条精确签名/6 个实际命中 | 其他 188 个实际命中必须不变；几何与主题规则不得变化 |
| `docs/execution/` | R02/U01C—U01G 有本轮修改 | 是，仅追加 U01H 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、其他 Bagu 消费者、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有 `app:bagu` 隔离场景；不读取或改写用户真实规则设置 |

U01H 不获得暂存、commit、push、发布、正式 build、真实酒馆设置操作或用户文件授权。开始时 `pnpm ui:contracts` 为 194 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01H 完成反查：BaguApp 只增加 6 个与现有准确 title 同值的 aria-label；定向测试先精确报告 6 条失败，实现后 1/1 通过。机器基线由 191 个签名/194 个实际命中精确降至 186/188，5 个签名/6 个实际差值全部属于本族，几何与主题遗留不变。`app:bagu` 三尺寸 3/3、完整 `pnpm verify` 的 90/90 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未操作用户规则设置、文件或远端。

## 二十六、U01I 工作树归属

U01I 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 20 个已跟踪修改和 7 个未跟踪文件，全部属于 R02/U01C—U01H 的已验证未发布成果。`src/components/extras/` 在本批前无 diff。

| 文件或范围 | U01I 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/extras/ExtrasBookOverviewPage.vue` | 已跟踪、无 diff；机器基线登记 6 条 aria-label | 是，仅给生成章节、编辑/删除番外、切换排序、编辑/删除总结按钮增加与现有准确 title 同义的 aria-label | 不改 title、emit、排序模型、路由、确认删除、番外/总结状态、布局或样式 |
| `scripts/unit/extras-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描上述页面并断言 6 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01H 后 186 个签名/188 个实际命中 | 是，只删除上述页面 6 条 aria-label 精确签名 | 其他 182 个实际命中必须不变；本目录两条几何遗留不在 U01I |
| `docs/execution/` | R02/U01C—U01H 有本轮修改 | 是，仅追加 U01I 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、其他 extras 页面与消费者、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有 `extras-summary-overview` 内存 fixture；不生成、删除或改写用户真实番外 |

U01I 不获得暂存、commit、push、发布、正式 build、真实番外/总结操作或用户文件授权。开始时 `pnpm ui:contracts` 为 188 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01I 完成反查：ExtrasBookOverviewPage 只增加 6 个与现有准确 title 同值的 aria-label；定向测试先精确报告 6 条失败，实现后 1/1 通过。机器基线由 186 个签名/188 个实际命中精确降至 180/182，6 条差值全部属于本族，本目录两条几何遗留未变化。`extras-summary-overview` 三尺寸 3/3、完整 `pnpm verify` 的 91/91 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未操作真实番外、总结、用户文件或远端。

## 二十七、U01J 工作树归属

U01J 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 21 个已跟踪修改和 8 个未跟踪文件，全部属于 R02/U01C—U01I 的已验证未发布成果。`src/components/forum/` 在本批前无 diff。

| 文件或范围 | U01J 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/forum/ForumBoardPage.vue`、`ForumCatalogPage.vue` | 已跟踪、无 diff；分别登记 2、4 条 aria-label | 是，仅给生成帖子、发帖、新建/编辑/删除板块按钮增加与现有准确 title 同义的 aria-label | 不改 title、emit、路由、确认删除、论坛状态、布局或样式 |
| `scripts/unit/forum-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描上述两页并断言 2+4 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01I 后 180 个签名/182 个实际命中 | 是，只删除上述两页 6 条 aria-label 精确签名 | 其他 176 个实际命中必须不变；本目录一条几何遗留不在 U01J |
| `docs/execution/` | R02/U01C—U01I 有本轮修改 | 是，仅追加 U01J 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、其他 forum 页面与消费者、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用 `forum-catalog`、`forum-board` 隔离 fixture；不生成、删除或改写用户真实论坛内容 |

U01J 不获得暂存、commit、push、发布、正式 build、真实论坛操作或用户文件授权。开始时 `pnpm ui:contracts` 为 182 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01J 完成反查：ForumBoardPage/ForumCatalogPage 只增加 2+4 个与现有准确 title 同值的 aria-label；定向测试先精确报告 6 条失败，实现后 1/1 通过。机器基线由 180 个签名/182 个实际命中精确降至 174/176，6 条差值全部属于本族，本目录一条几何遗留未变化。`forum-catalog`、`forum-board` 三尺寸 6/6、完整 `pnpm verify` 的 92/92 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未操作真实论坛内容、用户文件或远端。

## 二十八、U01K 工作树归属

U01K 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 23 个已跟踪修改和 9 个未跟踪文件，全部属于 R02/U01C—U01J 的已验证未发布成果。`src/apps/profiles/`、`src/testing/visual-harness.ts` 与 `src/testing/visual/scenarioCatalog.ts` 在本批前均无 diff。

| 文件或范围 | U01K 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `ProfilesCatalogPage.vue`、`ProfilesTableEditorPage.vue`、`ProfilesTableManagerPage.vue` | 已跟踪、无 diff；分别登记 3、2、1 条 aria-label；Catalog 另有一条几何遗留 | 是，仅给管理资料表、新增/生成资料、添加字段、拖拽排序、新建资料表按钮增加与现有准确 title 同义的 aria-label | 不改 title、emit、路由、生成、表格/字段写入、拖拽、布局或样式 |
| `src/testing/visual-harness.ts`、`src/testing/visual/scenarioCatalog.ts` | 已跟踪、无 diff；现有 profiles 场景缺少资料表管理页 | 是，仅登记 `profiles-table-manager`，使用 `createProfilesFixture()` 后路由到 `tables` 并断言管理页存在 | 不改其他场景、fixture 数据结构或协调器行为 |
| `scripts/baselines/ui-visual.json` | 204 个场景×3 尺寸的官方基线 | 是，仅通过全场景三尺寸重建增加 `profiles-table-manager` 的 3 条空 findings 记录并把场景总数更新为 205 | 其他 612 条运行记录及其 findings 必须不变；禁止手工伪造局部官方基线 |
| `scripts/unit/profiles-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描上述三页并断言 3+2+1 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01J 后 174 个签名/176 个实际命中 | 是，只删除上述三页 6 条 aria-label 精确签名 | 其他 170 个实际命中必须不变；Catalog 几何遗留不在 U01K |
| `docs/execution/` | R02/U01C—U01J 有本轮修改 | 是，仅追加 U01K 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、其他 profiles 业务文件、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 全部视觉场景使用重置后的内存资料表 fixture；不创建、生成或改写用户真实资料 |

U01K 不获得暂存、commit、push、发布、正式 build、真实资料/资料表操作或用户文件授权。开始时 `pnpm ui:contracts` 为 176 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01K 完成反查：profiles 三页面只增加 3+2+1 个与现有准确 title 同值的 aria-label；定向测试先精确报告 6 条失败，实现后 1/1 通过。新增 `profiles-table-manager` 场景以重置后的内存 fixture 独立渲染成功；官方视觉基线经 205×3 全量重建后只新增该场景三尺寸空 findings，场景 204→205、运行 612→615。机器 UI 基线由 174 个签名/176 个实际命中精确降至 168/170，6 条差值全部属于本族，Catalog 几何遗留不变。三个 profiles 场景三尺寸 9/9、完整 `pnpm verify` 的 93/93 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未操作真实资料、资料表、用户文件或远端。

## 二十九、U01L 工作树归属

U01L 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 29 个已跟踪修改和 10 个未跟踪文件，全部属于 R02/U01C—U01K 的已验证未发布成果。`src/apps/regex-wizard/RegexWizardApp.vue` 在本批前无 diff。

| 文件或范围 | U01L 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/regex-wizard/RegexWizardApp.vue` | 已跟踪、无 diff；机器基线登记 6 条 aria-label，另有两条几何遗留 | 是，仅给上移/下移/删除字段与复制表达式/模板/Flags 按钮增加与现有准确 title 同义的 aria-label | 不改 title、draft、生成/测试/复制/保存逻辑、布局、样式或几何遗留 |
| `scripts/unit/regex-wizard-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 RegexWizardApp 并断言 6 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01K 后 168 个签名/170 个实际命中 | 是，只删除 RegexWizardApp 6 条 aria-label 精确签名 | 其他 164 个实际命中必须不变；两条几何遗留不在 U01L |
| `docs/execution/` | R02/U01C—U01K 有本轮修改 | 是，仅追加 U01L 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有 `regex-wizard-fields` 隔离场景；不保存规则、不读取用户真实预设或发起 API |

U01L 不获得暂存、commit、push、发布、正式 build、真实规则/预设操作或用户文件授权。开始时 `pnpm ui:contracts` 为 170 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01L 完成反查：RegexWizardApp 只增加 6 个与现有准确 title 同值的 aria-label；定向测试先精确报告 6 条失败，实现后 1/1 通过。机器 UI 基线由 168 个签名/170 个实际命中精确降至 162/164，6 条差值全部属于本族，两条几何遗留未变化。`regex-wizard-fields` 三尺寸 3/3、完整 `pnpm verify` 的 94/94 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未保存真实规则、读取真实预设、操作用户文件或远端。

## 三十、U01M 工作树归属

U01M 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 30 个已跟踪修改和 11 个未跟踪文件，全部属于 R02/U01C—U01L 的已验证未发布成果。`src/apps/relationship/RelationshipApp.vue` 在本批前无 diff。

| 文件或范围 | U01M 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/relationship/RelationshipApp.vue` | 已跟踪、无 diff；机器基线登记 6 条 aria-label | 是，仅给 AI 识别、添加关联人物、新增/删除人物、新增/删除关系按钮增加与现有准确 title 同义的 aria-label | 不改 title、路由/生成、人物/资料关联/关系增删、确认、布局或样式 |
| `scripts/unit/relationship-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 RelationshipApp 并断言 6 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01L 后 162 个签名/164 个实际命中 | 是，只删除 RelationshipApp 6 条 aria-label 精确签名 | 其他 158 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01L 有本轮修改 | 是，仅追加 U01M 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有 `app:relationship` 空关系网隔离场景；不新增、删除、生成或关联真实资料 |

U01M 不获得暂存、commit、push、发布、正式 build、真实关系网/资料操作或用户文件授权。开始时 `pnpm ui:contracts` 为 164 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01M 完成反查：RelationshipApp 只增加 6 个与现有准确国际化 title 同值的 aria-label；定向测试先精确报告 6 条失败，实现后 1/1 通过。机器 UI 基线由 162 个签名/164 个实际命中精确降至 156/158，6 条差值全部属于本族。`app:relationship` 空关系网三尺寸 3/3、完整 `pnpm verify` 的 95/95 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未新增、删除、生成或关联真实关系/资料，未操作用户文件或远端。

## 三十一、U01N 工作树归属

U01N 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 31 个已跟踪修改和 12 个未跟踪文件，全部属于 R02/U01C—U01M 的已验证未发布成果。`src/apps/workbench/WorkbenchApp.vue` 在本批前无 diff。

| 文件或范围 | U01N 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/workbench/WorkbenchApp.vue` | 已跟踪、无 diff；机器基线登记 6 条 aria-label，另有三条几何遗留；步骤展开按钮已合格 | 是，仅给新建流程、刷新预设、新增步骤、步骤上移/下移/删除按钮增加与现有准确 title 同义的 aria-label | 不改既有展开按钮、title、流程/预设/步骤逻辑、布局、样式或几何遗留 |
| `scripts/unit/workbench-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 WorkbenchApp 并断言全部 7 个图标按钮名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01M 后 156 个签名/158 个实际命中 | 是，只删除 WorkbenchApp 6 条 aria-label 精确签名 | 其他 152 个实际命中必须不变；三条几何遗留不在 U01N |
| `docs/execution/` | R02/U01C—U01M 有本轮修改 | 是，仅追加 U01N 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有三个内存工作流场景；不执行真实生成、预设切换或用户文件操作 |

U01N 不获得暂存、commit、push、发布、正式 build、真实工作流生成/预设操作或用户文件授权。开始时 `pnpm ui:contracts` 为 158 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01N 完成反查：WorkbenchApp 只增加 6 个与现有准确国际化 title 同值的 aria-label，既有步骤展开按钮未变化；定向测试先精确报告 6 条失败，实现后全部 7 个图标按钮 1/1 通过。机器 UI 基线由 156 个签名/158 个实际命中精确降至 150/152，6 条差值全部属于本族，三条几何遗留未变化。三个工作台隔离场景三尺寸 9/9、完整 `pnpm verify` 的 96/96 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未执行真实生成、预设切换、用户文件或远端操作。

## 三十二、U01O 工作树归属

U01O 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 32 个已跟踪修改和 13 个未跟踪文件，全部属于 R02/U01C—U01N 的已验证未发布成果。`src/apps/card-writer/CardWriterApp.vue` 在本批前无 diff。

| 文件或范围 | U01O 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/card-writer/CardWriterApp.vue` | 已跟踪、无 diff；机器基线登记 5 条 aria-label，另有一条几何遗留 | 是，仅给成品库入口、刷新世界书、复制/导入/删除成品按钮增加与现有准确 title 同义的 aria-label | 不改 title、生成/导航/世界书/复制/资料导入/删除逻辑、布局、样式或几何遗留 |
| `scripts/unit/card-writer-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 CardWriterApp 并断言 5 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01N 后 150 个签名/152 个实际命中 | 是，只删除 CardWriterApp 5 条 aria-label 精确签名 | 其他 147 个实际命中必须不变；一条几何遗留不在 U01O |
| `docs/execution/` | R02/U01C—U01N 有本轮修改 | 是，仅追加 U01O 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有空配置与内存成品场景；不点击刷新、导入、删除，不写真实聊天/世界书/资料 |

U01O 不获得暂存、commit、push、发布、正式 build、真实聊天/世界书/资料操作、API 或用户文件授权。开始时 `pnpm ui:contracts` 为 152 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01O 完成反查：CardWriterApp 只增加 5 个与现有准确静态 title 同值的 aria-label；定向测试先精确报告 5 条失败，实现后 1/1 通过。机器 UI 基线由 150 个签名/152 个实际命中精确降至 145/147，5 条差值全部属于本族，一条几何遗留未变化。生成根页与内存成品流程两场景三尺寸 6/6、完整 `pnpm verify` 的 97/97 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未点击世界书刷新、资料导入或删除，未写真实聊天/世界书/资料，未操作 API、用户文件或远端。

## 三十三、U01P 工作树归属

U01P 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 33 个已跟踪修改和 14 个未跟踪文件，全部属于 R02/U01C—U01O 的已验证未发布成果。`src/apps/cloud-media/CloudMediaApp.vue` 在本批前无 diff。

| 文件或范围 | U01P 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/cloud-media/CloudMediaApp.vue` | 已跟踪、无 diff；机器基线登记 5 条 aria-label | 是，仅给 AI 生成入口、服务设置、配置新增/删除和密钥显隐按钮增加与现有准确 title 同义的 aria-label | 不改 title、路由/生成/配置/密钥逻辑、布局或样式 |
| `scripts/unit/cloud-media-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 CloudMediaApp 并断言 5 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01O 后 145 个签名/147 个实际命中 | 是，只删除 CloudMediaApp 5 条 aria-label 精确签名 | 其他 142 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01O 有本轮修改 | 是，仅追加 U01P 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有三个隔离路由场景；不发 API、不上传文件、不点击配置增删 |

U01P 不获得暂存、commit、push、发布、正式 build、真实配置增删、API、上传或用户文件授权。开始时 `pnpm ui:contracts` 为 147 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01P 完成反查：CloudMediaApp 只增加 5 个与现有准确国际化 title 同值的 aria-label，动态密钥名称保持同步；定向测试先精确报告 5 条失败，实现后 1/1 通过。机器 UI 基线由 145 个签名/147 个实际命中精确降至 140/142，5 条差值全部属于本族。三个云媒体隔离路由场景三尺寸 9/9、完整 `pnpm verify` 的 98/98 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未发 API、上传文件、增删真实配置、操作用户文件或远端。

## 三十四、U01Q 工作树归属

U01Q 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 34 个已跟踪修改和 15 个未跟踪文件，全部属于 R02/U01C—U01P 的已验证未发布成果。`src/components/diary/DiaryBookPage.vue` 在本批前无 diff。

| 文件或范围 | U01Q 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/diary/DiaryBookPage.vue` | 已跟踪、无 diff；机器基线登记 5 条 aria-label | 是，仅给生成、批量、排序、重命名、删除书架按钮增加与现有准确 title 同义的 aria-label | 不改 title、emit、父级业务、布局或样式 |
| `scripts/unit/diary-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 DiaryBookPage 并断言 5 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01P 后 140 个签名/142 个实际命中 | 是，只删除 DiaryBookPage 5 条 aria-label 精确签名 | 其他 137 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01P 有本轮修改 | 是，仅追加 U01Q 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、其他 diary 文件、视觉 fixture、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 使用既有内存书架场景；不执行真实生成、重命名或删除 |

U01Q 不获得暂存、commit、push、发布、正式 build、真实聊天/日记操作或用户文件授权。开始时 `pnpm ui:contracts` 为 142 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01Q 完成反查：DiaryBookPage 只增加 5 个与现有准确 title 同值的 aria-label，排序动态名称保持同步；定向测试先精确报告 5 条失败，实现后 1/1 通过。机器 UI 基线由 140 个签名/142 个实际命中精确降至 135/137，5 条差值全部属于本族。日记书架与目录排序持久化两场景三尺寸 6/6、完整 `pnpm verify` 的 99/99 单测、36/36 UI、15/15 外观证据均通过；Git index 仍为 0，正式 dist 哈希不变，未执行真实生成、重命名、删除、聊天/日记或用户文件操作。

## 三十五、U01R 工作树归属

U01R 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 35 个已跟踪修改和 16 个未跟踪文件，全部属于 R02/U01C—U01Q 的已验证未发布成果。reader-shared 四个业务组件与 `readerScenarios.ts` 在本批前无 diff；`visual-harness.ts` 和 `scenarioCatalog.ts` 只有 U01K 已验证的 `profiles-table-manager` 场景 diff，U01R 必须保留。

| 文件或范围 | U01R 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/ReaderApp.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label | 是，仅给刷新与规则开关增加和现有准确 title 同值的 aria-label | 不改加载、规则状态、聊天读取、布局或样式 |
| `src/components/ReaderDetailShell.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label 与另有 1 条几何遗留 | 是，仅给阅读工具入口增加和现有 title 同值的 aria-label | 不改菜单、底栏、选区、几何或样式 |
| `src/components/ReaderTextEditModal.vue`、`src/components/ReasoningModal.vue` | 已跟踪、无 diff；各登记 1 条 aria-label | 是，仅给关闭按钮增加和现有准确 title 同值的 aria-label | 不改关闭事件、正文替换、思维链内容、布局或样式 |
| `scripts/unit/reader-shared-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描四组件并断言 2+1+1+1 个名称同值、aria 缺失归零 |
| `src/testing/visual/readerScenarios.ts` | 已跟踪、无 diff | 是，仅新增只读 `reader-text-edit-modal` 场景 | 只建立 DOM 选区并打开弹窗，不点击保存或写回 |
| `src/testing/visual-harness.ts`、`src/testing/visual/scenarioCatalog.ts` | 已跟踪、有 U01K 已验证 diff | 是，仅追加 `card-writer-reasoning-modal` 分支和两个场景目录项 | 保留 `profiles-table-manager`；只用内存成品，不发 API |
| `scripts/baselines/ui-visual.json` | 已跟踪、有 U01K 已验证的三条场景记录；205 场景×3 尺寸 | 是，仅可经 207×3 全量重建新增两个场景六条记录及对应元数据 | 不得接受既有场景 finding 或外观变化 |
| `scripts/baselines/ui-contracts.json` | U01Q 后 135 个签名/137 个实际命中 | 是，只删除四组件 5 条 aria-label 精确签名 | 其他 132 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01Q 有本轮修改 | 是，仅追加 U01R 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、CardWriterApp、Reader store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 不改业务状态，不保存文本/成品，不操作真实聊天、生成、预设或用户文件 |

U01R 不获得暂存、commit、push、发布、正式 build、真实聊天写回、真实生成、预设/世界书/API 或用户文件授权。登记时 `pnpm ui:contracts` 为 137 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01R 完成反查：reader-shared 四组件只增加 5 个与现有准确 title 同值的 aria-label，动态规则名称保持同步；两个新增弹窗场景只建立内存选区/成品并打开弹窗，没有保存或生成。官方视觉基线 207×3 共 621/621 通过，只新增两场景六条记录；机器 UI 基线由 135 个签名/137 个实际命中精确降至 130/132。四个受影响入口三尺寸 12/12、完整 `pnpm verify` 的 100/100 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 40 个已跟踪修改、17 个未跟踪文件，均为 R02/U01C—U01R 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未执行真实聊天写回、生成、预设/世界书/API、用户文件或远端操作。

## 三十六、U01S 工作树归属

U01S 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 40 个已跟踪修改和 17 个未跟踪文件，全部属于 R02/U01C—U01R 的已验证未发布成果。`src/apps/timekeeper/TimekeeperApp.vue` 在本批前无 diff。

| 文件或范围 | U01S 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/timekeeper/TimekeeperApp.vue` | 已跟踪、无 diff；机器基线登记 5 条 aria-label | 是，仅给写入输入框、保存/删除历法模板、新增/删除人物按钮增加与现有准确 title 同值的 aria-label | 不改 title、事件、确认链、store、布局或样式 |
| `scripts/unit/timekeeper-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 TimekeeperApp 并断言 5 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01R 后 130 个签名/132 个实际命中 | 是，只删除 TimekeeperApp 5 条 aria-label 精确签名 | 其他 127 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01R 有本轮修改 | 是，仅追加 U01S 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、视觉 fixture、timekeeper store、profiles、设置、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用既有隔离根页场景；不点击写入、保存、删除或新增动作 |

U01S 不获得暂存、commit、push、发布、正式 build、真实历法/人物资料修改、酒馆输入框写入、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 132 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01S 完成反查：TimekeeperApp 只增加 5 个与现有准确国际化 title 同值的 aria-label；定向测试先精确报告 5 条失败，实现后 1/1 通过。机器 UI 基线由 130 个签名/132 个实际命中精确降至 125/127，5 条差值全部属于本族。隔离根页三尺寸 3/3、完整 `pnpm verify` 的 101/101 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 41 个已跟踪修改、18 个未跟踪文件，均为 R02/U01C—U01S 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未点击写入、保存、删除或新增动作，未操作真实历法、人物资料、设置、聊天、用户文件或远端。

## 三十七、U01T 工作树归属

U01T 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 41 个已跟踪修改和 18 个未跟踪文件，全部属于 R02/U01C—U01S 的已验证未发布成果。worldbook-link 三个目标页面在本批前均无 diff。

| 文件或范围 | U01T 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/worldbook-link/pages/WorldbookCatalogPage.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label 与 1 条几何遗留 | 是，仅给刷新按钮增加与现有准确 title 同值的 aria-label | 不改 emit、禁用状态、几何、布局或样式 |
| `src/apps/worldbook-link/pages/WorldbookDetailPage.vue` | 已跟踪、无 diff；机器基线登记 3 条 aria-label 与 1 条几何遗留 | 是，仅给应用、捕获、停止联动增加与现有准确 title 同值的 aria-label | 不改父级世界书操作、灯色、条目开关、几何或样式 |
| `src/apps/worldbook-link/pages/WorldbookEntryEditorPage.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label 与 1 条几何遗留 | 是，仅给删除条目增加与现有准确 title 同值的 aria-label | 不改删除确认、保存、字段、几何或样式 |
| `scripts/unit/worldbook-link-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描三页面并断言 1+3+1 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01S 后 125 个签名/127 个实际命中 | 是，只删除三页面 5 条 aria-label 精确签名 | 其他 122 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01S 有本轮修改 | 是，仅追加 U01T 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、WorldbookLinkApp/store/API、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存场景；不读取或写入用户真实世界书 |

U01T 不获得暂存、commit、push、发布、正式 build、真实世界书读取/写入、预设、聊天、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 127 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01T 完成反查：worldbook-link 三页面只增加 5 个与现有准确 title 同值的 aria-label；定向测试先精确报告 5 条失败，实现后 1/1 通过。机器 UI 基线由 125 个签名/127 个实际命中精确降至 120/122，5 条差值全部属于本族。三个内存世界书场景三尺寸 9/9、完整 `pnpm verify` 的 102/102 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 44 个已跟踪修改、19 个未跟踪文件，均为 R02/U01C—U01T 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未读取/写入真实世界书，未点击应用、捕获、停止联动或删除，未操作真实预设、聊天、设置、用户文件或远端。

## 三十八、U01U 工作树归属

U01U 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 44 个已跟踪修改和 19 个未跟踪文件，全部属于 R02/U01C—U01T 的已验证未发布成果。`src/components/ChatArchiveApp.vue` 在本批前无 diff。

| 文件或范围 | U01U 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/ChatArchiveApp.vue` | 已跟踪、无 diff；机器基线登记 4 条 aria-label | 是，仅给刷新角色、刷新聊天、迁移、跳转按钮增加与现有准确 title 同值的 aria-label | 不改读取、备份、恢复、改名、迁移、跳转、布局或样式 |
| `scripts/unit/chat-archive-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 ChatArchiveApp 并断言 4 个名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01T 后 120 个签名/122 个实际命中 | 是，只删除 ChatArchiveApp 4 条 aria-label 精确签名 | 其他 118 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01T 有本轮修改 | 是，仅追加 U01U 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、chat/archive/backup util、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用视觉环境；不操作用户真实聊天或备份 |

U01U 不获得暂存、commit、push、发布、正式 build、真实聊天/备份/设置/用户文件读取写入、迁移或跳转授权。登记时 `pnpm ui:contracts` 为 122 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01U 完成反查：ChatArchiveApp 只增加 4 个与现有准确国际化 title 同值的 aria-label；定向测试先精确报告 4 条失败，实现后 1/1 通过。机器 UI 基线由 120 个签名/122 个实际命中精确降至 116/118，4 条差值全部属于本族。三个 archive 视觉环境场景三尺寸 9/9、完整 `pnpm verify` 的 103/103 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 45 个已跟踪修改、20 个未跟踪文件，均为 R02/U01C—U01U 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未执行真实聊天/备份读取写入、恢复、导入导出、改名、迁移或跳转，未操作设置、用户文件或远端。

## 三十九、U01V 工作树归属

U01V 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 45 个已跟踪修改和 20 个未跟踪文件，全部属于 R02/U01C—U01U 的已验证未发布成果。`src/apps/gallery/GalleryApp.vue` 在本批前无 diff。

| 文件或范围 | U01V 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/gallery/GalleryApp.vue` | 已跟踪、无 diff；机器基线登记 4 条 aria-label，另有 2 个已合格名称和 1 条几何遗留 | 是，仅给新增、编辑、下载、删除按钮增加与现有准确 title 同值的 aria-label | 不改前后翻页、上传/下载/删除、路由、几何、布局或样式 |
| `scripts/unit/gallery-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 GalleryApp 全部 6 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01U 后 116 个签名/118 个实际命中 | 是，只删除 GalleryApp 4 条 aria-label 精确签名 | 其他 114 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01U 有本轮修改 | 是，仅追加 U01V 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、media store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 只打开隔离根页；不操作用户文件或图片数据 |

U01V 不获得暂存、commit、push、发布、正式 build、真实文件上传/下载/删除、图片数据、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 118 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01V 完成反查：GalleryApp 只增加 4 个与现有准确国际化 title 同值的 aria-label；定向测试先精确报告新增、编辑、下载、删除 4 条失败，且上一张/下一张已合格，实现后 1/1 通过。机器 UI 基线由 116 个签名/118 个实际命中精确降至 112/114，4 条差值全部属于本族，GalleryApp 几何遗留不变。`app:gallery` 隔离根页三尺寸 3/3、完整 `pnpm verify` 的 104/104 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 46 个已跟踪修改、21 个未跟踪文件，均为 R02/U01C—U01V 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未执行图片上传、保存、下载或删除，未操作设置、用户文件或远端。

## 四十、U01W 工作树归属

U01W 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 46 个已跟踪修改和 21 个未跟踪文件，全部属于 R02/U01C—U01V 的已验证未发布成果。`src/apps/theme/ThemeApp.vue` 在本批前无 diff。

| 文件或范围 | U01W 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/theme/ThemeApp.vue` | 已跟踪、无 diff；机器基线登记 4 条 aria-label，另有 3 条几何遗留 | 是，仅给两个返回、清除覆盖和导入字体按钮增加与现有准确 title 同值的 aria-label | 不改页面路由、主题/图标设置、字体导入、布局或样式 |
| `scripts/unit/theme-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 ThemeApp 并断言全部 4 个图标按钮名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01V 后 112 个签名/114 个实际命中 | 是，只删除 ThemeApp 4 条 aria-label 精确签名 | 其他 110 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01V 有本轮修改 | 是，仅追加 U01W 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、settings store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 只打开隔离根页；不修改真实主题设置或导入字体文件 |

U01W 不获得暂存、commit、push、发布、正式 build、真实主题/图标设置修改、字体文件导入、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 114 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01W 完成反查：ThemeApp 只增加 4 个与现有准确国际化 title 同值的 aria-label；定向测试先精确报告两个返回、清除覆盖、导入字体 4 条失败，实现后 1/1 通过。机器 UI 基线由 112 个签名/114 个实际命中精确降至 109/110，4 个实际命中差值全部属于本族，ThemeApp 三条几何遗留不变。`app:theme` 隔离根页三尺寸 3/3、完整 `pnpm verify` 的 105/105 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 47 个已跟踪修改、22 个未跟踪文件，均为 R02/U01C—U01W 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未切换主题、清除覆盖、导入字体或写入设置，未操作用户文件或远端。

## 四十一、U01X 工作树归属

U01X 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 47 个已跟踪修改和 22 个未跟踪文件，全部属于 R02/U01C—U01W 的已验证未发布成果。`src/apps/video/VideoApp.vue` 在本批前无 diff。

| 文件或范围 | U01X 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/video/VideoApp.vue` | 已跟踪、无 diff；机器基线登记 4 条 aria-label，另有 2 个已合格名称和 1 条几何遗留 | 是，仅给新增、编辑、下载、删除按钮增加与现有准确 title 同值的 aria-label | 不改前后翻页、上传/保存/下载/删除、路由、布局或样式 |
| `scripts/unit/video-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 VideoApp 全部 6 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01W 后 109 个签名/110 个实际命中 | 是，只删除 VideoApp 4 条 aria-label 精确签名 | 其他 106 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01W 有本轮修改 | 是，仅追加 U01X 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、media store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 只打开隔离根页；不操作用户文件或视频数据 |

U01X 不获得暂存、commit、push、发布、正式 build、真实文件上传/保存/下载/删除、视频数据、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 110 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01X 完成反查：VideoApp 只增加 4 个与现有准确国际化 title 同值的 aria-label；定向测试先精确报告新增、编辑、下载、删除 4 条失败，且上一个/下一个已合格，实现后 1/1 通过。机器 UI 基线由 109 个签名/110 个实际命中精确降至 105/106，4 条差值全部属于本族，VideoApp 几何遗留不变。`app:video` 隔离根页三尺寸 3/3、完整 `pnpm verify` 的 106/106 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 48 个已跟踪修改、23 个未跟踪文件，均为 R02/U01C—U01X 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未执行视频上传、保存、下载或删除，未操作设置、用户文件或远端。

## 四十二、U01Y 工作树归属

U01Y 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 48 个已跟踪修改和 23 个未跟踪文件，全部属于 R02/U01C—U01X 的已验证未发布成果。`src/components/PhoneOverlay.vue` 在本批前无 diff。

| 文件或范围 | U01Y 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/PhoneOverlay.vue` | 已跟踪、无 diff；机器基线登记文件夹控制 3 条 aria-label，另有 1 条几何遗留 | 是，仅给前移、移出到主界面、后移按钮增加与现有准确 title 同值的 aria-label | 不改文件夹、主页、Dock、持久化、布局或样式 |
| `scripts/unit/phone-overlay-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 只扫描 PhoneOverlay 并断言全部 3 个图标按钮名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01X 后 105 个签名/106 个实际命中 | 是，只删除 PhoneOverlay 3 条 aria-label 精确签名 | 其他 103 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01X 有本轮修改 | 是，仅追加 U01Y 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、主页/Dock store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 只打开隔离主页；不修改真实主页或 Dock 设置 |

U01Y 不获得暂存、commit、push、发布、正式 build、真实主页/Dock 设置修改、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 106 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01Y 完成反查：PhoneOverlay 只给文件夹管理区前移、移出到主界面、后移 3 个按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 3 条失败，实现后 1/1 通过。机器 UI 基线由 105 个签名/106 个实际命中精确降至 102/103，3 条差值全部属于本族，PhoneOverlay 几何遗留不变。`home` 隔离主页三尺寸 3/3、完整 `pnpm verify` 的 107/107 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 49 个已跟踪修改、24 个未跟踪文件，均为 R02/U01C—U01Y 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未重排、移出或解散文件夹，未写入主页/Dock 设置或操作用户文件、远端。

## 四十三、U01Z 工作树归属

U01Z 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 49 个已跟踪修改和 24 个未跟踪文件，全部属于 R02/U01C—U01Y 的已验证未发布成果。`src/apps/regex-display/RegexDisplayApp.vue` 在本批前无 diff。

| 文件或范围 | U01Z 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/regex-display/RegexDisplayApp.vue` | 已跟踪、无 diff；机器基线登记 3 条 aria-label，另有 2 个已合格名称和 2 条几何遗留 | 是，仅给新增规则、提高优先级、降低优先级按钮增加与现有准确 title 同值的 aria-label | 不改规则 CRUD、排序、预览、持久化、布局或样式 |
| `scripts/unit/regex-display-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 RegexDisplayApp 全部 5 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01Y 后 102 个签名/103 个实际命中 | 是，只删除 RegexDisplayApp 3 条 aria-label 精确签名 | 其他 100 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01Y 有本轮修改 | 是，仅追加 U01Z 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、正则 store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 只打开隔离根页；不修改真实正则规则 |

U01Z 不获得暂存、commit、push、发布、正式 build、真实正则规则修改、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 103 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01Z 完成反查：RegexDisplayApp 只给新增规则、提高优先级、降低优先级 3 个按钮增加与现有准确国际化 title 同值的 aria-label；复制/删除两按钮原已合格。定向测试先精确报告 3 条失败，实现后 1/1 通过；测试标签提取对 `>=` 的误判仅在本批测试内修正。机器 UI 基线由 102 个签名/103 个实际命中精确降至 99/100，3 条差值全部属于本族，两条几何遗留不变。`app:regex-display` 三尺寸 3/3、完整 `pnpm verify` 的 108/108 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 50 个已跟踪修改、25 个未跟踪文件，均为 R02/U01C—U01Z 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未新增、复制、删除、排序或保存正则规则，未操作用户文件或远端。

## 四十四、U01AA 工作树归属

U01AA 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 50 个已跟踪修改和 25 个未跟踪文件，全部属于 R02/U01C—U01Z 的已验证未发布成果。`src/components/summary/SummaryBookPage.vue` 与 `SummaryImportPage.vue` 在本批前均无 diff。

| 文件或范围 | U01AA 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/summary/SummaryBookPage.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label | 是，仅给生成总结、目录排序按钮增加与现有准确 title 同值的 aria-label | 不改生成、排序、导入、批量、改名、删除或布局 |
| `src/components/summary/SummaryImportPage.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给刷新楼层按钮增加与现有准确 title 同值的 aria-label | 不改聊天楼层读取、选择、导入或布局 |
| `scripts/unit/summary-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描两文件全部 3 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01Z 后 99 个签名/100 个实际命中 | 是，只删除 summary 两文件 3 条 aria-label 精确签名 | 其他 97 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01Z 有本轮修改 | 是，仅追加 U01AA 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、总结/聊天 store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存场景；不读取/写入真实聊天或总结数据 |

U01AA 不获得暂存、commit、push、发布、正式 build、真实聊天/总结数据读取写入、生成、导入、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 100 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AA 完成反查：SummaryBookPage 只给生成总结、动态排序 2 个按钮，SummaryImportPage 只给刷新楼层按钮增加与现有准确 title 同值的 aria-label。定向测试先精确报告 2+1 条失败，实现后 1/1 通过。机器 UI 基线由 99 个签名/100 个实际命中精确降至 96/97，3 条差值全部属于本族。`summary-book`、`summary-import` 两场景三尺寸 6/6、完整 `pnpm verify` 的 109/109 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 52 个已跟踪修改、26 个未跟踪文件，均为 R02/U01C—U01AA 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未触发生成、排序、刷新或导入，未读取/写入真实聊天或总结数据、用户文件或远端。

## 四十五、U01AB 工作树归属

U01AB 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 52 个已跟踪修改和 26 个未跟踪文件，全部属于 R02/U01C—U01AA 的已验证未发布成果。`src/apps/comfy/ComfyApp.vue` 在本批前无 diff。

| 文件或范围 | U01AB 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/comfy/ComfyApp.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label，另有 2 个已合格名称和 1 条几何遗留 | 是，仅给读取模型、新建工作流按钮增加与现有准确 title 同值的 aria-label | 不改服务读取、工作流 CRUD/导入、参数、持久化、布局或样式 |
| `scripts/unit/comfy-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 ComfyApp 全部 4 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AA 后 96 个签名/97 个实际命中 | 是，只删除 ComfyApp 2 条 aria-label 精确签名 | 其他 95 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AA 有本轮修改 | 是，仅追加 U01AB 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、Comfy 设置/服务、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存配置，只展开动作菜单；不连接真实服务或修改工作流 |

U01AB 不获得暂存、commit、push、发布、正式 build、真实 Comfy 服务连接、工作流修改、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 97 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AB 完成反查：ComfyApp 只给读取模型、新建工作流 2 个按钮增加与现有准确动态/静态 title 同值的 aria-label；两个参数折叠按钮原已合格。定向测试先精确报告 2 条失败，实现后 1/1 通过。机器 UI 基线由 96 个签名/97 个实际命中精确降至 94/95，2 条差值全部属于本族，ComfyApp 几何遗留不变。`comfy-action-menu` 三尺寸 3/3、完整 `pnpm verify` 的 110/110 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 53 个已跟踪修改、27 个未跟踪文件，均为 R02/U01C—U01AB 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未连接 Comfy 服务、读取模型或创建/修改工作流，未操作设置、用户文件或远端。

## 四十六、U01AC 工作树归属

U01AC 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 53 个已跟踪修改和 27 个未跟踪文件，全部属于 R02/U01C—U01AB 的已验证未发布成果。`src/apps/digest/DigestApp.vue` 在本批前无 diff。

| 文件或范围 | U01AC 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/digest/DigestApp.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label，另有 1 个已合格动态名称和 1 条几何遗留 | 是，仅给手动新增、AI 摘抄按钮增加与现有准确 title 同值的 aria-label | 不改排序名称、摘录 CRUD/生成/持久化、布局或样式 |
| `scripts/unit/digest-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 DigestApp 全部 3 个图标按钮，断言名称存在并保留排序按钮既有准确独立文案 |
| `scripts/baselines/ui-contracts.json` | U01AB 后 94 个签名/95 个实际命中 | 是，只删除 DigestApp 2 条 aria-label 精确签名 | 其他 93 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AB 有本轮修改 | 是，仅追加 U01AC 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、摘录 store/生成管线、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 只打开隔离根页；不真实生成或修改摘录数据 |

U01AC 不获得暂存、commit、push、发布、正式 build、真实生成、摘录数据修改、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 95 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AC 完成反查：DigestApp 只给手动新增、AI 摘抄 2 个按钮增加与现有准确 title 同值的 aria-label；排序按钮既有较简短但准确的动态 aria-label 原样保留。定向测试先精确报告 2 条失败，实现后 1/1 通过。机器 UI 基线由 94 个签名/95 个实际命中精确降至 92/93，2 条差值全部属于本族，DigestApp 几何遗留不变。`app:digest` 三尺寸 3/3、完整 `pnpm verify` 的 111/111 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 54 个已跟踪修改、28 个未跟踪文件，均为 R02/U01C—U01AC 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未排序、新增、生成或保存摘录，未操作用户文件或远端。

## 四十七、U01AD 工作树归属

U01AD 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 54 个已跟踪修改和 28 个未跟踪文件，全部属于 R02/U01C—U01AC 的已验证未发布成果。`src/components/letters/LettersBookPage.vue` 在本批前无 diff。

| 文件或范围 | U01AD 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/letters/LettersBookPage.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label | 是，仅给生成回信、动态排序按钮增加与现有准确 title 同值的 aria-label | 不改生成、排序、打开书信、数据、布局或样式 |
| `scripts/unit/letters-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 LettersBookPage 全部 2 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AC 后 92 个签名/93 个实际命中 | 是，只删除 LettersBookPage 2 条 aria-label 精确签名 | 其他 91 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AC 有本轮修改 | 是，仅追加 U01AD 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、书信 store/生成管线、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存书信场景；不真实生成或修改书信数据 |

U01AD 不获得暂存、commit、push、发布、正式 build、真实生成、书信数据修改、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 93 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AD 完成反查：LettersBookPage 只给生成回信、动态排序 2 个按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 2 条失败，实现后 1/1 通过。机器 UI 基线由 92 个签名/93 个实际命中精确降至 90/91，2 条差值全部属于本族。`letters-book` 三尺寸 3/3、完整 `pnpm verify` 的 112/112 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 55 个已跟踪修改、29 个未跟踪文件，均为 R02/U01C—U01AD 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未生成、排序、保存或修改书信数据，未操作用户文件或远端。

## 四十八、U01AE 工作树归属

U01AE 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 55 个已跟踪修改和 29 个未跟踪文件，全部属于 R02/U01C—U01AD 的已验证未发布成果。`src/components/settings/SettingsConnectionPanel.vue` 在本批前无 diff。

| 文件或范围 | U01AE 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/settings/SettingsConnectionPanel.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label | 是，仅给显示/隐藏 API Key、获取模型列表按钮增加与现有准确 title 同值的 aria-label | 不改事件、API Key、模型、外部配置、连接、数据、布局或样式 |
| `scripts/unit/settings-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 SettingsConnectionPanel 全部 7 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AD 后 90 个签名/91 个实际命中 | 是，只删除 SettingsConnectionPanel 2 条 aria-label 精确签名 | 其他 89 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AD 有本轮修改 | 是，仅追加 U01AE 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、settings store、外部 API adapter、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存外部 API 设置场景；不读取 Key、不连接服务、不写入真实设置 |

U01AE 不获得暂存、commit、push、发布、正式 build、真实 API/Key 读取或请求、设置修改、配置删除或用户文件授权。登记时 `pnpm ui:contracts` 为 91 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AE 完成反查：SettingsConnectionPanel 只给显示/隐藏 API Key、获取模型列表 2 个按钮增加与现有准确 title 同值的 aria-label，其他 5 个图标按钮原已合格；定向测试先精确报告 2 条失败，实现后 1/1 通过。机器 UI 基线由 90 个签名/91 个实际命中精确降至 88/89，2 条差值全部属于本族，SettingsConnectionPanel 一条几何遗留不变。`settings-connection-external` 三尺寸 3/3、完整 `pnpm verify` 的 113/113 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 56 个已跟踪修改、30 个未跟踪文件，均为 R02/U01C—U01AE 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未读取 Key、连接服务、获取模型或写入设置，未操作用户文件或远端。

## 四十九、U01AF 工作树归属

U01AF 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 56 个已跟踪修改和 30 个未跟踪文件，全部属于 R02/U01C—U01AE 的已验证未发布成果。`src/components/StatsApp.vue` 在本批前无 diff。

| 文件或范围 | U01AF 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/StatsApp.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label | 是，仅给动态刷新、内容统计折叠按钮增加与现有准确 title 同值的 aria-label | 不改刷新、折叠、统计读取/计算、聊天数据、布局或样式 |
| `scripts/unit/stats-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 StatsApp 全部 2 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AE 后 88 个签名/89 个实际命中 | 是，只删除 StatsApp 2 条 aria-label 精确签名 | 其他 87 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AE 有本轮修改 | 是，仅追加 U01AF 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、stats store/provider、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用隔离根页；不刷新或读取用户真实聊天 |

U01AF 不获得暂存、commit、push、发布、正式 build、真实聊天读取、统计刷新、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 89 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AF 完成反查：StatsApp 只给动态刷新、内容统计折叠 2 个按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 2 条失败，实现后 1/1 通过。机器 UI 基线由 88 个签名/89 个实际命中精确降至 86/87，2 条差值全部属于本族。`app:stats` 三尺寸 3/3、完整 `pnpm verify` 的 114/114 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 57 个已跟踪修改、31 个未跟踪文件，均为 R02/U01C—U01AF 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未刷新或读取真实聊天，未修改统计状态、用户文件或远端。

## 五十、U01AG 工作树归属

U01AG 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 57 个已跟踪修改和 31 个未跟踪文件，全部属于 R02/U01C—U01AF 的已验证未发布成果。`src/apps/storylines/StorylineEditorPage.vue` 在本批前无 diff。

| 文件或范围 | U01AG 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/storylines/StorylineEditorPage.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label | 是，仅给增加关联资料、移除关联按钮增加与现有准确 title 同值的 aria-label | 不改事件、剧情线/资料数据、保存/取消、布局或样式 |
| `scripts/unit/storylines-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 StorylineEditorPage 全部 2 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AF 后 86 个签名/87 个实际命中 | 是，只删除 StorylineEditorPage 2 条 aria-label 精确签名 | 其他 85 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AF 有本轮修改 | 是，仅追加 U01AG 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、storylines/profile store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存编辑草稿场景；不增加/移除或保存真实数据 |

U01AG 不获得暂存、commit、push、发布、正式 build、真实剧情线/资料修改、保存、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 87 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AG 完成反查：StorylineEditorPage 只给增加关联资料、移除关联 2 个按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 2 条失败，实现后 1/1 通过。机器 UI 基线由 86 个签名/87 个实际命中精确降至 84/85，2 条差值全部属于本族。`storylines-editor` 三尺寸 3/3、完整 `pnpm verify` 的 115/115 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 58 个已跟踪修改、32 个未跟踪文件，均为 R02/U01C—U01AG 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未增加/移除关联、保存或修改真实剧情线/资料，未操作用户文件或远端。

## 五十一、U01AH 工作树归属

U01AH 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 58 个已跟踪修改和 32 个未跟踪文件，全部属于 R02/U01C—U01AG 的已验证未发布成果。`src/components/theater/TheaterHistoryPage.vue` 在本批前无 diff。

| 文件或范围 | U01AH 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/theater/TheaterHistoryPage.vue` | 已跟踪、无 diff；机器基线登记 2 条 aria-label | 是，仅给动态排序、标签筛选按钮增加与现有准确 title 同值的 aria-label | 不改事件、类型标签/历史数据、布局或样式 |
| `scripts/unit/theater-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 TheaterHistoryPage 全部 2 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AG 后 84 个签名/85 个实际命中 | 是，只删除 TheaterHistoryPage 2 条 aria-label 精确签名 | 其他 83 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AG 有本轮修改 | 是，仅追加 U01AH 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、theater store/生成管线、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存历史场景；不修改真实小剧场数据或生成内容 |

U01AH 不获得暂存、commit、push、发布、正式 build、真实生成、小剧场数据修改、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 85 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AH 完成反查：TheaterHistoryPage 只给动态排序、标签筛选 2 个按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 2 条失败，实现后 1/1 通过。机器 UI 基线由 84 个签名/85 个实际命中精确降至 82/83，2 条差值全部属于本族。`theater-history` 三尺寸 3/3、完整 `pnpm verify` 的 116/116 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 59 个已跟踪修改、33 个未跟踪文件，均为 R02/U01C—U01AH 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未修改真实小剧场数据、生成内容、用户文件或远端。

## 五十二、U01AI 工作树归属

U01AI 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 59 个已跟踪修改和 33 个未跟踪文件，全部属于 R02/U01C—U01AH 的已验证未发布成果。`src/apps/tutorial/TutorialApp.vue` 与 `TutorialAppDirectory.vue` 在本批前均无 diff。

| 文件或范围 | U01AI 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/tutorial/TutorialApp.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给代码块复制按钮增加与现有准确 title 同值的 aria-label | 不改复制事件、剪贴板、教程内容、布局或样式 |
| `src/apps/tutorial/TutorialAppDirectory.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给动态打开 App 按钮增加与现有准确 title 同值的 aria-label | 不改路由、App 列表、打开事件、布局或样式 |
| `scripts/unit/tutorial-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描两文件全部 2 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AH 后 82 个签名/83 个实际命中 | 是，只删除两文件各 1 条 aria-label 精确签名 | 其他 81 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AH 有本轮修改 | 是，仅追加 U01AI 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、phone router、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用隔离教程场景；不操作真实剪贴板或打开用户 App |

U01AI 不获得暂存、commit、push、发布、正式 build、真实剪贴板写入、用户 App 路由切换、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 83 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AI 完成反查：TutorialApp 与 TutorialAppDirectory 只分别给复制、动态打开 App 各 1 个按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 1+1 条失败，实现后 1/1 通过。机器 UI 基线由 82 个签名/83 个实际命中精确降至 80/81，2 条差值全部属于本族，tutorial 三条几何遗留不变。两个教程场景三尺寸 6/6、完整 `pnpm verify` 的 117/117 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 61 个已跟踪修改、34 个未跟踪文件，均为 R02/U01C—U01AI 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未写入剪贴板、打开其他 App、修改教程内容、用户文件或远端。

## 五十三、U01AJ 工作树归属

U01AJ 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 61 个已跟踪修改和 34 个未跟踪文件，全部属于 R02/U01C—U01AI 的已验证未发布成果。`src/apps/chat-insert/ChatInsertApp.vue` 在本批前无 diff。

| 文件或范围 | U01AJ 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/chat-insert/ChatInsertApp.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给插入模板快捷按钮增加与现有准确动态 title 同值的 aria-label | 不改快捷模板、确认写入、聊天楼层/草稿、布局或样式 |
| `scripts/unit/chat-insert-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 ChatInsertApp 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AI 后 80 个签名/81 个实际命中 | 是，只删除 ChatInsertApp 1 条 aria-label 精确签名 | 其他 80 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AI 有本轮修改 | 是，仅追加 U01AJ 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、chat insert store/API、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用隔离根页；不写入真实聊天或持久化草稿 |

U01AJ 不获得暂存、commit、push、发布、正式 build、真实聊天楼层写入、草稿保存、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 81 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AJ 完成反查：ChatInsertApp 只给插入模板快捷按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 80 个签名/81 个实际命中精确降至 79/80，1 条差值属于本族，ChatInsertApp 一条几何遗留不变。`app:chat-insert` 三尺寸 3/3、完整 `pnpm verify` 的 118/118 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 62 个已跟踪修改、35 个未跟踪文件，均为 R02/U01C—U01AJ 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未打开快捷模板、写入聊天楼层、保存草稿、操作用户文件或远端。

## 五十四、U01AK 工作树归属

U01AK 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 62 个已跟踪修改和 35 个未跟踪文件，全部属于 R02/U01C—U01AJ 的已验证未发布成果。`src/components/FailedDraftList.vue` 在本批前无 diff。

| 文件或范围 | U01AK 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/FailedDraftList.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给删除失败草稿按钮增加与现有准确动态 title 同值的 aria-label | 不改打开/删除事件、失败草稿/原始输出、布局或样式 |
| `scripts/unit/failed-draft-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 FailedDraftList 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AJ 后 79 个签名/80 个实际命中 | 是，只删除 FailedDraftList 1 条 aria-label 精确签名 | 其他 79 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AJ 有本轮修改 | 是，仅追加 U01AK 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、生成失败草稿 store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用内存失败草稿场景；不删除真实草稿或原始输出 |

U01AK 不获得暂存、commit、push、发布、正式 build、真实失败草稿打开/删除、生成、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 80 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AK 完成反查：FailedDraftList 只给删除失败草稿按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 79 个签名/80 个实际命中精确降至 78/79，1 条差值属于本族。`theater-failed-draft` 三尺寸 3/3、完整 `pnpm verify` 的 119/119 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 63 个已跟踪修改、36 个未跟踪文件，均为 R02/U01C—U01AK 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未打开或删除真实失败草稿、修改原始输出、用户文件或远端。

## 五十五、U01AL 工作树归属

U01AL 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 63 个已跟踪修改和 36 个未跟踪文件，全部属于 R02/U01C—U01AK 的已验证未发布成果。`src/apps/file-repository/FileRepositoryApp.vue` 在本批前无 diff。

| 文件或范围 | U01AL 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/file-repository/FileRepositoryApp.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给刷新仓库清单按钮增加与现有准确 title 同值的 aria-label | 不改初始化/刷新事件、仓库清单、布局或样式 |
| `scripts/unit/file-repository-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 FileRepositoryApp 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AK 后 78 个签名/79 个实际命中 | 是，只删除 FileRepositoryApp 1 条 aria-label 精确签名 | 其他 78 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AK 有本轮修改 | 是，仅追加 U01AL 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、文件仓库 store/API、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用隔离根页；不点击刷新、快照、导入、恢复、删除或任何真实文件操作 |

U01AL 不获得暂存、commit、push、发布、正式 build、真实仓库文件扫描/快照/导入/恢复/删除、设置、用户文件或 API 调用授权。登记时 `pnpm ui:contracts` 为 79 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AL 完成反查：FileRepositoryApp 只给刷新仓库清单按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 78 个签名/79 个实际命中精确降至 77/78，1 条差值属于本族。`app:file-repository` 三尺寸 3/3、完整 `pnpm verify` 的 120/120 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 64 个已跟踪修改、37 个未跟踪文件，均为 R02/U01C—U01AL 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未点击刷新、扫描、快照、导入、恢复或删除，未操作真实仓库文件、用户文件、API 或远端。

## 五十六、U01AM 工作树归属

U01AM 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 64 个已跟踪修改和 37 个未跟踪文件，全部属于 R02/U01C—U01AL 的已验证未发布成果。`src/apps/media/MediaGenerateApp.vue` 在本批前无 diff。

| 文件或范围 | U01AM 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/media/MediaGenerateApp.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label，另有 1 条几何遗留 | 是，仅给 AI 填写参数按钮增加与现有准确动态 title 同值的 aria-label | 不改打开 Comfy 生成、参数、生成/失败草稿、布局、样式或几何遗留 |
| `scripts/unit/media-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 MediaGenerateApp 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AL 后 77 个签名/78 个实际命中 | 是，只删除 MediaGenerateApp 1 条 aria-label 精确签名 | 其他 77 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AL 有本轮修改 | 是，仅追加 U01AM 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、媒体/Comfy 生成 store/API、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用隔离根页；不点击 AI 填写参数、保存参数或发起真实生成 |

U01AM 不获得暂存、commit、push、发布、正式 build、真实媒体/Comfy 生成、API 请求、失败草稿操作、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 78 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AM 完成反查：MediaGenerateApp 只给 AI 填写参数按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 77 个签名/78 个实际命中精确降至 76/77，1 条差值属于本族，MediaGenerateApp 一条几何遗留不变。`app:media` 三尺寸 3/3、完整 `pnpm verify` 的 121/121 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 65 个已跟踪修改、38 个未跟踪文件，均为 R02/U01C—U01AM 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未点击 AI 填写参数、打开生成页、修改工作流参数、操作失败草稿或发起 API。

## 五十七、U01AN 工作树归属

U01AN 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 65 个已跟踪修改和 38 个未跟踪文件，全部属于 R02/U01C—U01AM 的已验证未发布成果。`src/apps/music/MusicApp.vue` 在本批前无 diff。

| 文件或范围 | U01AN 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/music/MusicApp.vue` | 已跟踪、无 diff；机器基线登记新增音乐按钮 1 条 aria-label，其他 4 个图标按钮已合格 | 是，仅给新增音乐按钮增加与现有准确动态 title 同值的 aria-label | 不改新增/下载/播放/编辑事件、音频数据、布局或样式 |
| `scripts/unit/music-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 MusicApp 全部 5 个图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AM 后 76 个签名/77 个实际命中 | 是，只删除 MusicApp 1 条 aria-label 精确签名 | 其他 76 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AM 有本轮修改 | 是，仅追加 U01AN 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、音频对象/下载、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用隔离根页；不点击新增、下载、播放、编辑或访问真实媒体 |

U01AN 不获得暂存、commit、push、发布、正式 build、真实音频播放/下载、媒体文件访问、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 77 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AN 完成反查：MusicApp 只给新增音乐按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过并覆盖文件内全部 5 个图标按钮。机器 UI 基线由 76 个签名/77 个实际命中精确降至 75/76，1 条差值属于本族。`app:music` 三尺寸 3/3、完整 `pnpm verify` 的 122/122 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 66 个已跟踪修改、39 个未跟踪文件，均为 R02/U01C—U01AN 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未点击新增、下载、播放或编辑，未访问真实音频或用户媒体。

## 五十八、U01AO 工作树归属

U01AO 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 66 个已跟踪修改和 39 个未跟踪文件，全部属于 R02/U01C—U01AN 的已验证未发布成果。`src/apps/preset-link/PresetLinkApp.vue` 在本批前无 diff。

| 文件或范围 | U01AO 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/preset-link/PresetLinkApp.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给刷新预设状态按钮增加与现有准确动态 title 同值的 aria-label | 不改刷新/选择/保存事件、当前聊天、预设或正则重载设置 |
| `scripts/unit/preset-link-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 PresetLinkApp 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AN 后 75 个签名/76 个实际命中 | 是，只删除 PresetLinkApp 1 条 aria-label 精确签名 | 其他 75 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AN 有本轮修改 | 是，仅追加 U01AO 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、聊天/预设/正则 store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用隔离根页；不刷新、选择、绑定或保存真实预设 |

U01AO 不获得暂存、commit、push、发布、正式 build、真实聊天/预设读取或修改、正则重载、设置、用户文件或 API 授权。登记时 `pnpm ui:contracts` 为 76 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AO 完成反查：PresetLinkApp 只给刷新预设状态按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 75 个签名/76 个实际命中精确降至 74/75，1 条差值属于本族。`app:preset-link` 三尺寸 3/3、完整 `pnpm verify` 的 123/123 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 67 个已跟踪修改、40 个未跟踪文件，均为 R02/U01C—U01AO 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未刷新、选择、绑定或保存真实预设，未操作聊天、正则或用户文件。

## 五十九、U01AP 工作树归属

U01AP 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 67 个已跟踪修改和 40 个未跟踪文件，全部属于 R02/U01C—U01AO 的已验证未发布成果。`src/components/ProfileEntryPicker.vue` 在本批前无 diff。

| 文件或范围 | U01AP 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/ProfileEntryPicker.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给查看资料按钮增加与现有准确动态 title 同值的 aria-label | 不改选择/查看 emit、禁用条件、资料状态、布局或样式 |
| `scripts/unit/profile-picker-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 ProfileEntryPicker 唯一图标按钮并断言动态名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AO 后 74 个签名/75 个实际命中 | 是，只删除 ProfileEntryPicker 1 条 aria-label 精确签名 | 其他 74 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AO 有本轮修改 | 是，仅追加 U01AP 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、资料 store/消费者、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用 storylines-editor 内存 fixture；不打开或修改真实资料 |

U01AP 不获得暂存、commit、push、发布、正式 build、真实资料读取/打开/修改、剧情线保存、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 75 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AP 完成反查：ProfileEntryPicker 只给查看资料按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 74 个签名/75 个实际命中精确降至 73/74，1 条差值属于本族。`storylines-editor` 三尺寸 3/3、完整 `pnpm verify` 的 124/124 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 68 个已跟踪修改、41 个未跟踪文件，均为 R02/U01C—U01AP 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未点击查看、打开或修改真实资料，未保存真实剧情线。

## 六十、U01AQ 工作树归属

U01AQ 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 68 个已跟踪修改和 41 个未跟踪文件，全部属于 R02/U01C—U01AP 的已验证未发布成果。`src/components/prompts/PromptTypeEditorPage.vue` 在本批前无 diff。

| 文件或范围 | U01AQ 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/prompts/PromptTypeEditorPage.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给新建分组按钮增加与现有准确 title 同值的 aria-label | 不改创建分组/保存事件、类型/分组数据、布局或样式 |
| `scripts/unit/prompts-shared-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 PromptTypeEditorPage 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AP 后 73 个签名/74 个实际命中 | 是，只删除 PromptTypeEditorPage 1 条 aria-label 精确签名 | 其他 73 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AP 有本轮修改 | 是，仅追加 U01AQ 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、提示词类型/分组 store、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用 prompts-type-editor 内存 fixture；不创建或保存真实分组 |

U01AQ 不获得暂存、commit、push、发布、正式 build、真实提示词类型/分组创建或保存、设置、用户文件或 API 授权。登记时 `pnpm ui:contracts` 为 74 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AQ 完成反查：PromptTypeEditorPage 只给新建分组按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 73 个签名/74 个实际命中精确降至 72/73，1 条差值属于本族。`prompts-type-editor` 三尺寸 3/3、完整 `pnpm verify` 的 125/125 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 69 个已跟踪修改、42 个未跟踪文件，均为 R02/U01C—U01AQ 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未新建或保存真实提示词分组，未操作用户配置。

## 六十一、U01AR 工作树归属

U01AR 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 69 个已跟踪修改和 42 个未跟踪文件，全部属于 R02/U01C—U01AQ 的已验证未发布成果。`src/apps/scene-planner/ScenePlannerApp.vue` 在本批前无 diff。

| 文件或范围 | U01AR 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/scene-planner/ScenePlannerApp.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给新方案按钮增加与现有准确 title 同值的 aria-label | 不改新建/生成/保存/删除/失败草稿事件、场景方案数据、布局或样式 |
| `scripts/unit/scene-planner-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 ScenePlannerApp 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AQ 后 72 个签名/73 个实际命中 | 是，只删除 ScenePlannerApp 1 条 aria-label 精确签名 | 其他 72 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AQ 有本轮修改 | 是，仅追加 U01AR 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、场景方案/生成 store/API、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用 app:scene-planner 内存 fixture；不新建、生成、保存或删除真实方案 |

U01AR 不获得暂存、commit、push、发布、正式 build、真实场景方案新建/生成/保存/删除、失败草稿操作、API、设置或用户文件授权。登记时 `pnpm ui:contracts` 为 73 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AR 完成反查：ScenePlannerApp 只给新方案按钮增加与现有准确 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 72 个签名/73 个实际命中精确降至 71/72，1 条差值属于本族。`app:scene-planner` 三尺寸 3/3、完整 `pnpm verify` 的 126/126 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 70 个已跟踪修改、43 个未跟踪文件，均为 R02/U01C—U01AR 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未新建、生成、保存或删除真实场景方案，未操作失败草稿或 API。

## 六十二、U01AS 工作树归属

U01AS 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 70 个已跟踪修改和 43 个未跟踪文件，全部属于 R02/U01C—U01AR 的已验证未发布成果。`src/components/SearchableCombobox.vue` 在本批前无 diff。

| 文件或范围 | U01AS 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/SearchableCombobox.vue` | 已跟踪、无 diff；机器基线登记 1 条 aria-label | 是，仅给下拉切换按钮增加与现有准确动态 title 同值的 aria-label | 不改展开/筛选/键盘/选择行为、ARIA 关系、布局或样式 |
| `scripts/unit/searchable-combobox-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 SearchableCombobox 唯一图标按钮并断言动态名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AR 后 71 个签名/72 个实际命中 | 是，只删除 SearchableCombobox 1 条 aria-label 精确签名 | 其他 71 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AR 有本轮修改 | 是，仅追加 U01AS 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、设置/连接 store/API、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用 settings-connection-external 内存 fixture；不展开下拉、修改设置或连接 API |

U01AS 不获得暂存、commit、push、发布、正式 build、真实设置修改、预设读取、连接/API、用户文件或任何消费者行为修改授权。登记时 `pnpm ui:contracts` 为 72 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AS 完成反查：SearchableCombobox 只给下拉切换按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 71 个签名/72 个实际命中精确降至 70/71，1 条差值属于本族。`settings-connection-external` 三尺寸 3/3、完整 `pnpm verify` 的 127/127 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 71 个已跟踪修改、44 个未跟踪文件，均为 R02/U01C—U01AS 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未展开下拉、修改真实设置、读取预设或连接 API。

## 六十三、U01AT 工作树归属

U01AT 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 71 个已跟踪修改和 44 个未跟踪文件，全部属于 R02/U01C—U01AS 的已验证未发布成果。`src/apps/world-slots/WorldSlotsApp.vue` 在本批前无 diff。

| 文件或范围 | U01AT 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/world-slots/WorldSlotsApp.vue` | 已跟踪、无 diff；机器基线登记最后 1 条 aria-label | 是，仅给新增槽位按钮增加与现有准确动态 title 同值的 aria-label | 不改新增/编辑/保存/启停事件、世界书槽位/条目数据、布局或样式 |
| `scripts/unit/world-slots-icon-button-contract.test.mjs` | 不存在 | 是，允许新建 | 扫描 WorldSlotsApp 唯一图标按钮并断言名称同值、aria 缺失归零 |
| `scripts/baselines/ui-contracts.json` | U01AS 后 70 个签名/71 个实际命中 | 是，只删除 WorldSlotsApp 最后 1 条 aria-label 精确签名 | 其他 70 个实际命中必须不变 |
| `docs/execution/` | R02/U01C—U01AS 有本轮修改 | 是，仅追加 U01AT 契约、失败证据和验证结果 | 不覆盖已完成批次记录 |
| 既有本轮源码/测试、visual fixture、世界书/槽位 store/API、正式 `dist`、Git index | 有已验证前批修改或无 diff | 否 | 复用 app:world-slots 内存 fixture；不新增、编辑、保存或启停真实槽位 |

U01AT 不获得暂存、commit、push、发布、正式 build、真实世界书/槽位读取或修改、设置、用户文件或 API 授权。登记时 `pnpm ui:contracts` 为 71 个已登记遗留、0 新增；正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

U01AT 完成反查：WorldSlotsApp 只给新增槽位按钮增加与现有准确动态 title 同值的 aria-label；定向测试先精确报告 1 条失败，实现后 1/1 通过。机器 UI 基线由 70 个签名/71 个实际命中精确降至 69/70，最后 1 条 aria-label 差值属于本族，R02 起点的 159 条 aria-label 遗留全部清零。`app:world-slots` 三尺寸 3/3、完整 `pnpm verify` 的 128/128 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 72 个已跟踪修改、45 个未跟踪文件，均为 R02/U01C—U01AT 已登记成果；Git index 仍为 0，正式 dist 哈希不变，未新增、编辑、保存或启停真实槽位，未操作用户文件或 API。

## 六十四、DISC-U02 只读发现归属

DISC-U02 开始时 U01 已完成，`HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；正式 dist 哈希不变。工作区 72 个已跟踪修改和 45 个未跟踪文件均属于 R02/U01C—U01AT 已登记且验证的未发布成果。

| 文件或范围 | DISC-U02 权限 | 保护说明 |
| --- | --- | --- |
| `src/global.css`、`.pc-segment` / `.pc-segment-btn` 消费者、局部 scoped 样式 | 只读 | 只统计权威规则、消费者与局部偏差，不修改源码或样式 |
| `src/testing/`、视觉基线、UI 契约基线、现有报告 | 只读 | 只确认可隔离场景和既有证据，不补场景、不写基线 |
| `docs/execution/` | 可写 | 只登记发现、D 编号、根因拆分和候选批次边界 |
| 用户数据、聊天、备份、设置、预设、世界书、API、正式 `dist`、Git index/远端 | 禁止 | 发现门不产生任何真实数据或发布影响 |

DISC-U02 未授权实现标签页调整；即使发现全局规则偏差，也必须先完成消费者清单、三尺寸复现、单一根因和验证设计，再决定能否自动登记首个 U02 实施批次。

DISC-U02 完成反查：只读确认全局 `.pc-segment-btn` 被普通文字按钮的 36px 规则误绑，而权威规范为 32px/13px；38 个 Vue 消费者已列出。UI 机器基线另有 7 个页面、8 个局部 segment 几何签名，属于不同根因。六个标准/特殊场景修改前三尺寸证据 18/18 已保存到临时目录；未修改源码、样式、测试、基线、正式 dist 或 Git index，未操作真实数据。

## 六十五、U02A 工作树归属

U02A 登记时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 72 个已跟踪修改和 45 个未跟踪文件，均属于 R02/U01C—U01AT 与 DISC-U02 的已登记成果。`src/global.css` 在 U02A 前无本轮 diff。

| 文件或范围 | U02A 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/global.css` | 已跟踪、无本轮 diff；segment-btn 与普通按钮共用 36px/13px | 是，仅拆分该声明，使普通按钮保持 36px，segment-btn 为 32px/13px | 不改颜色、圆角、间距、active 状态、其他控件或主题变量 |
| `scripts/unit/segment-control-contract.test.mjs` | 不存在 | 是，允许新建 | 精确断言普通按钮 36px、segment-btn 32px/13px，防止再次合并回退 |
| `docs/execution/` | R02/U01/DISC-U02 有本轮修改 | 是，仅追加 U02A 失败证据和验证结果 | 不覆盖既有记录 |
| 7 个局部覆盖页面、UI/视觉基线与 fixture、业务源码/数据、正式 `dist`、Git index | 有既有遗留或前批成果 | 否 | 使用 DISC-U02 已保存的六场景证据；局部覆盖留给后续独立批次 |

U02A 不获得局部标签页覆盖修改、按钮/字段/弹窗调整、真实数据操作、正式 build、暂存、commit、push 或发布授权。登记时机器遗留为 69 个签名/70 个实际命中（几何 67、主题色 3），正式 dist 哈希不变。

U02A 完成反查：`src/global.css` 只把 segment-btn 从普通文字按钮的 36px 组合声明中分离为 32px/13px，普通按钮保持 36px；定向测试由精确失败转为 1/1。六个标准/特殊场景修改后三尺寸 18/18、完整 `pnpm verify` 的 129/129 单测、36/36 UI、15/15 外观证据均通过。完成时工作区为 73 个已跟踪修改、46 个未跟踪文件，均为已登记成果；机器遗留仍为 69 个签名/70 个实际命中，Git index 为 0，正式 dist 哈希不变，未操作真实数据。

## 六十六、DISC-U02B 只读局部语义拆分归属

DISC-U02B 开始时 `HEAD` 与 `origin/main` 均为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区 73 个已跟踪修改、46 个未跟踪文件均为 R02/U01/DISC-U02/U02A 的已登记未发布成果。正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

| 文件或范围 | DISC-U02B 权限 | 保护说明 |
| --- | --- | --- |
| Comfy、Profiles、RegexWizard、Tutorial、WorldbookLink、Prompts、Settings 的模板与 scoped 样式 | 只读 | 只核对控件语义、覆盖属性和真实消费者，不修改任何业务源码或样式 |
| `scripts/baselines/ui-contracts.json`、视觉场景与 fixture | 只读 | 只读取 8 条现有签名并执行既有场景；不更新基线、不新增场景 |
| `tmp/ui-check/disc-u02-local` | 允许临时生成 | 七场景三尺寸 21/21 只读视觉证据；不是正式产物，不进入 Git 候选 |
| `docs/execution/` | 可写 | 只追加 DISC-U02B 契约、D-UI-TABS-003—008 和强制停止记录 |
| 正式 `dist`、Git index/远端、用户数据、聊天、备份、设置、预设、世界书、API | 禁止 | 不构建正式产物，不暂存、提交、推送，不读取或修改真实数据 |

DISC-U02B 完成反查：七场景三尺寸 21/21 通过，但 `app:comfy` 没有实际露出目标参数模式控件；其余证据确认原 8 条局部签名至少分属标准字号残留、合法宽度布局、五列窄屏取舍、紧凑行内模式、分类筛选和变体选项卡片六类根因。已仅在执行文档拆为 D-UI-TABS-003—008；工作区计数、机器基线、Git index 和正式 dist 均未变化，未操作真实数据。由于发现不同共同根因并出现产品语义选择，按协议停止，不获得任何实施权限。

## 六十七、U02B 工作树归属

用户于 2026-08-14 确认推荐语义后解除停止。U02B 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区 73 个已跟踪修改、46 个未跟踪文件均为 R02/U01/DISC-U02/U02A/DISC-U02B 的已登记未发布成果。正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

| 文件或范围 | U02B 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/regex-wizard/RegexWizardApp.vue` | 已含 U01L 的 6 个 aria-label 修改；目标规则另有 12px 字号覆盖 | 是，仅删除 `.pc-regex-wizard-purpose .pc-segment-btn` 的 `font-size: 12px` | 必须保留 U01L 修改、三列 grid、`min-width`、`padding-inline`、其他模板/样式/行为 |
| `scripts/unit/regex-wizard-segment-density-contract.test.mjs` | 不存在 | 是，新建定向失败契约 | 只约束目标规则不得覆盖字号/高度，并复核全局 32px/13px |
| `scripts/baselines/ui-contracts.json` | 69 个签名/70 个实际命中 | 是，只删除目标选择器 1 条精确几何签名 | 同文件 `.pc-regex-wizard-test .pc-area` 和其他全部遗留保持不变 |
| `docs/execution/` | 已有 DISC-U02B 停止记录 | 是，只追加用户确认、U02B 契约与证据 | 不覆盖历史停止事实；D-UI-TABS-004—008 仍不在本批 |
| `tmp/ui-check/u02b` | 不存在 | 允许临时生成 | 仅 `regex-wizard-fields` 三尺寸证据，不进入 Git 候选 |
| 正式 `dist`、Git index/远端、真实数据/API | 禁止 | 否 | 不正式构建、不暂存/提交/推送，不读取或修改聊天、备份、设置、预设、世界书或用户文件 |

U02B 不获得布局、文案、fixture、其他局部标签页语义、真实数据操作、正式 build、暂存、commit、push 或发布授权。若 13px 造成换行/裁切，或修复必须触及三列布局及其他选择器，立即停止。

U02B 停止反查：新增定向契约在旧代码上精确报告目标 `font-size: 12px`，删除该行后与全局分段契约 2/2 通过；未改变模板、事件、三列 grid、`min-width`、`padding-inline` 或 U01L 的 6 个 aria-label。机器守卫按选择器聚合命中，保留的 `padding-inline: 4px` 使 `.pc-regex-wizard-purpose .pc-segment-btn` 签名无法直接删除；试删基线后只出现该签名 `current=1, baseline=0`，现已恢复，`pnpm ui:contracts` 和严格 UI 复用检查均通过（70 个实际命中、0 新增）。未运行三尺寸与完整验证，未操作正式 dist、Git index 或真实数据。继续所需的最小契约扩充是：允许在目标规则前增加 `ui-reuse-allow: D-UI-TABS-003`，明确豁免合法三列布局，再删除该旧签名；未获确认前不执行。

U02B 恢复归属：用户要求先按 07 文档复核，确认选择器级例外会整体跳过守卫，而第 10.6 节只允许标准标签页的业务宽度布局；随后用户授权严格方案。当前允许在既有目标规则中继续删除 `padding-inline: 4px`、扩充同一新定向测试并自然删除同一机器签名；`min-width: 0`、三列 grid、模板、事件和其他样式仍受保护，禁止增加 `ui-reuse-allow`。此前建议的编号例外已替代但作为停止历史保留。

U02B 完成反查：目标局部规则最终只删除 `font-size: 12px` 与 `padding-inline: 4px`，保留 `min-width: 0` 和三列 grid；没有增加例外注释，也没有改动模板、事件、其他样式或 U01L 的 6 个 aria-label。两段 T0 均精确失败，修复后两项定向契约 2/2；机器遗留由 69 个签名/70 个实际命中精确降至 68/69，只有目标签名消失。`regex-wizard-fields` 三尺寸 3/3，完整 `pnpm verify` 的 130/130 单测、36/36 UI、15/15 外观证据和临时构建全部通过。完成时工作区为 73 个已跟踪修改、47 个未跟踪文件，均为已登记未发布成果；Git index 为 0，`HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，正式 dist 哈希仍为 `index.js=A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`index.css=BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。未操作真实聊天、备份、设置、预设、世界书、用户文件或 API。

## 六十八、U02C 工作树归属

U02C 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区 73 个已跟踪修改、47 个未跟踪文件均为截至 U02B 的已登记未发布成果。机器遗留为 68 个签名/69 个实际命中，正式 dist 哈希不变。

| 文件或范围 | U02C 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/profiles/pages/ProfilesCatalogPage.vue` | 已含 U01K 的 3 个 aria-label；目标规则为 40px 双图标宽度与 0 内边距 | 是，仅在目标选择器前增加 D-UI-TABS-008 编号原因注释 | 不改声明值、模板、事件或其他样式 |
| `src/apps/worldbook-link/pages/WorldbookCatalogPage.vue` | 已含 U01T 的 1 个 aria-label；目标规则为五列最小宽度与 6px 内边距 | 是，仅在目标选择器前增加 D-UI-TABS-008 编号原因注释 | 不改声明值、模板、事件或其他样式 |
| `scripts/unit/segment-layout-exceptions-contract.test.mjs` | 不存在 | 是，新建定向失败契约 | 精确锁定两个例外原因、允许声明和全局 32px/13px；禁止例外扩张 |
| `scripts/baselines/ui-contracts.json` | 68 个签名/69 个实际命中 | 是，只删除两个目标选择器的精确几何签名 | 其他 66 个签名/67 个实际命中必须不变 |
| `tmp/ui-check/u02c` | 不存在 | 允许临时生成 | 两场景三尺寸 6/6，不进入 Git 候选 |
| `docs/execution/` | U02B 已完成 | 是，只追加 U02C 契约与证据 | 不覆盖历史记录 |
| 正式 `dist`、Git index/远端、真实资料/世界书/用户数据/API | 禁止 | 否 | 不正式构建，不暂存/提交/推送，不读取或修改真实数据 |

U02C 不获得修改计算样式、业务行为、fixture、其他标签页根因、真实数据、正式 build、暂存、commit、push 或发布授权。若注释不能只豁免现有特殊布局，或定向测试无法阻止字号/高度等基础密度进入例外，立即停止。

U02C 完成反查：Profiles 与 Worldbook 两个目标规则只增加带 D-UI-TABS-008 和业务原因的注释，现有 width/min-width/padding 声明、模板、事件、其他样式及 U01 aria-label 均未改变。T0 精确报告两个缺失原因，修复后定向/守卫 10/10；机器遗留由 68 个签名/69 个实际命中精确降至 66/67，只有两个目标签名消失。两场景三尺寸 6/6，完整 `pnpm verify` 的 131/131 单测、36/36 UI、15/15 外观证据和临时构建全部通过。完成时工作区为 73 个已跟踪修改、48 个未跟踪文件；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希均不变，未读取或修改真实资料、世界书或其他用户数据。

## 六十九、U02D 只读预检归属（触发停止）

U02D 预检开始时 U02C 已验证完成，工作区仍为 73 个已跟踪修改、48 个未跟踪文件，机器遗留为 66 个签名/67 个实际命中；`src/components/SettingsApp.vue` 在本轮无 diff。只读检查确认该文件同一目标选择器有常规 12px 与 ≤380px 11px 两套字号，和 07 文档 13px/禁止 viewport 缩放/禁止普通标签页例外三条硬规则冲突。

本预检只允许读取 Settings 源码、机器基线、现有场景和权威文档，并更新执行记录；没有建立实施契约，也没有修改 `SettingsApp.vue`、测试、基线、fixture、正式 dist、Git index 或真实数据。继续所需的唯一决定是：是否让 07 文档硬规则覆盖此前“保留窄屏字号例外”的旧语义，改为全宽度继承 13px 后以三尺寸验证。

## 七十、U02D 工作树归属

用户确认由 07 文档硬规则覆盖旧语义后解除停止。U02D 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区 73 个已跟踪修改、48 个未跟踪文件均为截至 U02C 的已登记成果。机器遗留为 66 个签名/67 个实际命中，正式 dist 哈希不变。

| 文件或范围 | U02D 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/SettingsApp.vue` | 本轮无 diff；目标选择器常规/媒体规则共有 5 个基础密度声明 | 是，仅删除契约列明的高度、padding、12px/11px | 保留五列容器、宽度收缩、gap、单行、模板、事件和面板行为 |
| `scripts/unit/settings-segment-density-contract.test.mjs` | 不存在 | 是，新建定向失败契约 | 锁定局部无高度/padding/字号和全局 32px/13px |
| `scripts/baselines/ui-contracts.json` | 66 个签名/67 个实际命中；目标签名 count=2 | 是，只删除 Settings 目标签名 | 其他 65 个签名/65 个实际命中必须不变 |
| `tmp/ui-check/u02d` | 不存在 | 允许临时生成 | 设置场景三尺寸 3/3，不进入 Git 候选 |
| `docs/execution/` | 已有预检停止记录 | 是，只追加用户决定、契约与证据 | 保留历史停止事实 |
| 正式 `dist`、Git index/远端、真实设置/API/用户数据 | 禁止 | 否 | 不正式构建、不暂存/提交/推送，不读取或修改真实数据 |

U02D 不获得隐藏图标、横向滚动、改文案、修改设置行为/fixture、编号例外、真实数据、正式 build、暂存、commit、push 或发布授权。若 13px 在 350px 无法维持五列单行，立即停止并报告，不以缩小字号兜底。

U02D 完成反查：Settings 目标常规/媒体规则只删除契约列明的 5 个基础密度声明，保留五列容器、宽度收缩、gap、图标、文案、单行、模板和事件；没有增加编号例外。T0 精确报告 5 项，修复后定向/守卫 10/10；机器遗留由 66 个签名/67 个实际命中精确降至 65/65，只有目标签名的 2 个命中消失。设置场景三尺寸 3/3，完整 `pnpm verify` 的 132/132 单测、36/36 UI、15/15 外观证据和临时构建全部通过。完成时工作区为 74 个已跟踪修改、49 个未跟踪文件；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希均不变，未读取、保存或连接真实设置/API。

## 七十一、U02E 工作树归属

U02E 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 74 个已跟踪修改和 49 个未跟踪文件，均为截至 U02D 的已登记未发布成果。机器遗留为 65 个签名/65 个实际命中，正式 dist 哈希不变。

| 文件或范围 | U02E 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/comfy/ComfyApp.vue` | 已含 U01AB 两项 aria-label；参数模式局部规则为 30px/11px 并重画外观；既有 420px 参数窄屏规则错误使用 browser viewport | 是，给目标容器/按钮接入共享紧凑分段语义、删除两段局部基础/激活态规则；若 350px T1 不单行，只把该既有 `@media` 改为 `.pc-comfy-app` 已支持的 `@container` | 保留 U01AB、参数文案/顺序/事件、grid 宽度和其他 Comfy 模板/样式/行为；禁止缩小 12px 字号 |
| `src/global.css` | 已含 U02A 的标准分段 32px/13px；无 compact 分段修饰符 | 是，仅增加显式 `.pc-segment-btn.compact` 的 30px/12px/紧凑水平 padding | 不改变普通分段及其他按钮计算样式 |
| `src/testing/visual/scenarioCatalog.ts`、`src/testing/visual-harness.ts` | 已有其他已登记场景 diff；Comfy 只有动作菜单场景 | 是，仅登记并实现 `comfy-parameter-modes` 的隔离内存 fixture | 不覆盖既有场景，不连接 API，不触发模式点击或真实持久化 |
| `scripts/unit/comfy-parameter-mode-contract.test.mjs` | 不存在 | 是，新建定向失败契约 | 锁定共享 compact 30px/12px、模板语义和局部重画归零 |
| `scripts/baselines/ui-contracts.json` | 65 个签名/65 个实际命中；Comfy 目标签名 1 条 | 是，只删除该精确签名 | 其他 64 个签名/64 个实际命中必须不变 |
| `scripts/baselines/ui-visual.json` | 已含前批新增场景，正式清单为 207 场景/621 运行 | 是，只增加 `comfy-parameter-modes` 三尺寸零问题记录并改为 208/624 | 不改其他场景的 findings、顺序或尺寸 |
| `tmp/ui-check/u02e-before`、`tmp/ui-check/u02e-after` | 不存在 | 允许临时生成 | 单场景修改前后各三尺寸，不进入 Git 候选 |
| `docs/execution/` | U02D 已完成 | 是，只追加 U02E 契约、证据和结果 | 不覆盖历史记录 |
| 正式 `dist`、Git index/远端、真实 Comfy/设置/用户数据/API | 禁止 | 否 | 不正式构建、不暂存/提交/推送，不读取或修改真实数据 |

U02E 不获得修改工作流/生成行为、参数 mapping、真实配置、API、其他标签页根因、正式 build、暂存、commit、push 或发布授权。fixture 若不能只凭测试内存 JSON 稳定露出目标，或共享 compact 修饰符改变未显式使用者，立即停止。

U02E 完成反查：Comfy 参数模式只增加共享 `pc-segment`/`compact` 标记，删除局部 11px、边框、背景和激活态重画，并把同一参数窄屏规则由无效的 browser `@media` 改为已有容器的 `@container`；U01AB 的两个 aria-label、三态文案/顺序/事件、mapping、生成行为和其他样式均保留。新隔离场景只使用测试内存工作流并在测试 DOM 隐藏前置区块，不点击三态、不连接 API。两段 T0 均精确失败，最终定向/回归 3/3；机器遗留由 65/65 精确降至 64/64，正式视觉遗留清单由 207/621 增至 208/624，目标修改前后各三尺寸且正式基线 3/3。完整 `pnpm verify` 的 133/133 单测、36/36 UI、15/15 外观证据和临时构建全部通过。完成时工作区为 74 个已跟踪修改、50 个未跟踪文件，均为已登记未发布成果；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希不变，未读取或修改真实 Comfy、设置或用户数据。

## 七十二、U02F 工作树归属

U02F 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 74 个已跟踪修改和 50 个未跟踪文件，均为截至 U02E 的已登记未发布成果。机器遗留为 64 个签名/64 个实际命中，正式视觉清单为 208 场景/624 运行，正式 dist 哈希不变。

| 文件或范围 | U02F 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/tutorial/TutorialApp.vue` | 已含 U01AI 的复制按钮 aria-label；目标为 8 项四列筛选，却使用 `<nav>`/`.pc-segment-btn` 并局部重画 | 是，仅修正目标容器/按钮语义并删除该目标基础重写，保留自有四列和宽度布局 | 不改 U01AI、搜索、分类数据/顺序、点击筛选、文章与其他样式/遗留 |
| `scripts/unit/tutorial-category-filter-contract.test.mjs` | 不存在 | 是，新建定向失败契约 | 锁定 group/aria-pressed/共享 compact 软按钮和局部只剩 width |
| `scripts/baselines/ui-contracts.json` | 64 个签名/64 个实际命中；教程目标签名 1 条 | 是，只删除该精确签名 | 其他 63 个签名/63 个实际命中必须不变 |
| `tmp/ui-check/u02f-before`、`tmp/ui-check/u02f-after` | 不存在 | 允许临时生成 | 既有 `app:tutorial` 修改前后三尺寸，不进入 Git 候选 |
| `docs/execution/` | U02E 已完成 | 是，只追加 U02F 契约、证据和结果 | 不覆盖历史记录 |
| 全局样式、fixture、正式 `dist`、Git index/远端、真实设置/用户数据/API | 禁止 | 否 | 不新增共享控件、不改场景、不正式构建、不暂存/提交/推送、不读取或修改真实数据 |

U02F 不获得修改分类行为/数据、搜索、文章内容、全局按钮、其他教程遗留、正式 build、暂存、commit、push 或发布授权。若四列在 350px 无法维持单行短标签，立即停止，不以缩小字号兜底。

U02F 完成反查：TutorialApp 目标容器只由导航改为同页筛选 group，8 个按钮只切换为共享 compact 软按钮并增加动态 `aria-pressed`，局部样式只剩自有 `width: 100%`；分类数据/顺序、点击逻辑、搜索、文章、其他样式及 U01AI 的复制 aria-label 均保留。T0 精确报告 7 项，最终定向/回归 4/4；机器遗留由 64/64 精确降至 63/63。`app:tutorial` 修改前后三尺寸各 3/3，350px 四列两行全部单行。完整 `pnpm verify` 的 134/134 单测、36/36 UI、15/15 外观证据和临时构建全部通过。完成时工作区为 74 个已跟踪修改、51 个未跟踪文件，均为已登记未发布成果；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希不变，未读取或修改真实设置、教程数据或用户资料。

## 七十三、U02G 工作树归属

U02G 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 74 个已跟踪修改和 51 个未跟踪文件，均为截至 U02F 的已登记未发布成果。机器遗留为 63 个签名/63 个实际命中，正式视觉清单为 208 场景/624 运行，正式 dist 哈希不变。

| 文件或范围 | U02G 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/components/PromptsApp.vue` | 已含 U01H/U01AQ 等既有本轮修改；目标使用分段类并局部重写 6 项几何/排版 | 是，仅修正动态变体容器/按钮语义并把目标局部规则缩为自有两列可换行布局 | 保留全部既有修改、注册/选择/编辑逻辑、弹窗正文与其他样式/遗留 |
| `scripts/unit/prompts-variant-option-contract.test.mjs` | 不存在 | 是，新建定向失败契约 | 锁定 group/aria-pressed/共享软按钮和自有布局允许声明 |
| `scripts/baselines/ui-contracts.json` | 63 个签名/63 个实际命中；Prompts 目标签名 1 条 | 是，只删除该精确签名 | 其他 62 个签名/62 个实际命中必须不变 |
| `tmp/ui-check/u02g-before`、`tmp/ui-check/u02g-after` | 不存在 | 允许临时生成 | 既有 `prompts-app-detail` 修改前后三尺寸，不进入 Git 候选 |
| `docs/execution/` | U02F 已完成 | 是，只追加 U02G 契约、证据和结果 | 不覆盖历史记录 |
| 全局样式、fixture、正式 `dist`、Git index/远端、真实提示词/设置/用户数据/API | 禁止 | 否 | 不改共享类、不改场景、不正式构建、不暂存/提交/推送、不读取或修改真实数据 |

U02G 不获得修改动态注册协议、选项名称/数量/顺序、选择或编辑行为、其他 Prompts 遗留、全局按钮、正式 build、暂存、commit、push 或发布授权。若 350px 长标题无法在普通字号下自然扩高，立即停止，不以缩小字号或截断兜底。

U02G 完成反查：PromptsApp 目标容器只增加 labelled group 语义，动态按钮只切换为共享 `pc-soft-btn` 并增加 `aria-pressed`，局部规则只剩宽度、收缩与自然换行；注册协议、选项名称/数量/顺序、选择/编辑行为、弹窗正文和其他样式均保留。T0 精确报告 6 项，定向及前批回归 5/5；机器遗留由 63/63 精确降至 62/62。`prompts-app-detail` 修改前后三尺寸各 3/3，350px 无裁切/覆盖。完整 `pnpm verify` 的 135/135 单测、36/36 UI、15/15 外观证据和临时构建全部通过。完成时工作区为 75 个已跟踪修改、52 个未跟踪文件，均为已登记未发布成果；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希不变，未读取或修改真实提示词、设置或用户数据。

## 七十四、DISC-U03 工作树归属

DISC-U03 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 75 个已跟踪修改和 52 个未跟踪文件，均为截至 U02G 的已登记未发布成果。机器遗留为 62 个签名/62 个实际命中，正式视觉清单为 208 场景/624 运行，正式 dist 哈希不变。

| 文件或范围 | DISC-U03 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/global.css`、字段/输入消费者、局部 scoped style | 已含此前登记修改 | 否，只读 | 只统计权威规则、消费者和局部基础覆盖，不改任何声明 |
| `src/testing/visual*`、`scripts/baselines/*`、`scripts/unit/*` | 已含此前登记修改/未跟踪测试 | 否，只读 | 只映射现有场景和机器签名，不补 fixture、测试或基线 |
| `docs/execution/` | 已含截至 U02G 的执行记录 | 是，仅登记 DISC-U03 发现事实、D 编号、追踪和停止/继续结论 | 不覆盖历史记录 |
| 正式 `dist`、Git index/远端、真实设置/资料/聊天/API | 禁止 | 否 | 不正式构建、不暂存/提交/推送、不读取或修改真实数据 |

DISC-U03 不获得字段整改、全局样式调整、消费者迁移、动态下拉替换、新场景/测试、机器基线删改、正式 build、暂存、commit、push 或发布授权。发现多个共同根因、产品语义冲突或现有场景不能安全隔离时必须停止，不以局部覆盖或假数据兜底。

DISC-U03 完成反查：仅执行源码/全局样式/机器基线/现有场景只读检查和执行文档登记。确认五类共享字段相关消费者为 87/60/23/52/44 个源文件；29 条局部机器签名拆为 4 个搜索字段、22 个多语义文本域、2 个特殊数字字段和 1 个重复标签规则。八场景三尺寸 24/24，四场景计算样式证明共享控件当前为 16px，字段/下拉为 43/45px且没有显式最低高度。未修改源码、样式、测试、场景或基线；工作区计数、Git index、`HEAD`/`origin/main`、正式 dist 和真实数据均未变化。

## 七十五、U03A 与 DISC-U03B 工作树归属

U03A 仅在此前已修改的 `src/global.css` 增加全局字段字号/最低高度，在此前干净的 `scripts/baselines/ui-appearance.json` 精确更新六条预期截图哈希，并新增未跟踪 `scripts/unit/field-control-density-contract.test.mjs`；执行文档按契约更新。U03A 完成时工作区为 76 个已跟踪修改、53 个未跟踪文件，Git index 为 0；机器遗留仍为 62/62，`HEAD`/`origin/main` 和正式 dist 哈希不变。

| 文件或范围 | DISC-U03B 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| 29 条局部字段签名所在源码 | 已含此前登记修改或原始遗留 | 否，只读 | 逐条核对模板、声明与语义，不改消费者 |
| `src/global.css`、共享搜索/编辑组件 | 已含 U02/U03A 修改 | 否，只读 | 只作为权威参考，不继续改全局规则 |
| 测试、机器/视觉/外观基线、fixture、场景 | 已含截至 U03A 的登记成果 | 否，只读 | 不补场景、不删签名、不更新哈希 |
| `docs/execution/` | 已含 U03A 完成记录 | 是，仅发现记录与拆号 | 不覆盖历史记录 |
| 正式 dist、Git index/远端、真实数据/API | 禁止 | 否 | 不正式构建、不暂存/提交/推送、不读取或修改真实数据 |

DISC-U03B 不获得局部字段整改、新共享变体、模板迁移、测试/场景/基线变更、正式 build 或 Git 写入授权。发现产品语义分叉或现有证据不足时必须停止，不通过换类名逃避 UI 守卫。

DISC-U03B 完成反查：四个搜索字段的模板/局部样式、权威 `.pc-search-field` 和四个既有场景均只读。确认四处重复结构可直接复用共享搜索控件，修改前后三尺寸证据预备为 12/12；其余 25 条签名不能归入同一根因。工作区计数、源码、测试、机器/视觉/外观基线、正式 dist、Git index 和真实数据均未变化。U03B 只允许修改四个搜索模板及对应手写样式、新增定向测试、删除四条精确机器签名和更新执行文档。

U03B 完成反查：四个目标模板只切换共享搜索类并删除各自三段重复 scoped CSS；查询状态、事件、placeholder、工具栏和列表均保留。T0 精确报告 20 项，定向/相邻 3/3；机器遗留由 62/62 精确降至 58/58。四场景修改前后三尺寸各 12/12，完整 `pnpm verify` 的 137/137 单测、36/36 UI、15/15 外观证据和临时构建通过。完成时工作区为 77 个已跟踪修改、54 个未跟踪文件；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希不变，未操作真实数据。

U03C 只允许在已修改的 `WorkbenchApp.vue` 目标组合选择器中移除 `.pc-field-group span`，新增一项定向测试、删除一条精确机器签名并更新执行文档；不获得修改字段 margin、textarea、模板、工作流、全局样式、fixture、正式 dist 或 Git 状态的授权。

U03C 完成反查：只从 Workbench 目标组合选择器移除一项成员，定向 2/2，机器遗留由 58/58 精确降至 57/57；`workbench-forum-step` 修改前后三尺寸各 3/3且三张截图逐尺寸哈希完全一致。完整 `pnpm verify` 的 138/138 单测、36/36 UI、15/15 外观证据和临时构建通过。完成时工作区为 77 个已跟踪修改、55 个未跟踪文件；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希不变，未操作真实数据。

DISC-U03C 仅只读复核剩余 24 个字段签名并更新执行文档；未改源码、样式、测试、场景或基线。发现 textarea 至少存在短输入、多行模板、预览测试、长正文、全高编辑五种任务，特殊数字字段又分游戏输入和版本步进器；07 文档没有给出这些任务的唯一高度，已按自动执行协议触发产品语义停止。等待用户确认推荐的少量语义档位或选择保留现有高度后，才可建立后续实施批次。

## 七十六、U03D 工作树归属

2026-08-14 用户已确认 DISC-U03C 的推荐语义，原产品停止解除。U03D 登记时 `HEAD` 与 `origin/main` 仍同为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0；工作区已有 77 个已跟踪修改和 55 个未跟踪文件，均为截至 DISC-U03C 的已登记未发布成果。机器遗留为 57/57，正式视觉清单为 208 场景/624 运行，正式 dist 保持前批哈希。

| 文件或范围 | U03D 开始状态 | 本批是否允许修改 | 保护说明 |
| --- | --- | --- | --- |
| `src/apps/digest/DigestApp.vue` | 来源原文使用 `pc-area compact`，局部改为 100px | 是，只删除该目标高度覆盖 | 保留长正文、模板、v-model、保存和其他样式 |
| `src/apps/gallery/GalleryApp.vue`、`src/apps/video/VideoApp.vue` | 备注使用 `pc-area compact`，各局部改为 90px | 是，只删除两个目标高度覆盖 | 保留媒体编辑、上传/保存/删除行为和其他样式 |
| `src/apps/media/MediaGenerateApp.vue` | 参数预览同时命中 86px 专用规则和 90px generic compact 规则 | 是，只删除这两项高度覆盖 | 保留 raw area 180px、生成/解析/保存逻辑和其他媒体样式 |
| `src/apps/workbench/WorkbenchApp.vue` | 步骤直接子 compact 文本域局部改为 82px | 是，只删除该目标高度覆盖 | 保留工作流、步骤模板/事件、标签 margin 和其他样式 |
| `src/testing/visual-harness.ts`、`src/testing/visual/scenarioCatalog.ts` | 根场景未进入四个目标页；Workbench 已有隔离场景 | 是，只新增四个只读目标场景 | 空白编辑页不点击保存；Media 只用测试内存预览草稿，不连接 API |
| `scripts/unit/short-area-density-contract.test.mjs` | 不存在 | 是，新建定向失败契约 | 锁定全局 compact 96px 与六项局部覆盖归零 |
| `scripts/baselines/ui-contracts.json` | 57/57，五个目标签名 | 是，只删除五个精确签名 | 其他 52/52 必须不变 |
| `scripts/baselines/ui-visual.json` | 208 场景/624 运行 | 是，只增加四个新场景的三尺寸零问题记录 | 不改既有场景 findings |
| `docs/execution/` | 已保留 DISC-U03C 停止事实 | 是，只追加用户决定、U03D 契约与证据 | 不覆盖历史停止记录 |
| 其他 textarea/数字控件、全局样式、正式 dist、Git index/远端、真实数据/API | 禁止 | 否 | 不跨语义根，不正式构建、暂存/提交/推送，不读写真实数据 |

U03D 不获得改变任何模板交互、业务默认值、保存/生成/解析行为、120/160px 及长正文/全高/数字语义、正式 build、暂存、commit、push 或发布授权。任一场景不能用空白页/测试内存稳定露出目标，或 96px 引起窄屏覆盖/裁切时必须停止。

U03D 完成反查：五个消费者只删除六项局部 `min-height`，统一继承全局 compact 96px；模板、v-model、事件和业务逻辑未改。新增四个只读场景，与既有 Workbench 场景在修改前后三尺寸各 15/15；正式视觉清单增至 212/636。机器遗留由 57/57 降至 52/52，完整验证为 139/139 单测、36/36 UI、15/15 外观证据与临时构建通过。完成时工作区为 77 个已跟踪修改、56 个未跟踪文件；Git index 为 0，`HEAD`/`origin/main` 与正式 dist 哈希不变，未操作真实数据或 API。

## 七十七、U03E 工作树归属

U03E 登记时工作区为 77 个已跟踪修改、56 个未跟踪文件，机器遗留 52/52，正式视觉清单 212/636，Git index 为 0，`HEAD`/`origin/main` 与正式 dist 不变。本批只允许修改 `RegexDisplayApp.vue` 的 `.pc-area.preview-source` 高度、`RegexWizardApp.vue` 的 `.pc-regex-wizard-test .pc-area` 高度（保留等宽字体）、新定向测试、两个新只读场景、两条精确机器基线、六条新视觉运行和执行文档。其他正则样式/行为、textarea 语义、全局样式、正式 dist、Git 写入和真实数据均禁止。

U03E 完成反查：两个消费者只删除局部高度，RegexWizard 等宽字体、全部模板与业务行为保留。新增两个只读场景，修改前后各 6/6；正式视觉清单 214/642，机器遗留 50/50。完整验证为 140/140 单测、36/36 UI、15/15 外观证据与临时构建通过。完成时工作区为 77 个已跟踪修改、57 个未跟踪文件，Git index 为 0，正式 dist 哈希不变，未操作真实数据或 API。

## 七十八、U03F 工作树归属

U03F 登记时 `HEAD` 与 `origin/main` 仍为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0，工作区为 77 个已跟踪修改、57 个未跟踪文件，机器遗留 50/50，正式视觉清单 214/642，正式 dist 哈希不变。`ChatInsertApp.vue`、`EntryLibraryBindingsPage.vue`、`GenerationPanel.vue` 已有与本批 CSS/类名区域不重叠的 U01 aria-label diff；Extras、ForumThreadGenerate、GenerationSourceFields 目标区域无既有 diff。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/global.css` | 只新增 `.pc-area.pc-area-multiline` 120px | 不改 `.pc-area` 160px 和 `.compact` 96px |
| 6 个目标 Vue 文件 | 只替换目标 textarea 类名、删目标 120px；Extras 长正文选择器只增加排除 | 保留 U01 diff、模板事件、业务默认和其他样式 |
| `scripts/unit/multiline-area-density-contract.test.mjs` | 新建定向契约 | 锁定 6 个消费者、Extras 排除和共享 120px |
| 视觉 harness/catalog 与 `ui-visual.json` | 仅补稳定露出 6 类目标的只读场景/运行 | 不触发生成、插入、保存或真实数据 |
| `scripts/baselines/ui-contracts.json` | 只删 6 条目标签名 | 其他 44/44 必须不变 |
| `docs/execution/` | 记录 U03F 契约、T0/T1 和反查 | 不改历史证据 |

U03F 不授权修改业务逻辑、长正文高度数值、其他 textarea/数字根因、正式 dist、Git 写入、真实数据或 API。

U03F 完成反查：新增全局 multiline 120px、迁移 6 个消费者并删除 6 条局部高度；Extras 220px/vertical 规则仅增加排除且由离屏探针确认计算样式不变。新增 3 个场景，正式视觉清单全量生成至 217/651；机器遗留 44/44。完整验证为 141/141 单测、36/36 UI、15/15 外观证据与临时构建通过。完成时工作区为 83 个已跟踪修改、58 个未跟踪文件，Git index 为 0；正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，未操作真实数据或 API。

## 七十九、U03G 工作树归属

U03G 登记时 `HEAD` 与 `origin/main` 仍为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0，工作区为 83 个已跟踪修改、58 个未跟踪文件，机器遗留 44/44，正式视觉清单 217/651，正式 dist 哈希不变。PromptsApp 目标规则未包含在现有 U02G diff；ExtrasApp 目标规则属于刚完成的 U03F 收窄结果，均由当前自动批次链拥有。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/components/PromptsApp.vue` | 只删无法跨子组件命中的父级 field/area scoped 规则 | 保留 U02G 动态变体 diff、全部子页面和业务 |
| `src/components/ExtrasApp.vue` | 只删已无直属消费者的 220px 排除规则 | 保留 multiline 摘要、其他样式和业务 |
| `scripts/unit/orphan-parent-area-style-contract.test.mjs` | 新建定向契约 | 锁定父模板边界与三条死几何规则清零 |
| `scripts/baselines/ui-contracts.json` | 只删 3 条几何记录 | 其他 41/41 必须不变 |
| 既有四个视觉场景与 `docs/execution/` | 只保存同哈希证据、记录 T0/T1 | 不新增业务 fixture 或正式场景 |

U03G 不授权修改任何实际编辑器高度、子组件、全局样式、业务逻辑、正式 dist、Git 写入、真实数据或 API。

U03G 停止反查：初始模板截取被嵌套 template 提前终止，导致 Prompts 消费者漏检。删除试验使 Prompts 三场景三尺寸 9/9 发生真实视觉变化，已触发停止条件；所有 U03G 源码、测试和机器基线试验均已回滚。四场景三尺寸 12/12 与试验前 SHA-256 同哈希，严格机器基线恢复 44/44，U03F 已验证状态保持不变。未触碰正式 dist、Git index、真实数据或 API。下一步不得沿用本节初始契约，必须先取得修订语义拆分授权。

## 八十、U03H 工作树归属

用户于 2026-08-14 明确同意修订拆分并恢复自动执行。U03H 登记时 `HEAD`/`origin/main` 为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0，工作区为 83 个已跟踪修改、58 个未跟踪文件，机器遗留 44/44，正式视觉清单 217/651，正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/components/ExtrasApp.vue` | 只删无生产消费者的 220px/vertical 排除规则 | 保留 U03F multiline 摘要及全部业务/其他样式 |
| `src/testing/visual/extrasGenerationScenarios.ts` | 只删人工克隆普通 area 的离屏探针 | 保留真实摘要 fixture、滚动和可见性断言 |
| `scripts/unit/extras-orphan-area-style-contract.test.mjs` | 新建定向契约 | 使用完整父模板边界，防止重现 U03G 截断误判 |
| `scripts/unit/multiline-area-density-contract.test.mjs` | 只删“必须保留 Extras 排除规则”的过期断言 | 保留全局 120px、6 个消费者和局部高度清零守卫 |
| `scripts/baselines/ui-contracts.json` | 只删 Extras 目标签名 1 条 | 其他 43/43 必须不变 |
| `extras-chapter-preview-summary` 与执行文档 | 只保存修改前后同哈希证据和 T0/T1 | 不新增场景、不改正式视觉基线 |

U03H 不授权修改 Prompts、任何真实编辑器高度、全局样式、业务逻辑、正式 dist、Git 写入、真实数据或 API。

U03H 完成反查：Extras 只删除无生产消费者的 220px/vertical 排除规则、人工离屏探针和一条被新授权替代的历史断言；真实摘要 multiline、fixture、滚动、业务均保留。机器遗留 43/43，摘要三尺寸修改前后 3/3 同哈希；完整验证 142/142 单测、36/36 UI、15/15 外观与临时构建通过。完成时工作区为 83 个已跟踪修改、59 个未跟踪文件，Git index 为 0，正式 dist 哈希不变，未操作真实数据或 API。

## 八十一、U03I 工作树归属

U03I 登记时 `HEAD`/`origin/main` 仍为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0，工作区为 83 个已跟踪修改、59 个未跟踪文件，机器遗留 43/43，正式视觉清单 217/651，正式 dist 哈希不变。PromptsApp 已有 U02G 动态变体 diff，但本批目标为未修改的父级 area 选择器；PromptPhraseEditorPage 目标区域无既有 diff。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/components/prompts/PromptPhraseEditorPage.vue` | 只把共享 textarea 类从 compact 改为 multiline | 不改 kind、placeholder、保存/分组行为和局部布局 |
| `src/components/PromptsApp.vue` | 260px generic area 选择器只增加 multiline 排除 | 保留 260px/resize、compact 180px、margin、U02G diff 和业务 |
| `scripts/unit/prompts-multiline-area-contract.test.mjs` | 新建定向契约 | 锁定两个模式共享 120px 且解析 sample 仍为 compact |
| `src/testing/visual/promptsScenarios.ts`、`scenarioCatalog.ts`、`ui-visual.json` | 只新增 template-editor 只读场景及三尺寸运行 | 保留既有 phrase 场景和所有其他场景 |
| `scripts/baselines/ui-contracts.json` | 只把 Prompts generic 选择器键改为排除 multiline | 总数和其他 42/42 不变 |
| `docs/execution/` | 记录 U03I T0/T1 和反查 | 保留 U03G 停止、U03H 完成事实 |

U03I 不授权修改解析 sample、compact 180px、长模板高度、全局样式、业务逻辑、正式 dist、Git 写入、真实数据或 API。

U03I 完成反查：共享短语/模板 textarea 只从 compact 改为 multiline，父级 generic 260px 规则只增加 multiline 排除；新增模板编辑只读场景，正式视觉增至 218/654，机器遗留保持 43/43。完整验证为 143/143 单测、36/36 UI、15/15 外观与临时构建通过。完成时工作区为 85 个已跟踪修改、60 个未跟踪文件，Git index 为 0；正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，未操作真实数据或 API。

## 八十二、U03J 工作树归属

U03J 登记时 `HEAD`/`origin/main` 仍为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0，工作区为 85 个已跟踪修改、60 个未跟踪文件，机器遗留 43/43，正式视觉清单 218/654，正式 dist 哈希不变。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/global.css` | 新增 preview/test 160px 共享类 | 不改现有 area/compact/multiline 数值和其他全局控件 |
| `src/components/prompts/PromptOutputEditorPage.vue` | sample 只从 compact 改为 preview 类 | 保留 outputFormat、解析配置、测试/保存事件和局部布局 |
| `src/components/PromptsApp.vue` | generic 260px 只增加 preview 排除；删除 compact 180px | 保留 multiline 排除、260px/resize、margin、U02G diff 与业务 |
| `scripts/unit/prompts-preview-area-contract.test.mjs` | 新建定向契约 | 锁定 sample 160px、长正文 260px 和 multiline 120px 边界 |
| `scripts/baselines/ui-contracts.json` | generic 键迁移并删除 compact 记录 | 目标后总数 42/42，其他 41/41 不变 |
| `prompts-output-editor` 与执行文档 | 只保存三尺寸前后证据和 T0/T1 | 不新增业务 fixture，不改正式视觉场景总数 |

U03J 不授权修改长正文消费者/数值、PromptAppEditor、解析业务、其他 App、正式 dist、Git 写入、真实数据或 API。

U03J 停止反查：目标可见的修改前截图显示 sample 为全局 compact 96px、outputFormat 为全局 area 160px；临时编译选择器确认父组件 180/260px 规则要求子 textarea 携带父 scope 属性，实际不会命中。该事实与初始契约不一致，已回滚本批全部生产、场景、测试和机器基线试验；U03I 已验证源码、218/654 正式视觉基线、43/43 机器基线均保留，正式 dist、Git index、真实数据/API 未触碰。后续必须把真实控件高度迁移与无消费者父规则清理拆为不同批次。

## 八十三、U03K 工作树归属

用户于 2026-08-14 确认修订三批并恢复自动执行。U03K 登记时 `HEAD`/`origin/main` 仍为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0，工作区为 85 个已跟踪修改、60 个未跟踪文件，机器遗留 43/43，正式视觉清单 218/654，正式 dist 哈希不变。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/global.css` | 新增 preview/test 160px 共享类 | 不改 generic area、compact、multiline 和其他全局控件 |
| `src/components/prompts/PromptOutputEditorPage.vue` | sample 只从 compact 改为 preview | 不改 outputFormat、解析/测试/保存业务和局部布局 |
| `src/testing/visual/promptsScenarios.ts`、`ui-visual.json` | 既有 output 场景只稳定露出 sample，并更新三尺寸证据 | 不新增场景或业务 fixture |
| `scripts/unit/prompts-preview-area-contract.test.mjs` | 新建定向契约 | 锁定真实任务语义并保护父级 180/260px 规则原样保留 |
| `scripts/unit/prompts-multiline-area-contract.test.mjs` | 只删“专属批次前 sample 必须仍为 compact”的过期阶段断言 | 保留 multiline、模板场景和父级规则守卫 |
| `docs/execution/` | 记录 U03K T0/T1 和反查 | 保留 U03J 停止事实 |

U03K 不授权修改 Prompts 父级高度规则/机器基线、长正文消费者、解析业务、其他 App、正式 dist、Git 写入、真实数据或 API。

U03K 完成反查：只新增全局 preview 160px、迁移 sample 并使既有场景露出目标；机器 43/43，正式视觉 218/654，完整验证 144/144 单测及全部 UI/外观/临时构建通过。Git index 与正式 dist 不变，未操作真实数据/API。

## 八十四、U03L 工作树归属

U03L 登记时工作区为 86 个已跟踪修改、61 个未跟踪文件，Git index 为 0，机器 43/43，正式视觉 218/654，正式 dist 哈希不变。目标仅为 8 类不带 `pc-saved-content-area`、不属全高编辑器的普通长正文；各目标组件、全局类、定向测试、必要视觉场景和两条机器基线由本批拥有，其余范围保护。

U03L 完成反查：8 类普通长正文统一 260px，机器 41/41，正式视觉 220/660，完整验证通过；Git index、正式 dist、真实数据/API 未变。

## 八十五、U03M 工作树归属

U03M 只拥有 `PromptsApp.vue` 两条无消费者高度规则、对应机器基线、定向契约、五个只读场景同哈希证据和执行文档；不修改任何真实消费者或全局语义。

U03M 完成反查：两条孤儿规则及机器记录已删除，机器 39/39；五场景 15/15 前后同哈希，最终完整验证 146/146 单测及全部 UI/外观/临时构建通过。完成时工作区 90 个已跟踪修改、63 个未跟踪文件，Git index 为 0，正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，未操作真实数据/API。

## 八十六、U03N 工作树归属

U03N 登记时 `HEAD`/`origin/main` 仍为 `edcb741f466ad11013c5f3b625eb373a3f5835e2`，Git index 为 0，工作区起点为 90 个已跟踪修改、63 个未跟踪文件，机器遗留 39/39，正式视觉 220/660，正式 dist 哈希不变。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/global.css` | 只新增 `pc-guess-number-input` 的 22px/800/居中/无字距语义 | 不改普通字段及其他共享控件规格 |
| `src/apps/game-2048/GuessNumberGame.vue` | input 增加专用类并删除 `.pc-guess-form .pc-field` 目标规则 | 不改 model、清洗、校验、提交、存储、按钮或布局 |
| `scripts/unit/guess-number-input-contract.test.mjs` | 新建定向契约 | 同时锁定外观语义与输入/提交行为属性 |
| `src/testing/visual-harness.ts`、`scenarioCatalog.ts`、`ui-visual.json` | 只新增打开 `games/play?game=guess-number` 的只读三尺寸场景 | 不点击、输入、提交或操作存储 |
| `scripts/baselines/ui-contracts.json` | 只删除 GuessNumberGame 目标签名 | 其他 38/38 必须不变 |
| `docs/execution/` | 记录 U03N 的 T0/T1 与保护反查 | 版本导航另批处理 |

U03N 不授权修改版本导航、其他游戏、字段基础规格、业务逻辑、正式 dist、Git 写入、真实数据或 API。

U03N 完成反查：猜数字只增加专用共享语义、迁移原 input 类并删除局部通用字段重画；输入、提交和存储行为保持。机器遗留 38/38，正式视觉 221/663，完整验证 147/147 单测及全部 UI/外观/临时构建通过。350px 统计卡裁切另登记且未混入修复。完成时工作区 91 个已跟踪修改、64 个未跟踪文件，Git index 为 0，正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，未操作真实数据/API。

## 八十七、U03O 工作树归属

U03O 登记时工作区为 91 个已跟踪修改、64 个未跟踪文件，Git index 为 0，机器 38/38，正式视觉 221/663，正式 dist 哈希不变。

| 文件或范围 | 本批允许 | 保护说明 |
| --- | --- | --- |
| `src/global.css` | 只新增 `pc-version-index-input` 固定序号跳转语义 | 不改普通字段和 U03N 游戏输入 |
| `src/components/VersionNavigator.vue` | input 增加专用类并删除 `.pc-version-status .pc-field` 规则 | 不改 props、watch、取整/边界、emit、按钮或布局 |
| `scripts/unit/version-index-input-contract.test.mjs` | 新建定向契约 | 锁定外观语义与 blur/Enter 跳转链 |
| `src/testing/visual-harness.ts`、`scenarioCatalog.ts`、`ui-visual.json` | 只新增内存两版本的专属只读场景 | 不触发输入、失焦、版本选择、删除或保存 |
| `scripts/baselines/ui-contracts.json` | 只删除 VersionNavigator 目标签名 | 其他 37/37 不变 |
| `docs/execution/` | 记录 T0/T1 与 U03 收口 | 不提前进入 U04 |

U03O 不授权修改其他阅读器、版本业务、按钮、普通字段、正式 dist、Git 写入、真实数据或 API。

U03O 完成反查：VersionNavigator 只增加专用共享语义、迁移原 input 类并删除局部通用字段重画；版本选择业务保持。机器遗留 37/37，正式视觉 222/666，完整验证 148/148 单测及全部 UI/外观/临时构建通过。完成时工作区 92 个已跟踪修改、65 个未跟踪文件，Git index 为 0，正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，未操作真实数据/API。U03 字段阶段完成。

## 八十八、DISC-U04 工作树归属

DISC-U04 登记时工作区为 92 个已跟踪修改、65 个未跟踪文件，Git index 为 0，机器 37/37，正式视觉 222/666，正式 dist 哈希不变。本批只允许修改 `docs/execution/` 的发现记录；SearchableCombobox、23 个 `.pc-select` 源文件、场景、测试和基线全部只读。任何动态资源迁移必须先登记独立 D 编号和实施批次。

DISC-U04 完成反查：只更新执行文档和缺陷编号；源码、样式、41 个原生 select、98 个 SearchableCombobox 实例、测试、场景和基线均未改。发现结果拆为 U04A—U04D，未把共享长选项、固定类名、动态资源和阅读字体四个根因合批。

## 八十九、U04A 工作树归属

U04A 登记时工作区为 92 个已跟踪修改、65 个未跟踪文件，Git index 为 0，机器 37/37，正式视觉 222/666，正式 dist 哈希不变。本批只拥有 `SearchableCombobox.vue` 的 option span 换行规则、`searchable-select` 测试 fixture/交互链、新 dark-long 场景、定向契约、对应视觉基线和执行文档；toggle 34px、组件 API、98 个消费者及全部原生 select 受保护。

U04A 完成反查：共享组件只移除菜单项的强制省略并启用自然换行；98 个消费者、toggle、API 均未改。机器 37/37，正式视觉 223/669，完整验证 149/149 单测及全部 UI/外观/临时构建通过。完成时 Git index 为 0，正式 dist 哈希不变，未操作真实数据/API。

## 九十、U04B 工作树归属

U04B 只拥有 ChatInsertApp 两个与 ExtrasBookEditorPage 一个固定枚举的 class、定向契约、两个既有场景的前后同哈希证据和执行文档；其他 select、业务、全局样式、机器/视觉基线、正式 dist、Git 与真实数据受保护。

U04B 完成反查：三个目标均只改为固定枚举权威类；Extras 旧 `.pc-field` 同时承担的 14px 间距已由 `pc-extras-mode-select` 局部布局类显式承接，未改共享控件。两场景三尺寸 6/6 前后逐图同哈希，机器 37/37、正式视觉 223/669，完整验证 150/150 单测及全部 UI/外观/临时构建通过。完成时工作区 93 个已跟踪修改、67 个未跟踪文件，Git index 为 0，正式 dist 哈希不变，未操作真实数据/API。

## 九十一、U04C 工作树归属

U04C 只拥有 ExtrasChapterGeneratePage 的摘要规则、PromptTypeEditorPage 的分组、StorylineEditorPage 的两处剧情线归属选择器，以及必要 option computed/import、一个新增隔离场景、两个既有场景的定向增强、定向契约、正式视觉清单/基线和执行文档。其他 37 个原生 select、共享组件本体、阅读器字体、业务、机器基线、正式 dist、Git 与真实数据受保护。

U04C 完成反查：四个目标均迁移到共享可搜索选择器，三种空值、番外禁用与 v-model 语义保留；内存长正则/分组/剧情线在三个尺寸完整显示。机器 37/37，正式视觉 224/672，完整验证 151/151 单测及全部 UI/外观/临时构建通过。完成时工作区 94 个已跟踪修改、68 个未跟踪文件，Git index 为 0，正式 dist 哈希不变，未操作真实数据/API。

## 九十二、U04D 工作树归属

U04D 只拥有 SettingsReaderPanel 的字体选择器/映射、ThemeApp 未被模板消费的 reader 选择草稿清理、一个内存导入字体隔离场景、定向契约、正式视觉清单/基线和执行文档。字体上传/删除/文件 API、手机主字体、阅读器正文、共享组件本体、机器基线、正式 dist、Git 与真实数据受保护。

U04D 完成反查：实际设置页已接通全部导入字体，选择映射复用现有 store family API；ThemeApp 四项未消费 reader 草稿删除。三尺寸长名/搜索/选择通过，机器 37/37，正式视觉 225/675，完整验证 152/152 单测及全部 UI/外观/临时构建通过。完成时工作区 96 个已跟踪修改、69 个未跟踪文件，Git index 为 0，正式 dist 哈希不变，未操作字体文件、真实数据或 API。U04 阶段完成。

## 九十三、DISC-U05 工作树归属

DISC-U05 登记时工作区为 96 个已跟踪修改、69 个未跟踪文件，Git index 为 0，机器 37/37，正式视觉 225/675，正式 dist 哈希不变。本批只允许修改 `docs/execution/` 的发现记录；CatalogModal、ActionMenu、所有 overlay/menu 消费者、场景、测试和基线只读。任何交互迁移必须先登记独立 D 编号和实施批次。

DISC-U05 完成反查：只更新执行文档和缺陷编号；生产源码、10 个 dialog、5 个 ActionMenu、测试、场景、机器/视觉基线均未改。发现拆为 U05A Catalog 生命周期基础、U05B ActionMenu、U05C 简单弹窗与 U05D 复杂弹窗四批，没有把不同业务内容合并。

## 九十四、U05A 工作树归属

U05A 只拥有一个共享 modal lifecycle composable、CatalogModal 的生命周期/关闭按钮接线、既有 summary 目录场景交互增强、定向契约、必要机器/视觉基线和执行文档。Catalog 的 7 个消费者只读；ActionMenu、其他 9 个 dialog 定义、目录业务、正式 dist、Git 与真实数据受保护。

U05A 完成反查：新增共享 modal lifecycle composable，CatalogModal 只接线生命周期并把关闭按钮迁移到全局图标按钮；7 个消费者和目录业务均未修改。三尺寸 3/3，完整验证 153/153 单测、37/37 机器、36/36 UI、15/15 外观及临时构建通过。完成时工作区 98 个已跟踪修改、71 个未跟踪文件，Git index 为 0，正式视觉保持 225/675，正式 dist 哈希不变，未操作真实数据/API。

## 九十五、U05B 工作树归属

U05B 只拥有共享 ActionMenu 的 Escape/手机返回/窄屏长文本语义、既有 entry-library/comfy 内存场景的真实交互增强、定向契约、必要机器/视觉基线和执行文档。5 个消费者的业务动作与危险操作语义、其他 modal/menu、正式 dist、Git 与真实数据受保护。

U05B 完成反查：共享 ActionMenu 增加 Escape/手机返回、220px/60vh 边界、纵向滚动与长标签换行；5 个消费者业务模板未改。entry-library/comfy 相关内存场景全部通过真实 summary 到达；六个目标运行和完整验证通过。完成时工作区 99 个已跟踪修改、72 个未跟踪文件，Git index 为 0，正式视觉保持 225/675，正式 dist 哈希不变，未操作真实数据/API。

## 九十六、U05C 工作树归属

U05C 只拥有 CreationModeModal、ReasoningModal、GenerationPreviewPanel 解析提示、PromptsApp 两个只读详情的共享生命周期接线与重复监听清理、既有内存场景交互增强、定向契约、必要机器/视觉基线和执行文档。复杂编辑/迁移弹窗、提示词/生成业务、正式 dist、Git 与真实数据受保护。

U05C 完成反查：5 个简单 dialog 接入共享生命周期，Creation 重复监听删除；提示词和生成业务未改。修复日记空白视觉夹具，正式交互集扩为 9 项。完整验证 155/155 单测、37/37 机器、42/42 UI/交互、15/15 外观及临时构建通过。完成时工作区 104 个已跟踪修改、73 个未跟踪文件，Git index 为 0，正式视觉保持 225/675，正式 dist 哈希不变，未操作真实数据/API。

## 九十七、U05D 工作树归属

U05D 只拥有 BaguHitDetailsModal、ReaderTextEditModal、ContentTransferOverlay、PhoneOverlay 文件夹 dialog 的共享生命周期接线与重复监听清理、既有 bagu/reader/content-transfer 内存场景交互增强、定向契约、必要正式交互/视觉基线和执行文档。四项业务事务、其他 UI、正式 dist、Git 与真实数据受保护。

U05D 完成反查：四个复杂 dialog 接入共享生命周期，Bagu/Reader 重复监听删除；Reader 自动 textarea 选区、ContentTransfer busy guard、Bagu/文件夹业务均保留。活动 modal 场景的 DOM 守卫只排除被遮罩后台，仍检查顶层 dialog 内部。完整验证 156/156 单测、37/37 机器、51/51 UI/交互、15/15 外观及临时构建通过。完成时工作区 106 个已跟踪修改、74 个未跟踪文件，Git index 为 0；正式 dist JS/CSS 哈希分别为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，均与起点一致。U05 生命周期阶段完成，D-UI-MODAL-002 转 DISC-U06。

## 九十八、DISC-U06 工作树归属

DISC-U06 只读审计 10 个 dialog 的遮罩/表面/业务布局与现有场景；仅执行文档登记矩阵和 D-TEST-MODAL-002。生产源码、全局 CSS、场景、基线、正式 dist、Git 与真实数据均未改。结论拆为 U06A backdrop 与 U06B surface，不新增 ModalShell。

## 九十九、U06A 工作树归属

U06A 只拥有全局 `.pc-modal-backdrop`、10 个模板类名、9 个 scoped 遮罩块的重复基础清理、定向契约、既有 modal 参考场景、必要基线与执行文档。dialog 表面/尺寸/滚动、生命周期、业务关闭、正式 dist、Git 与真实数据受保护。

U06A 完成反查：10 个定义均消费唯一 `.pc-modal-backdrop`；局部遮罩只保留层级、Prompts 偏移与 ContentTransfer/Home 模糊差异。七个场景三尺寸 21/21、完整验证 157/157 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。完成时工作区 106 个已跟踪修改、75 个未跟踪文件，Git index 为 0；正式 dist 哈希不变。

## 一百、U06B 工作树归属

U06B 只拥有全局 `.pc-modal-dialog`、10 个模板类名、9 个 scoped dialog 表面重复清理、ContentTransfer 既有内存场景的深色不透明断言、定向契约、必要基线与执行文档。dialog 宽高/圆角/padding/布局/滚动、生命周期和业务关闭、正式 dist、Git 与真实数据受保护。

U06B 完成反查：10 个定义均消费唯一 `.pc-modal-dialog`；只迁移主题边框、实底、阴影与 outline，业务几何和事务未改。ContentTransfer 深色计算背景 alpha≥0.99，七场景三尺寸 21/21，完整验证 158/158 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。完成时工作区 106 个已跟踪修改、76 个未跟踪文件，Git index 为 0；正式 dist JS/CSS 哈希与起点一致，未操作真实数据/API。

## 一百零一、DISC-GEN 工作树归属

DISC-GEN 只读审计生成表单、generation service、运行/预览/失败草稿状态、任务 store/schema 和既有场景；仅执行文档登记 D-GEN-LIFECYCLE-001、授权后实施设计与 C 级权限结论。生产源码、schema、公共 API、场景、正式 dist、Git 和真实数据/API 均未改。GEN01 无已复现缺陷；GEN02 的 15 个业务所有者/14 条卸载停止链等待 schema 授权；GEN03 不把 14 处业务协调重复冒充功能缺陷。

## 一百零二、GEN02A 工作树归属

用户于 2026-08-14 明确授权 GEN02 修改持久任务 schema 与共享状态 API。GEN02A 仅拥有 `src/type/generationTask.ts`、`src/store/generationTasks.ts`、generation-tasks 备份域、共享 single session、`GenerationTaskCenter.vue`、必要的 generation lifecycle 类型导出、mock/fixture、定向契约和执行文档。15 个业务消费者、业务 parse/save/preview、旧批量/工作台恢复语义、正式 dist、Git、真实聊天/设置/API 均受保护；任何需要改变这些范围的发现必须停止并另立后续批次。

GEN02A 完成反查：旧 v1 备份无损迁移、running single 重载中断、停止提交 pending raw、TaskCenter 无假续跑及 raw 展开均由定向契约/fixture 验证；162/162 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。工作区 112 个已跟踪修改、78 个未跟踪文件、Git index 0；正式 dist 哈希不变，未操作真实数据/API。

## 一百零三、GEN02B 工作树归属

GEN02B 仅新增拥有 Storylines 与 ScenePlanner 的普通生成协调函数、其对应卸载停止链、两个 App 的 mock 场景/定向契约及必要的 GEN02A 公共 API 同根因回修。两个 App 的 prompt/provider/source/reference、XML 解析、实体/失败草稿/preview schema 和结果路由语义均受保护；其余生成消费者、正式 dist、Git、真实数据/API 不得修改。

GEN02B 完成反查：两个参考消费者只替换运行态所有权与生命周期；三条离页 mock 三尺寸 9/9 覆盖 preview/saved/failed-draft，完整验证 165/165 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。工作区 113 个已跟踪修改、79 个未跟踪文件、Git index 0；正式 dist 哈希不变，未操作真实数据/API。

## 一百零四、GEN02C 工作树归属

GEN02C 只拥有 Digest、Media、CloudMedia、Profiles、Relationship 五个 App 的 generationState 运行字段、generate lifecycle、stop/卸载停止链、任务完成路由、定向契约和代表 mock 场景；必要时可回修同根因 single session。五个 App 的 prompt/provider/source/reference、adapter/解析/保存、表单、preview/failed-draft schema 及其他消费者均受保护；正式 dist、Git、真实数据/API 不得修改。

GEN02C 完成反查：五个同型消费者只替换运行态所有权与生命周期；Digest/Relationship 两条离页 mock 三尺寸 6/6，完整验证 168/168 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。工作区 114 个已跟踪修改、80 个未跟踪文件、Git index 0；正式 dist 哈希不变，未操作真实数据/API。

## 一百零五、GEN02D 工作树归属

GEN02D 只拥有 Summary、Diary、Letters、Theater、Forum、Extras 六个内容域的普通单次 generation runtime、lifecycle、stop/卸载链、task 结果路由、定向契约和代表 mock 场景；日记/总结既有批量 task kind、store、pause/resume 和业务楼层 materialization 受保护。六域 prompt/provider/source/reference、adapter/解析/保存、表单、preview/failed-draft schema、其他消费者、正式 dist、Git、真实数据/API 均不得修改。

GEN02D 完成反查：六域九个普通 action 只迁移运行态所有权；日记/总结批量任务未进入 single。Summary/Theater 离页 mock 三尺寸 6/6，完整验证 171/171 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。工作区 123 个已跟踪修改、81 个未跟踪文件；正式 dist 哈希不变。

## 一百零六、GEN02E 工作树归属

GEN02E 只拥有 `src/apps/app-builder/CustomAppHost.vue` 的动态 single runtime、lifecycle、stop、task 路由及对应契约/mock。自制 App schema、定义、转换规则、动态 adapter parse/save、编辑器、其他消费者、正式 dist、Git、真实数据/API 均受保护。

GEN02E 完成反查：动态 CustomAppHost 只替换运行态所有权，appId 在挂载时固定且不同自制 App 不串任务；动态离页 mock 三尺寸 3/3，完整验证 173/173 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。正式视觉 243/729，正式 dist 哈希不变，未操作真实数据/API。

## 一百零七、GEN02F 工作树归属

GEN02F 只拥有 `src/apps/card-writer/CardWriterApp.vue` 的多阶段 sequence runtime/lifecycle/stop、通用 previewDraft 接线、task 结果路由；共享 single session 仅可增加聚合 raw 写回 API；另拥有定向契约、一个多阶段离页 mock、必要正式视觉基线和执行文档。CardWriter preset、ordered prompt、阶段定义/解析、成品/世界书 schema 与保存语义、其他消费者、正式 dist、Git、真实数据/API 均受保护。

开始反查：当前工作区 123 个已跟踪修改、83 个未跟踪文件，Git index 为 0；新增/修改均在既有自动执行成果与 GEN02F 所有权内。正式 dist JS/CSS 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02`、`BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

GEN02F 完成反查：CardWriter 只替换 sequence 运行态所有权，并复用既有通用 previewDraft 保存阶段可修复状态；preset、ordered prompt、阶段定义/解析、成品与世界书保存语义不变。定向契约 3/3、离页 mock 三尺寸 3/3，完整验证 176/176 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。完成时工作区仍为 123 个已跟踪修改、83 个未跟踪文件，Git index 0；正式 dist 哈希不变，未操作真实数据/API。GEN02 阶段完成。

## 一百零八、GEN03A 工作树归属

GEN03A 只拥有 SummaryFailedDraftPage 的 Summary 专属事件/model 接线、SummaryApp 的失败草稿 reparse handler、独立 summary reparse 浏览器场景/最小契约及执行文档。共享解析器、FailedDraftRepairPage、RawOutputEditor、其他业务重解析、schema、正式 dist、Git、真实数据/API 均受保护。T0 已在 350px 证明按钮启用且合法 XML 留在输入框，但没有 toastr、页面异常或路由变化；先处理 Summary 专属事件边界，不扩大到共享层。

GEN03A 完成反查：诊断确认事件和模型均正常，实际根因是 SummaryApp 缺少 `parseSimpleXmlResult` 运行时导入；最终生产改动仅新增该 import。独立重解析场景三尺寸 3/3，定向 2/2，完整验证 178/178 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建通过。完成时工作区 123 个已跟踪修改、84 个未跟踪文件，Git index 0；正式 dist JS/CSS 哈希与起点一致，未操作真实数据/API。GEN 系列完成。

## 一百零九、DISC-READ 工作树归属

DISC-READ 只读审计 ReaderDetailShell、DetailFooter、VersionNavigator、八个指定阅读域及既有场景；仅执行文档登记 D-READ-SHELL-001、D-READ-REASON-001、D-READ-SOURCE-001 和 READ02 无改码结论。生产源码、样式、场景、schema、正式 dist、Git 与真实数据均未改。后续严格拆为 READ01、READ03A、READ03B。

## 一百一十、READ01 工作树归属

READ01 只拥有 `src/apps/storylines/StorylineDetailPage.vue` 的详情外壳/局部布局、既有 storyline 视觉 fixture 的共享壳到达选择器、一项定向契约和执行文档。剧情结构化字段、store/CRUD、共享 ReaderDetailShell、其他阅读域、schema、正式 dist、Git、真实数据/API 受保护。

READ01 完成反查：生产修改只把 StorylineDetailPage 迁入 `ReaderDetailShell custom-content`，旧业务字段、跳转、编辑和删除未改；视觉 fixture 仅适配共享壳的真实 DOM/工具点击链。定向契约 2/2、详情/编辑三尺寸 6/6、严格 UI 复用、180/180 单测、37/37 机器、51/51 UI/交互、15/15 外观和临时构建均通过。完成时工作区 124 个已跟踪修改、85 个未跟踪文件，Git index 0；正式 dist JS/CSS 哈希与起点一致。

## 一百一十一、READ03A 工作树归属

READ03A 只拥有一个共享 message reasoning 提取工具、ReaderMessage/Pending 运行时类型与归一化、ReaderApp 到共享壳的 prop、ChatArchive 重复提取的替换，以及独立夹具/场景/契约和执行文档。聊天正文解析/编辑、备份 schema/恢复、ReasoningDisclosure 视觉、来源 meta、正式 dist、Git、真实数据/API 受保护。

READ03A 完成反查：两个原消费者已共用唯一提取入口，Reader 正文规则/编辑数据没有改变，备份结构与恢复事务未改。定向 3/3、三尺寸折叠交互 3/3、严格复用和完整 `pnpm verify` 退出 0：183/183 单测、37/37 机器、51/51 UI/交互、15/15 外观及临时构建通过。额外 738 次全目录尝试在 621 次时超时，没有写入基线，不记为通过。完成时工作区 125 个已跟踪修改、87 个未跟踪文件，Git index 0；正式 dist 哈希不变。

## 一百一十二、READ03B 工作树归属

READ03B 只拥有 ReaderDetailShell 的可选 sourceLabel 展示位/局部样式、日记/番外/论坛/书信/小剧场五个详情 prop、五个既有场景的内存来源夹具/断言、定向契约和执行文档。来源 schema/写入、Summary/Digest、reasoning、正文/版本/编辑、正式 dist、Git、真实数据/API 受保护。

READ03B 完成反查：五域只读取已有持久来源，番外为旧纪录保留 `sourceLabel` 回退；共享壳未传值时无新 DOM，其他消费者不受影响。定向 2/2、五场景三尺寸 15/15，严格复用与完整 `pnpm verify` 退出 0：185/185 单测、37/37 机器、51/51 UI/交互、15/15 外观及临时构建通过。完成时工作区 130 个已跟踪修改、88 个未跟踪文件，Git index 0；正式 dist 哈希不变。READ 系列完成。

## 一百一十三、DISC-CRUD 工作树归属

DISC-CRUD 只读审计正式页面的删除/解除 handler、store 调用链、共享确认入口和 registry 能力边界；生产源码、样式、schema、场景、正式 dist、Git 与真实数据均未改。登记 D-CRUD-CONFIRM-001/002，并把实施严格拆为 CRUD01A 原生确认迁移与 CRUD01B 无确认永久删除；CRUD02 导入导出、SETTINGS01 设置持久化继续隔离。

## 一百一十四、CRUD01A 工作树归属

CRUD01A 只拥有 CloudMedia 配置删除和 ChatArchive scope 迁移两个协调 handler、共享确认静态契约及执行文档；store、schema、迁移算法、其他删除入口、样式、正式 dist、Git 和真实数据/API 受保护。完成时定向 1/1、186/186 单测及完整验证通过；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百一十五、E05D 工作树归属

E05D 仅拥有 `readerScenarios.ts` 的 footer 场景就绪等待、一项契约和执行文档。CRUD01B 五个页面、Reader/ReaderDetailShell 生产代码、样式、schema、正式 dist、Git 与真实数据/API 均受保护；触发证据为正式全量写基线在 622/711 报共享壳尚未挂载，旧 225/675 基线未写入。

E05D 完成反查：只增加 fixture 就绪门，reader 混排 9/9、全量 711/711、关键基线读取 12/12；正式基线更新为 237/711。CRUD01B 完成时工作区 132 个已跟踪修改、91 个未跟踪文件，Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百一十六、DISC-CRUD02 工作树归属

DISC-CRUD02 仅只读审计 App registry 的 `backupDomains`、`contentReceiver`、`contentSourceProvider`，全局/当前聊天备份事务、页面导入导出入口及失败原文保留；生产源码、schema、基线、正式 dist、Git、真实文件和用户数据均受保护。未建立 D 编号前不实施修改。

DISC-CRUD02 完成：中央备份覆盖注册域且完整/当前聊天恢复事务成立；登记 D-CRUD-TRANSFER-001、D-CRUD-BACKUP-001/002。后两项涉及 MVU localStorage 兼容迁移和手工完整备份格式演进，未获得新授权；CRUD02A 可在现有格式内自动继续。

## 一百一十七、CRUD02A 工作树归属

CRUD02A 仅拥有 `src/util/contentTransfer.ts` 对共享备份事务执行器的接线、一项定向事务契约与执行文档。内容域 schema、copy/merge/replace 算法、页面 UI、插件预设、MVU、正式 dist、Git、真实文件/API/用户数据均受保护。

CRUD02A 完成反查：生产代码只把手写回滚替换为既有共享事务执行器，结构守卫同步禁止旧回滚重新出现；定向 1/1、事务行为 4/4，完整 `pnpm verify` 退出 0：190/190 单测、37/37 机器、51/51 UI/交互、15/15 外观及临时构建通过。完成时工作区 134 个已跟踪修改、92 个未跟踪文件、Git index 0；正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，未操作真实文件/API/用户数据。下一批停在新的 schema 授权边界。

## 一百一十八、CRUD02B 工作树归属

用户于 2026-08-15 明确授权 CRUD02B/CRUD02C 修改持久化 schema、完整备份格式和兼容迁移。CRUD02B 仅拥有 MVU 收藏/历史的新持久 store/schema、旧 localStorage 纯读取兼容函数、`mvu-modifier` 备份域、页面状态接线、定向测试与执行文档。MVU 实际变量 API、其他业务状态、插件预设/完整备份 v2（留 CRUD02C）、UI 样式、正式 dist、Git 与真实数据均受保护；测试只使用内存键值映射，不访问用户浏览器存储。

CRUD02B 完成反查：新 MVU store/schema、全局备份域和页面接线均在批次所有权内；旧键只读不删，损坏新字段不自动覆盖。定向 3/3、类型检查和完整 `pnpm verify` 通过；未改 UI 样式、MVU API、真实数据或正式 dist。

## 一百一十九、CRUD02C 工作树归属

CRUD02C 仅拥有 full PhoneBackup v2 的预设记录字段、设置页手工完整导出/恢复协调、插件预设 `replaceRecords` 的失败事务、对应纯/静态契约、教程与执行文档。current-chat v1、文件仓库 `repositorySchemaVersion: 1` 及其独立 pluginPresets 包装必须保持；单预设编辑、酒馆预设、页面样式、正式 dist、Git、真实 `/api/files` 和用户数据受保护。

CRUD02C 完成反查：v2 只用于设置页新手工完整备份，v1 full/legacy/current-chat 和文件仓库外层均保持；预设 records 与 appDefaults 同包，加载不全拒绝导出，文件替换与设置提交均有恢复边界。定向 3/3、最终完整验证 196/196 单测、37/37 机器、51/51 UI/交互、15/15 外观通过。完成时工作区 141 个已跟踪修改、97 个未跟踪文件、Git index 0；正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`，未调用真实 `/api/files`、未读写用户预设/MVU/备份。

## 一百二十、DISC-SETTINGS01 工作树归属

DISC-SETTINGS01 只读审计设置页面、settings store、资源导入/删除、共享确认、保存和重进恢复调用链；生产源码、样式、schema、测试基线、正式 dist、Git、真实文件/API/用户数据均受保护。未登记新的 D 编号前不得实施修改。

## 一百二十一、SETTINGS01A 工作树归属

SETTINGS01A 仅拥有 `src/store/settings.ts` 的资源删除顺序、`src/util/settingsResourceTransaction.ts` 的纯协调器、定向测试与执行文档。资源 schema、上传、设置 deep watch、页面 UI/样式、其他文件仓库、正式 dist、Git、真实 `/api/files` 与用户文件均受保护。

完成反查：字体/壁纸远端删除失败时不会再修改设置引用；不支持文件 API 时明确失败。定向事务 2/2，完整 `pnpm verify` 退出 0：198/198 单测、37/37 机器、51/51 UI/交互、15/15 外观通过。完成时工作区 142 个已跟踪修改、99 个未跟踪文件、Git index 0；正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。DISC 同时登记的跨 App 清理事务保持未修改，等待共享 reset API 授权；真实文件/API/用户数据均未操作。

## 一百二十二、SETTINGS01B 工作树归属

用户于 2026-08-15 明确授权修改 `PhoneAppResetHandler` 共享契约和外部副作用快照/回滚 API。SETTINGS01B 仅拥有 appRegistry reset 类型、`settingsResetTransaction.ts`、设置高级页事务接线、worldbook-link/world-slots 两个外部补偿消费者、定向测试与执行文档。备份 schema、清空全部、其他设置事件、页面视觉/样式、正式 dist、Git、真实聊天/世界书/API 均受保护。

完成反查：中央 reset 事务顺序执行、逆序补偿并恢复设置；两个会写酒馆世界书的消费者均不再逃逸失败边界。定向 4/4，完整 `pnpm verify` 退出 0：202/202 单测、37/37 机器、51/51 UI/交互、15/15 外观通过。后续自动完成 A01—A02 后的统一状态为 148 个已跟踪修改、103 个未跟踪文件、Git index 0；正式 dist 哈希仍为 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。未调用真实世界书或用户数据。

## 一百二十三、DISC-A 工作树归属

DISC-A 只读验收阶段 4 A 批的 PhoneOverlay、设置、主题、首页/Dock/文件夹/顶栏和全局提示。生产源码、样式、schema、测试/视觉基线、正式 dist、Git、真实设置/文件/API 均受保护；未登记 D 编号前不实施修改。

## 一百二十四、A01 工作树归属

A01 仅拥有 `home-layout-drag` 视觉场景、共享交互清单、对应静态契约、正式视觉测试基线和执行文档。PhoneOverlay、appLayout、settings 生产逻辑及全部样式受保护。完成证据为三尺寸实际拖拽链、714/714 全目录、203/203 单测和 54/54 默认交互；没有修改生产代码或真实设置。

## 一百二十五、A02 工作树归属

A02 仅拥有 `settings-theme-persistence` 内存视觉场景、共享交互清单、对应静态契约、正式视觉测试基线和执行文档。settings/theme 生产逻辑、样式、schema、真实 saveSettings/API、正式 dist 与 Git 受保护。完成证据为三尺寸 rehydrate、204/204 单测、57/57 默认交互与正式 239×3＝717 基线。

## 一百二十六、DISC-B 工作树归属

DISC-B 只读验收阶段 4 B 批的生成与阅读内容 App；未登记新 D 编号前，生产源码、样式、schema、测试/视觉基线、正式 dist、Git、真实聊天/API 均受保护。

完成反查：生成配置、离页任务与阅读详情能力已有既定回归；稳定缺口仅为五条独立失败草稿解析链没有真实浏览器重解析证据，登记为 `D-TEST-BUSINESS-REPARSE-001`。DISC-B 只修改执行文档。

## 一百二十七、B01 工作树归属

B01 仅拥有 B 批失败草稿视觉 fixture、场景目录、浏览器交互断言、对应静态契约、正式零 finding 基线和执行文档。生产解析器、业务组件/store、样式、schema、真实聊天/API、正式 dist 与 Git 受保护。

完成反查：七域三尺寸定向 21/21，正式全目录 244×3＝732/732；完整 `pnpm verify` 为 206/206 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、105 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百二十八、DISC-C 工作树归属

DISC-C 只读验收阶段 4 C 批的数据与酒馆配置 App；未登记新 D 编号前，生产源码、样式、schema、测试/视觉基线、正式 dist、Git、真实聊天/文件/世界书/API 均受保护。

阶段记录：已登记两个世界书消费者缺少显式 strategy 灯色证据；其他 C 域仍保持只读。

## 一百二十九、C02 工作树归属

C02 仅拥有世界书视觉 fixture、`world-strategy-lamps` 场景、对应静态契约、正式零 finding 基线和执行文档。worldbook/world-slots 生产代码和样式、schema、真实世界书/API、正式 dist 与 Git 受保护。

完成反查：定向契约 2/2、三尺寸 3/3、正式 245×3＝735/735 及完整门禁通过；完成时工作区 148 个已跟踪修改、106 个未跟踪文件、Git index 0，正式 dist 哈希不变。

## 一百三十、C03 工作树归属

C03 仅拥有 timekeeper/profiles 内存视觉 fixture、`timekeeper-profile-sync` 场景、对应静态契约、正式零 finding 基线和执行文档。两 App 生产代码和样式、schema、真实资料/聊天/API、正式 dist 与 Git 受保护。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 246×3＝738/738；完整 `pnpm verify` 为 210/210 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、107 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十一、C04 工作树归属

C04 仅拥有文件仓库专属内存文件服务 fixture、`file-repository-operations` 场景、对应静态契约、正式零 finding 基线和执行文档。文件仓库生产代码与样式、schema/备份格式/公共 API、真实酒馆文件/API、正式 dist 与 Git 受保护。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 247×3＝741/741；完整 `pnpm verify` 为 212/212 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、109 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十二、C05 工作树归属

C05 仅拥有 C04/C05 共用的内存文件服务、C04 fixture 的无行为复用、预设管理专属 `plugin-preset-lifecycle` 场景、对应静态契约、正式零 finding 基线和执行文档。预设生产代码与样式、schema/备份格式/公共 API、酒馆原生预设与真实文件/API、正式 dist 与 Git 受保护。

完成反查：T0 0/2、T1 两批契约 4/4、C05 三尺寸 3/3、正式 248×3＝744/744；完整 `pnpm verify` 为 214/214 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、112 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十三、C06 工作树归属

C06 仅拥有关系网内存 fixture、`relationship-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。relationship/profiles 生产代码与样式、schema/公共 API、真实资料/聊天/生成、正式 dist 与 Git 受保护。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 249×3＝747/747；修正一条测试局部变量 lint 后，完整 `pnpm verify` 为 216/216 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、114 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十四、C07 工作树归属

C07 仅拥有正则向导隔离设置 fixture、`regex-wizard-save` 场景、对应静态契约、正式零 finding 基线和执行文档。regex-wizard/regex-display/preset-link 生产代码与样式、schema/公共 API、真实酒馆正则/预设/聊天、正式 dist 与 Git 受保护。登记时工作区为 148 个已跟踪修改、114 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 250×3＝750/750；完整 `pnpm verify` 为 218/218 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、116 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十五、C08 工作树归属

C08 仅拥有正则替换隔离设置 fixture、`regex-display-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。regex-display/regex-wizard 生产代码与样式、schema/公共 API、真实酒馆正则/预设/聊天、正式 dist 与 Git 受保护。登记时工作区为 148 个已跟踪修改、116 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 251×3＝753/753；完整 `pnpm verify` 为 220/220 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、118 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十六、C09 工作树归属

C09 仅拥有 prompts 隔离数据 fixture、`prompts-library-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。prompts 生产代码与样式、schema/公共 API、类型提示词/任务模板语义、真实预设/聊天、正式 dist 与 Git 受保护。登记时工作区为 148 个已跟踪修改、118 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 252×3＝756/756；完整 `pnpm verify` 为 222/222 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、119 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十七、C10 工作树归属

C10 仅拥有 prompts 隔离配置 fixture、`prompts-config-save` 场景、对应静态契约、正式零 finding 基线和执行文档。prompts 生产代码与样式、schema/公共 API、模板默认值/产品语义、真实预设/聊天、正式 dist 与 Git 受保护。登记时工作区为 148 个已跟踪修改、119 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2；首次三尺寸准确发现 fixture 导航栈不真实，修正后 T1 契约 2/2、三尺寸 3/3、正式 253×3＝759/759；完整 `pnpm verify` 为 224/224 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、120 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百三十八、DISC-C 本地结束与 T3 边界

DISC-C 的本地生产代码、隔离 fixture、场景、基线和执行记录已经反查；C02—C10 全部通过。真实聊天/备份恢复与改名、酒馆原生预设生命周期、真实世界书写入和运行时预设/正则应用未执行，归属 `F00-T3`，必须另行授权并使用专用测试角色/聊天及可恢复快照。

## 一百三十九、DISC-D 工作树归属

DISC-D 只读验收阶段 4 D 批的加工与媒体 App；未登记新 D 编号前，生产源码、样式、schema、测试/视觉基线、正式 dist、Git、真实聊天/文件/API 均受保护。开始时工作区为 148 个已跟踪修改、120 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

## 一百四十、D01 工作树归属

D01 仅拥有共享媒体域的隔离视觉 fixture、`media-library-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。Gallery/Music/Video/Media 生产代码与样式、schema/公共 API、媒体播放/下载/生成、真实聊天/文件/API、正式 dist 与 Git 受保护。登记时工作区为 148 个已跟踪修改、120 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 254×3＝762/762；完整 `pnpm verify` 为 226/226 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 148 个已跟踪修改、122 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十一、D02 工作树归属

D02 仅拥有小游戏隔离设置 fixture、10 个 `game-*-play` 场景、对应静态契约、正式零 finding 基线和执行文档。全部游戏生产组件/store/规则、schema/备份格式/公共 API、正式 dist、Git 与真实设置/聊天受保护。登记时工作区为 148 个已跟踪修改、122 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、10 场景三尺寸 30/30、正式 264×3＝792/792；完整 `pnpm verify` 为 228/228 单元、37/37 机器、75/75 UI/交互、15/15 外观。测试 fixture 仅在模块加载前后补齐确定性 lodash `shuffle`；完成时工作区 149 个已跟踪修改、124 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十二、D03 工作树归属

D03 仅拥有 workbench 隔离数据 fixture、`workbench-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。Workbench 生产组件/store/runner、schema/公共 API、真实工作流运行/聊天插入/API、正式 dist 与 Git 受保护。登记时工作区为 149 个已跟踪修改、124 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 265×3＝795/795；完整 `pnpm verify` 为 230/230 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 149 个已跟踪修改、126 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十三、D04 工作树归属

D04 仅拥有 chat-insert 隔离视觉场景、测试期间临时安装并恢复的内存 `createChatMessages`/`setChatMessages`/`saveChat` 函数、对应静态契约、正式零 finding 基线和执行文档。ChatInsert 生产组件/store/util、schema/公共 API、真实聊天/引用/文件/API、正式 dist 与 Git 受保护。登记时工作区为 149 个已跟踪修改、126 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 266×3＝798/798；完整 `pnpm verify` 为 232/232 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 149 个已跟踪修改、128 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十四、D05 工作树归属

D05 仅拥有 cloud-media 隔离设置 fixture、`cloud-media-profile-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。CloudMedia 生产组件/store/provider/generation、schema/公共 API、密钥、真实媒体/文件/API、正式 dist 与 Git 受保护。登记时工作区为 149 个已跟踪修改、128 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 267×3＝801/801；完整 `pnpm verify` 为 234/234 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 149 个已跟踪修改、130 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十五、D06 工作树归属

D06 仅拥有 comfy 隔离 settings fixture、`comfy-workflow-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。Comfy 生产组件/store/网络实现、schema/公共 API、文件导入、读取模型、真实 Comfy/API/媒体、正式 dist 与 Git 受保护。登记时工作区为 149 个已跟踪修改、130 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 268×3＝804/804；完整 `pnpm verify` 为 236/236 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 149 个已跟踪修改、132 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十六、D07 工作树归属

D07 仅拥有 App 工坊隔离 extension settings fixture、`custom-app-definition-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。AppBuilder/store/schema/runtimeModules 生产代码与样式、导入导出、公共 API、真实自制 App/聊天/文件、正式 dist 与 Git 受保护。登记时工作区为 149 个已跟踪修改、132 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 269×3＝807/807；完整 `pnpm verify` 为 238/238 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 149 个已跟踪修改、134 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十七、D08 工作树归属

D08 仅拥有 MVU 修改器视觉内存 runtime fixture、`mvu-modifier-crud` 场景、对应静态契约、正式零 finding 基线和执行文档。MVU 生产组件/model/api、持久化 store/schema/公共 API、真实 MVU/聊天数据、正式 dist 与 Git 受保护。登记时工作区为 149 个已跟踪修改、134 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 270×3＝810/810；完整 `pnpm verify` 为 240/240 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 149 个已跟踪修改、136 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十八、D09 工作树归属

D09 仅拥有场景编排隔离 scope fixture、`scene-planner-history` 场景、对应静态契约、正式零 finding 基线和执行文档。ScenePlanner 生产组件/store/generation、schema/公共 API、真实聊天/生成/API、正式 dist 与 Git 受保护。登记时工作区为 149 个已跟踪修改、136 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

完成反查：T0 0/2、T1 契约 2/2、三尺寸 3/3、正式 271×3＝813/813；完整 `pnpm verify` 为 242/242 单元、37/37 机器、75/75 UI/交互、15/15 外观。完成时工作区 149 个已跟踪修改、138 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百四十九、DISC-D 本地结束与阶段 5 边界

DISC-D 已反查 07 阶段 4 明列的全部加工与媒体域；D01—D09 均通过。真实媒体播放/下载/生成、外部 API 和真实聊天写入未执行，也不计入本地完成。下一步只读审计阶段 5 尚未随业务批次拆分的纯结构债务；在登记独立结构缺陷与批次契约前，不修改生产协调器、内部接口、schema、备份格式、共享公共 API、正式 dist、Git 或真实数据。

## 一百五十、DISC-S01 与 S01A 工作树归属

DISC-S01 只读确认 PhoneOverlay 仍混有五类生命周期所有权，并登记 `D-STRUCT-PHONE-001`。S01A 只拥有新增的内部 toast 桥接 composable、PhoneOverlay 的等价调用替换、一项结构契约和执行文档；notice store/公共 API、提示语义、模板、样式、其他协调器职责、schema、备份、正式基线/dist、Git 和真实数据/API 受保护。登记时工作区为 149 个已跟踪修改、138 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

S01A 完成反查：结构契约 1/1、定向三尺寸 9/9、完整门禁 243/243 单元与 75/75 UI/交互通过；根脚本由约 1065 行降至 999 行。完成时工作区 149 个已跟踪修改、140 个未跟踪文件、Git index 0，正式 dist 哈希不变。S01B 只拥有内部路由滚动 composable、等价调用替换、一项结构契约和执行文档；PhoneRoute/store 公共 API、路由/返回和 0/80/240ms 恢复语义受保护。

S01B 完成反查：结构契约 1/1、四类滚动返回三尺寸 12/12、完整门禁 244/244 单元与 75/75 UI/交互通过；根脚本降至约 953 行。完成时工作区 149 个已跟踪修改、142 个未跟踪文件、Git index 0，正式 dist 哈希不变。S01C 只拥有内部窗口位置 composable、等价解构/调用、一项结构契约和执行文档；settings schema/store 公共 API、默认定位、6px 阈值、首页 App 拖动和真实设置受保护。

S01C 完成反查：结构契约 1/1、home/drag/settings 三尺寸 9/9、完整门禁 245/245 单元与 75/75 UI/交互通过；根脚本降至约 846 行。完成时工作区 149 个已跟踪修改、144 个未跟踪文件、Git index 0，正式 dist 哈希不变。S01D 只拥有内部标题适配 composable、等价 refs/调用、一项结构契约和执行文档；顶栏模板、11—14px 语义、主题/字体设置和公共 API 受保护。

S01D 完成反查：结构契约 1/1、home/preset/reader 三尺寸 9/9、完整门禁 246/246 单元与 75/75 UI/交互通过；根脚本降至约 811 行。完成时工作区 149 个已跟踪修改、146 个未跟踪文件、Git index 0，正式 dist 哈希不变。S01E 只拥有内部 PhoneHome 组件、首页唯一状态/模板/专属样式的等价迁移、根组件装载替换、一项结构契约和执行文档；公共 API/schema、产品语义、全局控件、正式基线/dist 与真实数据受保护。

S01E 完成反查：结构契约及迁移后的弹窗/图标契约 5/5、首页全组三尺寸 15/15、完整门禁 247/247 单元与 75/75 UI/交互通过；PhoneOverlay 由约 2027 行降至 662 行。严格 UI 基线只删除已迁移 24px 文件夹控制的旧 finding，并以缺陷号注释保留业务例外，当前 36 条、0 新增。完成时工作区 150 个已跟踪修改、148 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百五十一、DISC-S02 工作树归属

DISC-S02 只读审计 ReaderApp、RecoveryApp 与 ChatArchiveApp 的现有页面/会话边界和回归证据；未登记新结构缺陷前不修改生产源码、样式、schema、备份格式、公共 API、测试/基线、正式 dist、Git 或真实聊天/文件/API。开始时工作区为 150 个已跟踪修改、148 个未跟踪文件，Git index 为 0，正式 dist 哈希不变。

DISC-S02 完成只读确认三者均有不同结构债务；选择已有独立弹窗和视觉/工具护栏的 Reader 文本编辑会话作为最小根因并登记 `D-STRUCT-READER-001`。S02A 只拥有一个内部文本编辑会话 composable、ReaderApp 等价调用替换、一项结构契约和执行文档；Reader/store/runtime 公共 API、schema、备份格式、模板/文案/样式、聊天加载、普通整层编辑、正式基线/dist、Git 与真实聊天/API 受保护。

S02A 完成反查：结构与既有编辑工具测试 4/4、详情/文本编辑弹窗三尺寸 6/6、完整门禁 248/248 单元与 75/75 UI/交互通过；ReaderApp 降至 1580 行。完成时工作区 150 个已跟踪修改、150 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。S02B 只拥有一个内部聊天来源会话 composable、ReaderApp 的等价调用/重置替换、一项结构契约和执行文档；运行时 API 参数/顺序、规则/归一化、公共 API、schema、备份、模板/样式、正式基线/dist、Git 与真实聊天/API 受保护。

S02B 完成反查：结构与相邻会话测试 5/5、Reader 六场景三尺寸 18/18、完整门禁 249/249 单元与 75/75 UI/交互通过；ReaderApp 降至 1440 行、根脚本 629 行。完成时工作区 150 个已跟踪修改、151 个未跟踪文件、Git index 0；正式 dist 哈希保持不变。DISC-S03 只读审计 RecoveryApp 与 ChatArchiveApp 的会话边界；未登记新缺陷前不修改生产源码、样式、store/API、schema、备份格式、测试/基线、正式 dist、Git 或真实备份/设置/聊天/API。

DISC-S03 完成选择 Recovery 的只读详情/导入三页作为最小连续流程并登记 `D-STRUCT-RECOVERY-001`。S03A 只拥有一个内部 `RecoveryReadImportFlow` 页面组件及专属样式、RecoveryApp 等价装载/旧分支删除、一项结构契约和执行文档；导入/跳转/删除 API、store/schema、备份格式、设置快照/查重/清理/书架、公共 API、正式基线/dist、Git 与真实数据/API 受保护。

S03A 完成反查：结构/Recovery 安全契约 2/2、详情/确认/结果三尺寸 9/9、完整门禁 250/250 单元与 75/75 UI/交互通过；RecoveryApp 降至 1393 行、根脚本 473 行。完成时工作区 151 个已跟踪修改、153 个未跟踪文件、Git index 0；正式 dist 哈希不变。S03B 只拥有一个内部聊天备份维护页面组件及专属样式、RecoveryApp 等价装载/入口/旧分支删除、迁移既有安全契约、一项结构契约和执行文档；扫描/哈希/删除语义、store/schema、备份格式、设置快照、书架/详情、公共 API、正式基线/dist、Git 与真实备份/API 受保护。

S03B 完成反查：结构/Recovery 安全契约 3/3、查重/清理三尺寸 6/6、完整门禁 251/251 单元与 75/75 UI/交互通过；RecoveryApp 降至 915 行、根脚本 269 行。完成时工作区 151 个已跟踪修改、155 个未跟踪文件、Git index 0；正式 dist 哈希不变。S03C 只拥有 Recovery 视觉 fixture、正式场景目录/契约、正式视觉 finding 基线中新增场景三条空记录和执行文档；生产组件/store/schema/API、真实设置文件、交互删除、其他基线记录、正式 dist 与 Git 受保护。

S03C 完成反查：场景契约 1/1、正式全目录 272×3＝816/816、新场景普通比较 3/3、完整门禁 252/252 单元与 75/75 UI/交互通过；正式基线只新增目标三尺寸空 finding。S03D 只拥有一个内部设置快照页面组件及专属样式、RecoveryApp 等价装载/入口/旧分支删除、迁移既有安全契约、一项结构契约和执行文档；设置 API/文案、store/schema/文件格式、聊天备份其他页面、视觉 fixture/正式基线、正式 dist、Git 与真实设置文件/API 受保护。

S03D 完成反查：结构/Recovery 安全契约 2/2、设置列表/查重三尺寸 6/6、完整门禁 253/253 单元与 75/75 UI/交互通过；`RecoveryApp.vue` 降至 483 行、根脚本 135 行。删除两条随迁移暴露的旧局部按钮尺寸覆盖后，严格 UI 例外由 36 降至 34；完成时工作区 152 个已跟踪修改、158 个未跟踪文件、Git index 0，正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。DISC-S04 只读审计 ChatArchive 页面/事务边界；未登记新结构缺陷前不修改生产代码、测试、基线、store/schema、楼层备份格式、共享 API、正式 dist、Git 或真实酒馆聊天/角色/API。

DISC-S04 完成命中 `archive-floor-backup` 在空测试库下可跳过阅读页核心断言，登记 `D-TEST-ARCHIVE-FLOOR-001 / S04A`。S04A 只拥有 ChatArchive 测试视觉场景、一个测试专用固定夹具模块、一项静态契约、必要时目标场景三条正式 finding 基线和执行文档；生产 ChatArchive、`chatFloorBackup` schema/格式/API、store/共享 API、其他场景、正式 dist、Git 与真实酒馆/IndexedDB 受保护。

S04A 完成反查：静态 1/1、固定楼层备份阅读三尺寸 3/3、完整门禁 254/254 单元与 75/75 UI/交互通过；正式视觉 finding 基线未改。完成时工作区 152 个已跟踪修改、160 个未跟踪文件、Git index 0，正式 dist 哈希保持不变。S04B 只拥有一个内部楼层备份阅读页面组件及专属样式、ChatArchive 根组件等价装载/旧视图删除、一项结构契约和执行文档；备份 CRUD/恢复/自动保存、IndexedDB schema/格式/API、详情/索引、改名/迁移、视觉场景/基线、正式 dist、Git 与真实数据/API 受保护。

S04B 完成反查：结构/思维链 4/4、固定阅读页三尺寸 3/3、完整门禁 255/255 单元与 75/75 UI/交互通过；ChatArchive 根为 1362 行/865 行脚本。完成时工作区 152 个已跟踪修改、162 个未跟踪文件、Git index 0，正式 dist 哈希不变。S04C 只拥有一个内部角色/聊天目录会话 composable、根组件等价消费替换、一项结构契约和执行文档；模板/样式、备份写事务/自动服务、改名/跳转/迁移、schema/格式/公共 API、视觉场景/基线、正式 dist、Git 与真实数据/API 受保护。

S04C 完成反查：目录会话结构契约 1/1、`archive-owner-list`/`archive-floor-backup` 三尺寸 6/6、完整门禁 256/256 单元、75/75 UI/交互与 15/15 外观通过；ChatArchive 根为 932 行/435 行脚本，写事务仍由根唯一持有。完成时工作区 152 个已跟踪修改、163 个未跟踪文件、Git index 0；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。

## 一百五十二、F00-local 工作树归属

F00-local 只拥有 272 个正式视觉场景 × 3 尺寸的普通比较、最终工作树/哈希反查和执行文档；没有精确失败时不拥有生产代码或视觉基线。正式 `dist`、schema、备份格式、共享公共 API、真实酒馆数据/API、产品语义、Git 提交与推送受保护。开始时以前述 S04C 完成工作树为基线。

F00-local 完成反查：正式全目录 272×3＝816/816、15/15 外观证据通过且未更新基线；严格 UI 复用为 34 条既有 finding、0 新增。完成时工作区 152 个已跟踪修改、165 个未跟踪文件、Git index 0，`tmp/ui-check/f00-local` 未进入 Git 状态；正式 dist 哈希保持 `A1C335D962CC59463FDF8F7F06BC9DC3FBF82A606017E809FCD92FAE77D26C02` / `BDE849420EB6E882C835AED509FEDBA722082349DBE8C388F470B380D701EE03`。本地自动执行已完成；`F00-T3`、正式 dist、提交与推送均等待单独授权。

## 一百五十三、F00-T3 工作树归属

F00-T3 经用户授权只操作测试角色“李沐晨”、聊天“测试用”、当前 API，以及本批创建且名称以 `F00-T3-` 开头的临时预设/世界书。允许把正式 `dist` 临时加载到 PID 30184 的隔离 Chrome；禁止覆盖服务端扩展、提交或推送。生产修改只拥有真实复验登记出的预设管理来源路由串线和不安全酒馆改名防线，以及对应定向契约、正式 dist 与执行文档。

完成反查：内置日记预设捕获/生成、世界书 `constant=蓝灯` 与 `selective=绿灯`、非当前酒馆预设删除均在隔离环境验证。酒馆预设详情入口现显式清除插件 ID/来源；酒馆改名在输入弹窗前安全阻止，插件私有预设改名保持原语义。所有 `F00-T3-*` 临时预设/世界书已删除，当前预设恢复“荧惑·万象剧场”，酒馆回到“李沐晨 / 测试用”12 层、11 条正则。隔离 Chrome 配置仅为本批临时测试资料，结束后删除。最终完整门禁为 259/259 单元、75/75 UI/交互、15/15 外观证据，正式 dist 哈希为 `6118F813E36EF6A08DD4AE816A6DFD340AF33634E1F49D0BA9B96ACF8491A18E` / `BA741F4FC22FA702B455E1890FE65904E840FD5BCA1066E5DE731BB0C8ECFE1D`；完成时工作区 156 个已跟踪修改、166 个未跟踪文件、Git index 0，未提交或推送。

## 一百五十四、G01 工作树归属

G01 只拥有 `GuessNumberGame.vue` 的统计区业务布局钩子、小游戏既有真实玩法场景中的文字溢出断言、猜数字定向契约、`D-UI-GAME-001` 与过期执行状态校正、正式 dist。猜数字玩法/存储/输入、其他小游戏、全局统计卡、视觉基线、真实酒馆数据/API、Git 提交与推送受保护。

完成反查：布局只新增 `0.8fr/0.8fr/1.4fr` 三轨内容权重；350/390/430 三尺寸真实玩法 3/3 且六段统计文字均无溢出，350px 截图人工核对通过。完整门禁为 260/260 单元、75/75 UI/交互、15/15 外观证据，严格 UI 复用 34 条既有/0 新增。正式 dist 哈希更新为 `C750CE8C9AE39482D6C848F7B08E08BD67C95CE8220585838D40F7473A89B75E` / `AE17AD89CB6165CA9EAAB933CDBEBF7D5BDFC83C14FA5102362CD8AD7832E709`；工作区 156 个已跟踪修改、166 个未跟踪文件、Git index 0。按用户要求在此暂停，未部署、提交或推送。
