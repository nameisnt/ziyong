当用户输入`先分析`时，不要修改任何代码。

## UI 复用优先硬规则

制作或修改任何前端 UI 前，必须先检查现有全局样式、组件和文档，优先复用，不要在 App 内重新造一套相同控件。

- 新增按钮优先使用全局类：`.pc-primary-btn`、`.pc-soft-btn`、`.pc-icon-btn`、`.pc-segment-btn`。
- 新增输入控件优先使用全局类：`.pc-field`、`.pc-select`、`.pc-area`、`.pc-field-group`、`.pc-field-label`。
- 新增卡片优先使用全局类：`.pc-section-card`、`.pc-editor-card`。
- 新增表单底部操作区优先使用全局类：`.pc-form-actions`。
- 新增空状态优先使用 `EmptyState.vue`。
- 新增生成页优先使用 `GenerationPanel.vue`，生成预览优先使用 `GenerationPreviewPanel.vue`。
- 新增引用选择优先使用 `ReferencePicker.vue`。
- 新增八股检测优先使用 `BaguScanPanel.vue`。
- 新增提示说明优先使用 `InfoHint.vue`。
- 新增详情页底部导航优先使用 `DetailFooter.vue`。

禁止在单个 App 的 scoped style 中重复定义全局控件基础样式，例如 `.pc-primary-btn`、`.pc-soft-btn`、`.pc-icon-btn`、`.pc-field`、`.pc-select`、`.pc-area`、`.pc-form-actions`。只有该 App 独有布局、局部排列或特殊状态可以写局部 CSS。

新增主题相关颜色必须使用全局主题变量，例如 `--pc-theme-accent`、`--pc-surface`、`--pc-surface-strong`、`--pc-border`、`--pc-text`、`--pc-muted`、`--pc-danger`。不要新增绕过主题系统的变量或硬编码主题色，除非是明确的业务状态色并有注释说明。

每次 UI 改动后，运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/check-ui-reuse.ps1
```

该脚本默认只提醒不阻塞；如果需要把疑似重复样式作为失败处理，可追加 `-Strict`。

如果脚本提示疑似重复全局样式，需要优先改成复用；确实需要保留时，在代码旁添加简短注释说明原因。

## 重复缺陷防复发硬规则

修改现有功能或新建 App 时，不能只让当前页面“看起来能用”。开始编码前必须先查找同类页面、全局组件、共享流程和历史问题，选定一个已经合格的参考实现；没有合格实现时，应先建立共享规范或组件，禁止在新 App 内复制一套临时实现。

- 用户指出“其他页面也有”“又出现同样问题”或问题属于共享交互时，必须全库搜索同类模式，但修改范围只包括与本次要求具有同一根因、同一共享入口的命中项。搜索到的无关问题只报告，不得顺手修改。
- 新 App 必须复用既有按钮、输入框、标签页、弹窗、详情页、生成页、阅读页和错误反馈体系。禁止自行决定另一套字号、高度、间距、圆角、透明度或操作布局。
- 每个用户可见按钮和可点击区域必须验证真实事件链，包括正常、禁用、加载和失败状态。不得保留无处理器按钮、被遮挡按钮、点击无反馈或只在视觉上存在的操作。
- 涉及数据对象时，必须逐项判断新建、查看、编辑、删除/解除、导入、导出、失败恢复和作用域切换是否需要支持。不需要的能力也要在验收记录中明确标为“不适用”，不能因为漏想而缺失。
- 解析或生成失败时必须保留原始内容、明确错误位置，并允许修改后重新解析；禁止失败后清空内容或只弹一句失败。
- UI 修改至少验证 `350x700`、`390x844`、`430x900`，并检查日间与夜间模式。不能以构建成功或单张截图代替按钮可用性、滚动、弹窗、返回和状态保持验证。
- 用户多次调整方案时，编码前必须整理唯一的“当前需求基线”，把已被替代的旧要求明确标为失效，禁止把不同轮次的要求自行拼接。代码审计或“评价”发现的问题必须转成可定位的缺陷项、修复范围和回归验证，不能只停留在文字结论。
- 禁止“顺手优化”“顺便重构”“顺手补功能”和“以防万一”的兜底。不得自行增加默认值、静默回退、兼容分支、重复状态、备用事件链或第二套实现。确有数据安全或版本兼容需要时，必须先说明触发条件、影响范围、失败表现和移除条件，由用户确认后再实施。
- 完成前必须按用户要求逐条建立验收表，记录实现位置和验证证据。未验证的项目只能报告“未验证”，不得笼统声称“已完成”或“已检查其他页面”。

详细执行协议及已知复发问题清单见 `docs/07-当前代码与UI复用审计及设计要求.md` 第九节。

当用户要求按方案自动进行、连续执行多个阶段或无人监督完成时，必须先遵守该文档第十一节“自动执行协议”。在完成工作树归属登记和当前批次契约前，只能进行只读检查，不得修改业务代码。自动执行只能在当前批次全部验证通过、未触及用户真实数据且未扩大到不同根因时继续；涉及删除覆盖、真实聊天、真实 API、schema、兼容回退、Git 提交推送或不可逆操作时必须停止并请求授权。
